# Reviewed Translation

- Source language: zh-CN
- Target language: en-001
- Scope: editable DOCX text only; image-contained Chinese is preserved.

## p-0007-dc198198ea87

- Location: `paragraph:7`

Source:

```text
OrinNano人工智能实验平台
```

Translation:

```text
OrinNano Artificial Intelligence Experiment Platform
```

## p-0008-83a9fb6be60e

- Location: `paragraph:8`

Source:

```text
——《PyQt5、应用开发》实验手册
```

Translation:

```text
-- PyQt5 Application Development Lab Manual
```

## p-0031-9cc20ba819b5

- Location: `paragraph:31`

Source:

```text
安全提示
```

Translation:

```text
Safety Instructions
```

## p-0033-65ee7f380b63

- Location: `paragraph:33`

Source:

```text
本文档为OrinNano物联网人工智能实验平台的《PyQt5应用开发》手册，使用此产品前必须仔细阅读本文档，为了保证产品正常安全运行，请注意以下事项：
```

Translation:

```text
This document is the "PyQt5 Application Development" manual for the OrinNano Internet of Things Artificial Intelligence Experiment Platform. You must read this document carefully before using this product. In order to ensure the normal and safe operation of the product, please pay attention to the following matters:
```

## p-0034-888d4c8ca6ea

- Location: `paragraph:34`

Source:

```text
1）在使用本机之前，请仔细阅读本用户手册并小心保存以供参考，本用户手册中的图解和说明与实物可能会有稍许差异，请以实物为准。
```

Translation:

```text
1) Before using this machine, please read this user manual carefully and save it carefully for reference. The illustrations and instructions in this user manual may be slightly different from the actual product. Please refer to the actual product.
```

## p-0035-6ecaefef38c7

- Location: `paragraph:35`

Source:

```text
2）设备应运行干燥和空气流通环境下，严禁在高温高压、强磁场、腐蚀等恶劣环境下工作。
```

Translation:

```text
2) The equipment should be operated in a dry and well-ventilated environment. It is strictly prohibited to work in harsh environments such as high temperature, high pressure, strong magnetic field, corrosion, etc.
```

## p-0036-5129a8a58bae

- Location: `paragraph:36`

Source:

```text
3）由于电子产品的特殊性，请勿使水、油、化学试剂等导电液体或腐蚀性液体流到PCB板上，避免在潮湿环境下使用本产品。
```

Translation:

```text
3) Due to the particularity of electronic products, do not allow conductive liquids such as water, oil, and chemical reagents or corrosive liquids to flow onto the PCB board, and avoid using this product in humid environments.
```

## p-0037-bb9704f08ae0

- Location: `paragraph:37`

Source:

```text
4）实验平台须使用配套的电源适配器，不能将其他不同参数的电源接入本设备使用。
```

Translation:

```text
4) The experimental platform must use a matching power adapter, and other power supplies with different parameters cannot be connected to this equipment for use.
```

## p-0038-7d0f52dc3e64

- Location: `paragraph:38`

Source:

```text
5）在每次对模块进行插拔或连接前必须断电状态下操作，以免出现触电危险或让设备短路。
```

Translation:

```text
5) Before plugging, unplugging or connecting the module each time, the power must be cut off to avoid the risk of electric shock or short circuit of the equipment.
```

## p-0039-7f9ef5af0fd7

- Location: `paragraph:39`

Source:

```text
6）在使用产品过程中出现危险情况，请及时切断电源以免造成更大的损失。
```

Translation:

```text
6) If a dangerous situation occurs while using the product, please cut off the power supply in time to avoid greater losses.
```

## p-0040-daa0bf07776a

- Location: `paragraph:40`

Source:

```text
7）必须严格遵守本机以及用户手册上的警告指示，电子产品应由技术人员进行维护修理，任何非专业人员对本手册涉及的产品进行维护和修理将有可能对产品造成严重伤害。
```

Translation:

```text
7) The warning instructions on this machine and the user manual must be strictly followed. Electronic products should be maintained and repaired by technical personnel. Any maintenance and repair of the products covered by this manual by non-professionals may cause serious damage to the product.
```

## p-0051-41e5243e2d34

- Location: `paragraph:51`

Source:

```text
目录
```

Translation:

```text
Table of Contents
```

## p-0052-7ffed43e1d3a

- Location: `paragraph:52`

Source:

```text
安全提示	2
```

Translation:

```text
Safety Tip 2
```

## p-0053-10c6cde85eaf

- Location: `paragraph:53`

Source:

```text
目录	3
```

Translation:

```text
Table of Contents 3
```

## p-0054-bd4d829ec726

- Location: `paragraph:54`

Source:

```text
第一章 PyQt5应用开发基础	5
```

Translation:

```text
Chapter 1 PyQt5 application development basics 5
```

## p-0055-fc005a649074

- Location: `paragraph:55`

Source:

```text
1.1 PyQt5简介	5
```

Translation:

```text
1.1 Introduction to PyQt5 5
```

## p-0056-5b7a4c952219

- Location: `paragraph:56`

Source:

```text
1.2 实验平台硬件介绍	6
```

Translation:

```text
1.2 Introduction to experimental platform hardware 6
```

## p-0057-a6746a3a2a56

- Location: `paragraph:57`

Source:

```text
第二章 PyQt5环境搭建与使用	7
```

Translation:

```text
Chapter 2 PyQt5 environment construction and use 7
```

## p-0058-7617b097ecdc

- Location: `paragraph:58`

Source:

```text
2.1 PyQt5环境搭建	7
```

Translation:

```text
2.1 PyQt5 environment construction 7
```

## p-0059-73fe4896c076

- Location: `paragraph:59`

Source:

```text
2.2 PyQt5应用开发流程	30
```

Translation:

```text
2.2 PyQt5 application development process 30
```

## p-0060-7be31066357d

- Location: `paragraph:60`

Source:

```text
2.3 导入PyQt5实验实例工程	43
```

Translation:

```text
2.3 Import PyQt5 experimental example project 43
```

## p-0061-cfed88018409

- Location: `paragraph:61`

Source:

```text
第三章 PyQt5基础应用开发	49
```

Translation:

```text
Chapter 3 PyQt5 basic application development 49
```

## p-0062-38671ce458bb

- Location: `paragraph:62`

Source:

```text
3.1 窗口属性设置	49
```

Translation:

```text
3.1 Window property settings 49
```

## p-0063-02d946d37e76

- Location: `paragraph:63`

Source:

```text
3.2 信号与槽	55
```

Translation:

```text
3.2 Signals and Slots 55
```

## p-0064-9bc47b2a1e64

- Location: `paragraph:64`

Source:

```text
3.3 多窗口设计	62
```

Translation:

```text
3.3 Multi-window design 62
```

## p-0065-0e5c7d876637

- Location: `paragraph:65`

Source:

```text
3.4 控件设计-文本类	68
```

Translation:

```text
3.4 Control Design-Text Class 68
```

## p-0066-e2f371ee7239

- Location: `paragraph:66`

Source:

```text
3.5 控件设计-按钮类	75
```

Translation:

```text
3.5 Control Design-Button Class 75
```

## p-0067-72e3127c7aeb

- Location: `paragraph:67`

Source:

```text
3.6 控件设计-时间日期类	80
```

Translation:

```text
3.6 Control design-time and date class 80
```

## p-0068-4799f8a48c5b

- Location: `paragraph:68`

Source:

```text
3.7 控件设计-进度条类	83
```

Translation:

```text
3.7 Control Design-Progress Bar Class 83
```

## p-0069-b284fd2d41ee

- Location: `paragraph:69`

Source:

```text
3.8 控件设计-对话框类	86
```

Translation:

```text
3.8 Control Design-Dialog Class 86
```

## p-0070-3c9af8f64421

- Location: `paragraph:70`

Source:

```text
3.9 布局管理-线性	90
```

Translation:

```text
3.9 Layout Management-Linear 90
```

## p-0071-9211e3dc3da0

- Location: `paragraph:71`

Source:

```text
3.10 布局管理-网格	94
```

Translation:

```text
3.10 Layout Management-Grid 94
```

## p-0072-233d96ca9282

- Location: `paragraph:72`

Source:

```text
第四章 PyQt5进阶应用开发	97
```

Translation:

```text
Chapter 4 PyQt5 Advanced Application Development 97
```

## p-0073-0557f6788159

- Location: `paragraph:73`

Source:

```text
4.1 数据库操作	97
```

Translation:

```text
4.1 Database operations 97
```

## p-0074-26268e472bc4

- Location: `paragraph:74`

Source:

```text
4.2 文件操作	104
```

Translation:

```text
4.2 File operations 104
```

## p-0075-659af083df27

- Location: `paragraph:75`

Source:

```text
4.3 多线程-Qtimer	108
```

Translation:

```text
4.3 Multithreading-Qtimer 108
```

## p-0076-7b08785f59b6

- Location: `paragraph:76`

Source:

```text
4.4 多线程-QThread	111
```

Translation:

```text
4.4 Multithreading-QThread 111
```

## p-0077-bce9217e8c07

- Location: `paragraph:77`

Source:

```text
4.5 网络编程-TCP	115
```

Translation:

```text
4.5 Network Programming-TCP 115
```

## p-0078-74c401279f56

- Location: `paragraph:78`

Source:

```text
附录	124
```

Translation:

```text
Appendix 124
```

## p-0079-70be13cdbf3b

- Location: `paragraph:79`

Source:

```text
附1.1 平台启动	124
```

Translation:

```text
Appendix 1.1 Platform startup 124
```

## p-0080-f80c407765af

- Location: `paragraph:80`

Source:

```text
附1.2 网络连接	126
```

Translation:

```text
Appendix 1.2 Network connection 126
```

## p-0081-d1367cb14f76

- Location: `paragraph:81`

Source:

```text
附1.3 SSH登录	127
```

Translation:

```text
Appendix 1.3 SSH login 127
```

## p-0082-a6c36628f764

- Location: `paragraph:82`

Source:

```text
附1.4 文件拷贝	131
```

Translation:

```text
Appendix 1.4 File copy 131
```

## p-0084-0fa6cf9cd7ba

- Location: `paragraph:84`

Source:

```text
第一章 PyQt5应用开发基础
```

Translation:

```text
Chapter 1 PyQt5 application development basics
```

## p-0085-0165ba6de97d

- Location: `paragraph:85`

Source:

```text
1.1 PyQt5简介
```

Translation:

```text
1.1 Introduction to PyQt5
```

## p-0086-6e7bc0ede1d5

- Location: `paragraph:86`

Source:

```text
PyQt5是基于Digia公司强大的图形程序框架Qt的Python接口，由一组Python模块构成，它是一个创建GUI应用程序的工具包，由Phil Thompson进行开发。
```

Translation:

```text
PyQt5 is a Python interface based on Digia's powerful graphics program framework Qt. It consists of a set of Python modules. It is a toolkit for creating GUI applications and was developed by Phil Thompson.
```

## p-0087-1712876bf666

- Location: `paragraph:87`

Source:

```text
自从1998年，首次将Qt移植到Python上形成PyQt以来，已经发布了PyQt3、PyQt4和PyQt5等三个主要版本。
```

Translation:

```text
Since Qt was first ported to Python to form PyQt in 1998, three major versions have been released: PyQt3, PyQt4 and PyQt5.
```

## p-0089-910b79cc87e7

- Location: `paragraph:89`

Source:

```text
PyQt5的主要特点如下：
```

Translation:

```text
The main features of PyQt5 are as follows:
```

## p-0090-19a5b3027615

- Location: `paragraph:90`

Source:

```text
1）对Qt库进行完全封装
```

Translation:

```text
1) Completely encapsulate the Qt library
```

## p-0091-54dd0f0e6c41

- Location: `paragraph:91`

Source:

```text
2）使用信号/槽机制进行通信
```

Translation:

```text
2) Use signal/slot mechanism for communication
```

## p-0092-09c3d5417917

- Location: `paragraph:92`

Source:

```text
3）提供了一整套进行GUI程序开发的窗口控件
```

Translation:

```text
3) Provides a complete set of window controls for GUI program development
```

## p-0093-d909fda16a44

- Location: `paragraph:93`

Source:

```text
4）本身拥有超过620个类和近6000个函数及方法
```

Translation:

```text
4) It has more than 620 classes and nearly 6,000 functions and methods
```

## p-0094-2711b59529e1

- Location: `paragraph:94`

Source:

```text
5）可以跨平台运行在所有主要的操作系统上，包括UNIX、Windows和Mac OS等。
```

Translation:

```text
5) Can run cross-platform on all major operating systems, including UNIX, Windows and Mac OS, etc.
```

## p-0095-176f26e2774f

- Location: `paragraph:95`

Source:

```text
6）支持使用Qt的可视化设计器进行图形界面设计，并能够自主生成Python代码。
```

Translation:

```text
6) Supports the use of Qt's visual designer for graphical interface design, and can independently generate Python code.
```

## p-0101-63896552d427

- Location: `paragraph:101`

Source:

```text
1.2 实验平台硬件介绍
```

Translation:

```text
1.2 Introduction to experimental platform hardware
```

## p-0102-3183809669f8

- Location: `paragraph:102`

Source:

```text
人工智能机器视觉实验平台采用OrinNano边缘计算网关作为主控，又加入了麦克风阵列、六轴机械臂、无线传感网节点、嵌入式实验单元以及人机交互单元等硬件模块。可用于人工智能领域机器视觉、自然语言等方向的教学或科研，利用平台上丰富的实验单元可以进行人工智能场景化的应用开发，比如门禁人脸识别系统、物品分拣等。实验平台请以实物为准。
```

Translation:

```text
The artificial intelligence machine vision experiment platform uses the OrinNano edge computing gateway as the main control, and also adds hardware modules such as a microphone array, a six-axis robotic arm, a wireless sensor network node, an embedded experimental unit, and a human-computer interaction unit. It can be used for teaching or scientific research in the fields of machine vision and natural language in the field of artificial intelligence. The rich experimental units on the platform can be used to develop scenario-based applications of artificial intelligence, such as access control face recognition systems, item sorting, etc. Please refer to the actual product for the experimental platform.
```

## p-0110-35b5ba5916a6

- Location: `paragraph:110`

Source:

```text
第二章 PyQt5环境搭建与使用
```

Translation:

```text
Chapter 2 PyQt5 environment construction and use
```

## p-0111-e27d3bc75843

- Location: `paragraph:111`

Source:

```text
2.1 PyQt5环境搭建
```

Translation:

```text
2.1 PyQt5 environment construction
```

## p-0113-b777f5b08077

- Location: `paragraph:113`

Source:

```text
1.Anaconda3简介
```

Translation:

```text
1.Anaconda3 Introduction
```

## p-0114-aa3a5c15344f

- Location: `paragraph:114`

Source:

```text
Anaconda3 是一个开源的 Python 和 R 数据科学平台，专注于简化包管理、环境管理和科学计算任务。它集成了 Conda（跨平台包和环境管理器）、Jupyter Notebook、Spyder IDE 等工具，广泛应用于 数据分析、机器学习、科学计算等领域。
```

Translation:

```text
Anaconda3 is an open source Python and R data science platform focused on simplifying package management, environment management and scientific computing tasks. It integrates Conda (cross-platform package and environment manager), Jupyter Notebook, Spyder IDE and other tools, and is widely used in data analysis, machine learning, scientific computing and other fields.
```

## p-0116-d2d3619694c1

- Location: `paragraph:116`

Source:

```text
2.核心特点：
```

Translation:

```text
2. Core features:
```

## p-0117-7588a45f08cf

- Location: `paragraph:117`

Source:

```text
1）	强大的包管理（Conda）
```

Translation:

```text
1) Powerful package management (Conda)
```

## p-0118-e8ba0100104f

- Location: `paragraph:118`

Source:

```text
支持 Python 和 R 的库安装与管理（如 NumPy、Pandas、TensorFlow）。
```

Translation:

```text
Supports Python and R library installation and management (such as NumPy, Pandas, TensorFlow).
```

## p-0119-0bdb9fa39c92

- Location: `paragraph:119`

Source:

```text
自动解决依赖关系，避免版本冲突。
```

Translation:

```text
Automatically resolve dependencies and avoid version conflicts.
```

## p-0120-483f6e948cea

- Location: `paragraph:120`

Source:

```text
提供 conda install 和 pip install 两种方式管理包。
```

Translation:

```text
Provides two ways to manage packages: conda install and pip install.
```

## p-0122-fee2f244145a

- Location: `paragraph:122`

Source:

```text
虚拟环境管理
```

Translation:

```text
Virtual environment management
```

## p-0123-6dfbd620006f

- Location: `paragraph:123`

Source:

```text
可创建独立的 Python 环境，不同项目可使用不同版本的 Python 和库：
```

Translation:

```text
Independent Python environments can be created, and different projects can use different versions of Python and libraries:
```

## p-0125-b6afe0f46845

- Location: `paragraph:125`

Source:

```text
预装 1500+ 科学计算库
```

Translation:

```text
Pre-installed 1500+ scientific computing libraries
```

## p-0126-36a8d7d50921

- Location: `paragraph:126`

Source:

```text
数据分析：Pandas、NumPy、Matplotlib
```

Translation:

```text
Data analysis: Pandas, NumPy, Matplotlib
```

## p-0127-5ccf488514d2

- Location: `paragraph:127`

Source:

```text
机器学习：Scikit-learn、TensorFlow、PyTorch
```

Translation:

```text
Machine learning: Scikit-learn, TensorFlow, PyTorch
```

## p-0128-e7efaddcf6f5

- Location: `paragraph:128`

Source:

```text
大数据处理：Dask、Vaex
```

Translation:

```text
Big data processing: Dask, Vaex
```

## p-0129-2ae1c931d390

- Location: `paragraph:129`

Source:

```text
可视化：Seaborn、Plotly
```

Translation:

```text
Visualization: Seaborn, Plotly
```

## p-0131-f09f13622c27

- Location: `paragraph:131`

Source:

```text
集成开发工具
```

Translation:

```text
Integrated development tools
```

## p-0132-0760b8cdcb1b

- Location: `paragraph:132`

Source:

```text
Jupyter Notebook（交互式编程和可视化）
```

Translation:

```text
Jupyter Notebook (interactive programming and visualization)
```

## p-0133-d925380a0f1d

- Location: `paragraph:133`

Source:

```text
Spyder（类似 MATLAB 的科学计算 IDE）
```

Translation:

```text
Spyder (MATLAB-like scientific computing IDE)
```

## p-0134-2e0df720f0c3

- Location: `paragraph:134`

Source:

```text
VS Code / PyCharm 兼容
```

Translation:

```text
VS Code / PyCharm compatible
```

## p-0136-f7ab29f3da73

- Location: `paragraph:136`

Source:

```text
跨平台支持
```

Translation:

```text
Cross-platform support
```

## p-0137-add515e27d17

- Location: `paragraph:137`

Source:

```text
Windows / macOS / Linux 全平台兼容
```

Translation:

```text
Compatible with all Windows / macOS / Linux platforms
```

## p-0139-b1dcf8e58b9c

- Location: `paragraph:139`

Source:

```text
3.Anaconda3安装
```

Translation:

```text
3.Anaconda3 installation
```

## p-0140-d66481c15583

- Location: `paragraph:140`

Source:

```text
1）在产品光盘->开发环境->PyQt5中找到Anaconda3-2020.11-Windows-x86_64.exe文件，右键选择“以管理员身份运行”选项开始安装，会出现如下界面，选择Next。
```

Translation:

```text
1) Find the Anaconda3-2020.11-Windows-x86_64.exe file in the product CD->Development Environment->PyQt5, right-click and select the "Run as Administrator" option to start the installation. The following interface will appear, select Next.
```

## p-0143-2f0b54f6e6f0

- Location: `paragraph:143`

Source:

```text
2）在下面界面中选择I Agree选项。
```

Translation:

```text
2) Select the I Agree option in the interface below.
```

## p-0146-b02dfc2d9d2c

- Location: `paragraph:146`

Source:

```text
3）在下面界面中选择Next选项。
```

Translation:

```text
3) Select the Next option in the interface below.
```

## p-0149-def4fa6ff912

- Location: `paragraph:149`

Source:

```text
4）在下面界面中通过Browse…按键浏览选择安装路径，然后选择Next选项。
```

Translation:

```text
4) In the following interface, use the Browse... button to browse and select the installation path, and then select the Next option.
```

## p-0152-f9f4d7102ecc

- Location: `paragraph:152`

Source:

```text
5）在下面界面中选择Install选项。
```

Translation:

```text
5) Select the Install option in the interface below.
```

## p-0155-f51597f5e5b0

- Location: `paragraph:155`

Source:

```text
6）耐心等待Anaconda3安装进度的完成。
```

Translation:

```text
6) Wait patiently for the completion of Anaconda3 installation progress.
```

## p-0158-cbf68a0ce2de

- Location: `paragraph:158`

Source:

```text
7）在下面界面中选择Next选项。
```

Translation:

```text
7) Select the Next option in the interface below.
```

## p-0160-51ccea38e909

- Location: `paragraph:160`

Source:

```text
8) 在下面界面中选择Next选项。
```

Translation:

```text
8) Select the Next option in the following interface.
```

## p-0163-4721ea86c63a

- Location: `paragraph:163`

Source:

```text
9) 在下面界面中选择Finish选项，完成Anaconda3的安装。
```

Translation:

```text
9) Select the Finish option in the following interface to complete the installation of Anaconda3.
```

## p-0166-e53124d4d00b

- Location: `paragraph:166`

Source:

```text
2.1.2 tf2虚拟环境
```

Translation:

```text
2.1.2 tf2 virtual environment
```

## p-0167-8533cdae37da

- Location: `paragraph:167`

Source:

```text
1.tf2虚拟环境简介
```

Translation:

```text
1. Introduction to tf2 virtual environment
```

## p-0168-3db885a9a4cd

- Location: `paragraph:168`

Source:

```text
TensorFlow 2 (TF2) 虚拟环境是一种隔离的 Python 运行环境，专门用于开发和运行 TensorFlow 2.x 相关项目。使用虚拟环境可以避免不同项目间的依赖冲突，保持开发环境的整洁和可复现性。
```

Translation:

```text
The TensorFlow 2 (TF2) virtual environment is an isolated Python runtime environment designed for developing and running TensorFlow 2.x related projects. Using a virtual environment can avoid dependency conflicts between different projects and keep the development environment clean and reproducible.
```

## p-0170-3b8dc0507b01

- Location: `paragraph:170`

Source:

```text
2.tf2的作用
```

Translation:

```text
2.The role of tf2
```

## p-0171-40f2fd7fa386

- Location: `paragraph:171`

Source:

```text
版本隔离：不同 TensorFlow 项目可能需要不同版本的 TF 或依赖库
```

Translation:

```text
Version isolation: Different TensorFlow projects may require different versions of TF or dependent libraries
```

## p-0172-bcccc0057b79

- Location: `paragraph:172`

Source:

```text
避免污染系统环境：防止安装的包影响系统其他 Python 项目
```

Translation:

```text
Avoid polluting the system environment: Prevent installed packages from affecting other Python projects on the system
```

## p-0173-a445b8b19ced

- Location: `paragraph:173`

Source:

```text
项目可复现性：可以精确记录项目所需的所有依赖
```

Translation:

```text
Project reproducibility: All dependencies required by the project can be accurately recorded
```

## p-0174-822998dff35a

- Location: `paragraph:174`

Source:

```text
多项目并行开发：同时开发多个 TF2 项目而互不干扰
```

Translation:

```text
Multi-project parallel development: develop multiple TF2 projects at the same time without interfering with each other
```

## p-0176-f4cb469d3e9e

- Location: `paragraph:176`

Source:

```text
3.tf2的搭建
```

Translation:

```text
3. Construction of tf2
```

## p-0177-f78360bcba96

- Location: `paragraph:177`

Source:

```text
1）在产品光盘->开发环境->PyQt5中找到tf2.zip的压缩包文件，并将其解压到Anaconda3\envs文件夹下。
```

Translation:

```text
1) Find the compressed package file of tf2.zip in the product CD->Development Environment->PyQt5, and extract it to the Anaconda3\envs folder.
```

## p-0180-372a3fd74afa

- Location: `paragraph:180`

Source:

```text
2）在开始菜单中找到Anaconda Prompt（Anaconda3）选项。
```

Translation:

```text
2) Find the Anaconda Prompt (Anaconda3) option in the start menu.
```

## p-0183-e671a59a3149

- Location: `paragraph:183`

Source:

```text
2）会弹出cmd命令行窗口，在其中输入如下命令查看环境是否包含tf2包。
```

Translation:

```text
2) A cmd command line window will pop up, enter the following command to check whether the environment contains the tf2 package.
```

## p-0187-da6f45e0dc0c

- Location: `paragraph:187`

Source:

```text
3）输入如下命令可以进入tf2虚拟环境。
```

Translation:

```text
3) Enter the following command to enter the tf2 virtual environment.
```

## p-0191-543e42b45198

- Location: `paragraph:191`

Source:

```text
4）在tf2虚拟环境中输入如下命令可以查看python版本并进入python命令行。
```

Translation:

```text
4) Enter the following command in the tf2 virtual environment to view the python version and enter the python command line.
```

## p-0194-39f8fd3a5e07

- Location: `paragraph:194`

Source:

```text
使用快捷键 Ctrl + Z可以退出环境。
```

Translation:

```text
Use the shortcut key Ctrl + Z to exit the environment.
```

## p-0196-588d2742d39c

- Location: `paragraph:196`

Source:

```text
2.1.3 pyqt5及工具安装
```

Translation:

```text
2.1.3 PyQt5 and tool installation
```

## p-0197-eab75f93a04b

- Location: `paragraph:197`

Source:

```text
1）在tf2命令行中输入如下命令安装 PyQt5。
```

Translation:

```text
1) Enter the following command in the tf2 command line to install PyQt5.
```

## p-0201-f6a5d1d268c3

- Location: `paragraph:201`

Source:

```text
2）继续在tf2命令中输入如下命令安装 pyqt5-tools包。
```

Translation:

```text
2) Continue to enter the following command in the tf2 command to install the PyQt5-tools package.
```

## p-0204-f948ca63c02b

- Location: `paragraph:204`

Source:

```text
3）继续在tf2命令中输入如下命令安装pyinstaller工具包。
```

Translation:

```text
3) Continue to enter the following command in the tf2 command to install the pyinstaller tool package.
```

## p-0209-eb1753f6edd4

- Location: `paragraph:209`

Source:

```text
2.1.4 PyCharm安装与配置
```

Translation:

```text
2.1.4 PyCharm installation and configuration
```

## p-0210-0db47ef1c096

- Location: `paragraph:210`

Source:

```text
1）在产品光盘->开发环境->PyQt5中找到pycharm-community-2020.3.2.exe文件，右键选择“以管理员身份运行”选项开始安装，会出现如下界面，选择Next。
```

Translation:

```text
1) Find the pycharm-community-2020.3.2.exe file in the product CD->Development Environment->PyQt5, right-click and select the "Run as Administrator" option to start the installation. The following interface will appear, select Next.
```

## p-0212-ae2fbfce6beb

- Location: `paragraph:212`

Source:

```text
2）在下面界面中通过Browse…按键浏览选择安装路径，然后选择Next选项。
```

Translation:

```text
2) In the following interface, use the Browse... button to browse and select the installation path, and then select the Next option.
```

## p-0215-17ea935b0b94

- Location: `paragraph:215`

Source:

```text
3）在下面界面中勾选左侧的3个单选框，然后选择Next选项。
```

Translation:

```text
3) Check the 3 radio boxes on the left in the interface below, and then select the Next option.
```

## p-0218-a159c4c547f0

- Location: `paragraph:218`

Source:

```text
4）在下面界面中选择Install选项。
```

Translation:

```text
4) Select the Install option in the interface below.
```

## p-0221-e217c45a2fa7

- Location: `paragraph:221`

Source:

```text
5）耐心等待Pycharm安装进度的完成。
```

Translation:

```text
5) Wait patiently for the completion of the Pycharm installation progress.
```

## p-0224-0f8b9219aa50

- Location: `paragraph:224`

Source:

```text
6）在下面界面中选择Finish选项，完成Pycharm的安装。
```

Translation:

```text
6) Select the Finish option in the interface below to complete the installation of Pycharm.
```

## p-0227-2a071e5c8a3f

- Location: `paragraph:227`

Source:

```text
7）此时桌面上会自动创建快捷方式，双击打开即可运行Pycharm。
```

Translation:

```text
7) At this time, a shortcut will be automatically created on the desktop. Double-click to open it to run Pycharm.
```

## p-0230-00c193213c47

- Location: `paragraph:230`

Source:

```text
8）首次运行Pycharm会弹出如下提示，勾选同意后选择Continue选项。
```

Translation:

```text
8) When you run Pycharm for the first time, the following prompt will pop up. Check the box to agree and select the Continue option.
```

## p-0233-c103272e2db3

- Location: `paragraph:233`

Source:

```text
9）在下面界面中选择Don`t Send选项。
```

Translation:

```text
9) Select the Don`t Send option in the following interface.
```

## p-0236-41648efa450c

- Location: `paragraph:236`

Source:

```text
10）Pycharm软件欢迎界面如下，选择New Project选项。
```

Translation:

```text
10) The welcome interface of Pycharm software is as follows, select the New Project option.
```

## p-0238-fcc360616991

- Location: `paragraph:238`

Source:

```text
11）新建工程时首先在Location选项中选择保存路径，然后勾选Previously configured interpreter单选框，然后点击Interpreter选项后面…按钮。
```

Translation:

```text
11) When creating a new project, first select the save path in the Location option, then check the Previously configured interpreter radio button, and then click the... button behind the Interpreter option.
```

## p-0241-0954caf929d2

- Location: `paragraph:241`

Source:

```text
12）然后会进入如下界面，继续点击Interpreter选项后面…按钮。
```

Translation:

```text
12) Then you will enter the following interface, continue to click the... button behind the Interpreter option.
```

## p-0244-0555fe72c804

- Location: `paragraph:244`

Source:

```text
13）在弹出的对话框中浏览选择Anaconda3\envs\tf2\python.exe，然后点击OK保存。
```

Translation:

```text
13) Browse and select Anaconda3\envs\tf2\python.exe in the pop-up dialog box, and then click OK to save.
```

## p-0247-b7b16436eaed

- Location: `paragraph:247`

Source:

```text
14）回到新建工程界面中后，可以看到Interpreter选项中已选择python，然后选择Create。
```

Translation:

```text
14) After returning to the new project interface, you can see that python has been selected in the Interpreter option, and then select Create.
```

## p-0249-8564c151a77c

- Location: `paragraph:249`

Source:

```text
15）工程新建完成后界面如下。
```

Translation:

```text
15) After the new project is completed, the interface is as follows.
```

## p-0252-af28a02e8ab0

- Location: `paragraph:252`

Source:

```text
16）为了使用方便可以调整一下Pycharm软件的界面风格。首先，在菜单栏选择File->Settings…选项打开设置界面。
```

Translation:

```text
16) For convenience of use, you can adjust the interface style of Pycharm software. First, select the File->Settings… option in the menu bar to open the settings interface.
```

## p-0255-3f5e4d0b7a13

- Location: `paragraph:255`

Source:

```text
17）在设置界面中找到Appearance选项中的Theme主题选项，通过下拉菜单选择IntelliJ Light选项后，点击OK保存。
```

Translation:

```text
17) Find the Theme theme option in the Appearance option in the settings interface, select the IntelliJ Light option through the drop-down menu, and click OK to save.
```

## p-0258-9c246e224be9

- Location: `paragraph:258`

Source:

```text
18）主题修改后Pycharm软件会变成白色风格。
```

Translation:

```text
18) After the theme is modified, the Pycharm software will turn into a white style.
```

## p-0260-c85b22bf4987

- Location: `paragraph:260`

Source:

```text
19）如果Pycharm软件运行时出现如下错误，解决方法如下：
```

Translation:

```text
19) If the following error occurs when the Pycharm software is running, the solution is as follows:
```

## p-0262-13757aeb9b33

- Location: `paragraph:262`

Source:

```text
在运行中输入Regedit命令打开注册表编辑器，依次定位到HKEY_CURRENT_USER＼Software＼Microsoft＼Windows＼Windows Error Reporting选项，并修改DontshowUI的值为1。
```

Translation:

```text
Enter the Regedit command during operation to open the registry editor, navigate to the HKEY_CURRENT_USER\Software\Microsoft\Windows\Windows Error Reporting option, and modify the value of DontshowUI to 1.
```

## p-0264-122723a41392

- Location: `paragraph:264`

Source:

```text
2.1.5 PyQt5设计器配置
```

Translation:

```text
2.1.5 PyQt5 designer configuration
```

## p-0265-fe53a6b7060a

- Location: `paragraph:265`

Source:

```text
QtDesigner配置
```

Translation:

```text
QtDesigner configuration
```

## p-0266-2b5370f4447d

- Location: `paragraph:266`

Source:

```text
1）在菜单栏选择File->Settings…选项打开设置界面。
```

Translation:

```text
1) Select the File->Settings… option in the menu bar to open the settings interface.
```

## p-0268-4bb9e3a8bb29

- Location: `paragraph:268`

Source:

```text
2）在设置界面中找到Tools->External Tools选项，并点击+按键添加Qt Designer。
```

Translation:

```text
2) Find the Tools->External Tools option in the settings interface and click the + button to add Qt Designer.
```

## p-0271-78ed061ea2bb

- Location: `paragraph:271`

Source:

```text
3）点击“+”后会弹出“Create Tool”窗口，按照下表依次填入内容，点击OK按钮保存。
```

Translation:

```text
3) After clicking "+", the "Create Tool" window will pop up. Fill in the content according to the table below and click the OK button to save.
```

## p-0274-7a3a293b194f

- Location: `paragraph:274`

Source:

```text
4）保存后在Tools->External Tools选项中可以看到新添加的Qt Designer工具。
```

Translation:

```text
4) After saving, you can see the newly added Qt Designer tool in the Tools->External Tools option.
```

## p-0277-b39b5094c9e9

- Location: `paragraph:277`

Source:

```text
PyUIC工具配置
```

Translation:

```text
PyUIC tool configuration
```

## p-0278-a3fbaaa6479c

- Location: `paragraph:278`

Source:

```text
5）继续在Tools->External Tools选项中点击+按键添加PyUIC工具。
```

Translation:

```text
5) Continue to click the + button in the Tools->External Tools option to add the PyUIC tool.
```

## p-0281-91afae3b5c82

- Location: `paragraph:281`

Source:

```text
6）点击“+”后会弹出“Create Tool”窗口，按照下表依次填入内容，点击OK按钮保存。
```

Translation:

```text
6) After clicking "+", the "Create Tool" window will pop up. Fill in the content according to the table below and click the OK button to save.
```

## p-0284-aa09bc35b2ab

- Location: `paragraph:284`

Source:

```text
7）保存后在Tools->External Tools选项中可以看到新添加的PyUIC工具。
```

Translation:

```text
7) After saving, you can see the newly added PyUIC tool in the Tools->External Tools option.
```

## p-0287-252e3d002096

- Location: `paragraph:287`

Source:

```text
8）Qt Designer和PyUIC工具配置完成后，在Pycharm软件的Tools->External Tools菜单中会出现之前创建的Qt Designer和pyUIC工具。
```

Translation:

```text
8) After the Qt Designer and PyUIC tools are configured, the previously created Qt Designer and pyUIC tools will appear in the Tools->External Tools menu of the Pycharm software.
```

## p-0291-677f88cc64b0

- Location: `paragraph:291`

Source:

```text
2.2 PyQt5应用开发流程
```

Translation:

```text
2.2 PyQt5 application development process
```

## p-0292-4e831fbd78ae

- Location: `paragraph:292`

Source:

```text
2.2.1 PyCharm新建工程
```

Translation:

```text
2.2.1 PyCharm new project
```

## p-0293-22c3b176f6ce

- Location: `paragraph:293`

Source:

```text
1）在Pycharm软件欢迎界面选择New Project选项新建工程。
```

Translation:

```text
1) Select the New Project option on the Pycharm software welcome interface to create a new project.
```

## p-0295-0a4dc72cd0f8

- Location: `paragraph:295`

Source:

```text
2）在新建工程界面中，在Localtion中选择工程路径，勾选Previously configured interpreter单选框，然后选择Interpreter为Anaconda3\envs\tf2\python.exe，最后点击Create。
```

Translation:

```text
2) In the new project interface, select the project path in Localtion, check the Previously configured interpreter radio button, then select the Interpreter as Anaconda3\envs\tf2\python.exe, and finally click Create.
```

## p-0298-e5fec7c2347f

- Location: `paragraph:298`

Source:

```text
2.2.2 QtDesigner设计UI
```

Translation:

```text
2.2.2 QtDesigner design UI
```

## p-0299-2afb6caaa881

- Location: `paragraph:299`

Source:

```text
1.启动Qt Designer
```

Translation:

```text
1. Start Qt Designer
```

## p-0300-e3247460d51b

- Location: `paragraph:300`

Source:

```text
1）在Pycharm软件的Tools->External Tools菜单中选择之前创建的Qt Designer工具，打开Qt Designer设计器。
```

Translation:

```text
1) Select the previously created Qt Designer tool in the Tools->External Tools menu of the Pycharm software and open the Qt Designer.
```

## p-0303-c8fde26a9acf

- Location: `paragraph:303`

Source:

```text
2）这时会启动一个名为Qt设计师-Qt Designer界面，如下图所示：
```

Translation:

```text
2) At this time, an interface named Qt Designer-Qt Designer will be launched, as shown in the figure below:
```

## p-0306-adf0d4297865

- Location: `paragraph:306`

Source:

```text
2.新建主窗口
```

Translation:

```text
2. Create a new main window
```

## p-0307-d3fef2ac26ed

- Location: `paragraph:307`

Source:

```text
1）在“新建窗体”窗口中，选择“Main Window”选项，然后点击创建按钮。
```

Translation:

```text
1) In the "New Form" window, select the "Main Window" option, and then click the Create button.
```

## p-0308-5819458558cd

- Location: `paragraph:308`

Source:

```text
在PyQt5中最常用的窗口有三种，说明如下：
```

Translation:

```text
There are three most commonly used windows in PyQt5, as described below:
```

## p-0309-8a5b80e85746

- Location: `paragraph:309`

Source:

```text
Main Window：主窗口，提供一个带有菜单栏、工具栏和状态栏的窗口
```

Translation:

```text
Main Window: Main window, providing a window with menu bar, toolbar and status bar
```

## p-0310-211affa9b116

- Location: `paragraph:310`

Source:

```text
Widget：通用窗口，没有嵌入其他控件的窗口
```

Translation:

```text
Widget: a universal window, a window with no other controls embedded in it
```

## p-0311-53052a181002

- Location: `paragraph:311`

Source:

```text
Dialog：对话框窗口，主要用来执行短期任务，或者与用户进行交互，没有菜单栏、工具栏和状态栏。
```

Translation:

```text
Dialog: Dialog window, mainly used to perform short-term tasks or interact with users. It has no menu bar, toolbar and status bar.
```

## p-0314-2be4b786de46

- Location: `paragraph:314`

Source:

```text
2）创建后Qt Designer界面中间会多出一块名为MainWindow的窗口设计区。Qt Designer界面分为菜单栏、工具栏、工具箱、窗口设计区、对象查看器、属性查看器、信号与槽编辑器、动作编辑器、资源管理器等，它们的布局如下。
```

Translation:

```text
2) After creation, there will be an additional window design area called MainWindow in the middle of the Qt Designer interface. The Qt Designer interface is divided into menu bar, toolbar, toolbox, window design area, object viewer, property viewer, signal and slot editor, action editor, resource manager, etc. Their layout is as follows.
```

## p-0317-a2720a441a0b

- Location: `paragraph:317`

Source:

```text
3.设计主窗口
```

Translation:

```text
3. Design the main window
```

## p-0318-da1ffa631561

- Location: `paragraph:318`

Source:

```text
1）创建完主窗口后，主窗口后默认只有一个菜单栏和一个状态栏，在左侧的Widget Box工具箱中找到PushButton控件，选中它并按住鼠标左键，将其拖放到主窗口内即可，如下图所示：
```

Translation:

```text
1) After creating the main window, it contains only a menu bar and a status bar by default. In the Widget Box on the left, locate the PushButton widget, select it, hold down the left mouse button, and drag it into the main window, as shown in the following figure:
```

## p-0321-6d06a4022d3e

- Location: `paragraph:321`

Source:

```text
2）因为新增了UI设计，所以在MainWindow-untiled后尾有个“*”号，表示工程未保存，此时选择文件->保存菜单。
```

Translation:

```text
2) Because the UI design has been added, there is a "*" at the end of MainWindow-untiled, which means that the project has not been saved. At this time, select the File->Save menu.
```

## p-0324-9ce749935791

- Location: `paragraph:324`

Source:

```text
3）点击保存后，会弹出对话框，默认会选择保存到工程路径下，修改文件名为firstPyQt.ui，即与工程同名。
```

Translation:

```text
3) After clicking Save, a dialog box will pop up. By default, it will be selected to save to the project path. Modify the file name to firstPyQt.ui, which is the same name as the project.
```

## p-0326-941a1a4a8673

- Location: `paragraph:326`

Source:

```text
4）UI文件保存完成后，切换到Pycharm软件中时，可以看到firstPyQt.ui文件自动出现在了工程文件列表中。
```

Translation:

```text
4) After the UI file is saved, when you switch to the Pycharm software, you can see that the firstPyQt.ui file automatically appears in the project file list.
```

## p-0328-97c23d3f391e

- Location: `paragraph:328`

Source:

```text
2.2.3 将.ui转换为.py
```

Translation:

```text
2.2.3 Convert .ui to .py
```

## p-0329-4af6977b198a

- Location: `paragraph:329`

Source:

```text
1）选中firstPyQt.ui文件，然后在菜单栏选择Tools->External Tools->pyUIC选项进行转换。
```

Translation:

```text
1) Select the firstPyQt.ui file, and then select the Tools->External Tools->pyUIC option in the menu bar to convert.
```

## p-0332-645311d2e6df

- Location: `paragraph:332`

Source:

```text
2）转换完成后在工程文件列表中会多出来一个firstPyQt.py文件，双击打开它之后可以查看其文件内容。
```

Translation:

```text
2) After the conversion is completed, there will be an additional firstPyQt.py file in the project file list. You can view its file contents by double-clicking it to open it.
```

## p-0335-5cad23ecb596

- Location: `paragraph:335`

Source:

```text
2.2.4 编写main.py
```

Translation:

```text
2.2.4 Writing main.py
```

## p-0336-f44b35c302fe

- Location: `paragraph:336`

Source:

```text
1）通过上面的步骤已经将firstPyQt.ui转换成firstPyQt.py，但是因为firstPyQt.py文件没有程序入口，所以不能直接运行。因此我们需要修改main.py文件，将其作为程序入口，通过它来调用firstPyQt.py运行。
```

Translation:

```text
1) FirstPyQt.ui has been converted into firstPyQt.py through the above steps, but because the firstPyQt.py file does not have a program entry, it cannot be run directly. Therefore, we need to modify the main.py file and use it as the program entry point to call firstPyQt.py to run.
```

## p-0337-b4e6d5c52e2a

- Location: `paragraph:337`

Source:

```text
在main.py中添加代码内容如下，它通过__main__来设置程序入口，并通过MainWindow()对象调用show()函数来显示firstPyQt的UI窗口。
```

Translation:

```text
Add the following code to main.py. It sets the program entry through __main__ and calls the show() function through the MainWindow() object to display the firstPyQt UI window.
```

## p-0340-3b7a26a4f9f4

- Location: `paragraph:340`

Source:

```text
代码添加后如下图所示：
```

Translation:

```text
After the code is added, it will look like the following picture:
```

## p-0343-7b22692dd7de

- Location: `paragraph:343`

Source:

```text
2）然后在菜单栏选择File->Save All选项对修改进行保存。
```

Translation:

```text
2) Then select the File->Save All option in the menu bar to save the changes.
```

## p-0345-df4010f2da34

- Location: `paragraph:345`

Source:

```text
2.2.5 PC端运行PyQt应用
```

Translation:

```text
2.2.5 Running PyQt application on PC
```

## p-0346-130eb11b7ba5

- Location: `paragraph:346`

Source:

```text
1）在菜单栏选择Run->Run‘main’选项运行程序，或者直接在右上角的工具栏点击绿色的运行图标运行程序。
```

Translation:

```text
1) Select the Run->Run ‘main’ option in the menu bar to run the program, or directly click the green run icon on the toolbar in the upper right corner to run the program.
```

## p-0348-962b4807bf8b

- Location: `paragraph:348`

Source:

```text
程序运行后会弹出一个窗口，其界面与之前设计的一致。
```

Translation:

```text
After the program is run, a window will pop up, and its interface is consistent with the previously designed one.
```

## p-0351-d1cf87ff8f35

- Location: `paragraph:351`

Source:

```text
2.2.6 PC端PyQt5应用打包
```

Translation:

```text
2.2.6 PC-side PyQt5 application packaging
```

## p-0352-c14950b99dd5

- Location: `paragraph:352`

Source:

```text
如果PyQt5应用工程已经设计完成，可以通过pyinstaller工具将工程打包成在PC端的.exe可执行程序，操作步骤如下：
```

Translation:

```text
If the PyQt5 application project has been designed, you can use the pyinstaller tool to package the project into an .exe executable program on the PC. The steps are as follows:
```

## p-0353-54a2802f234a

- Location: `paragraph:353`

Source:

```text
1）首先，为了操作方便，将firstPyQt工程文件夹拷贝到磁盘根目录，比如D盘根目录。
```

Translation:

```text
1) First, for the convenience of operation, copy the firstPyQt project folder to the root directory of the disk, such as the root directory of drive D.
```

## p-0355-15f162e23c20

- Location: `paragraph:355`

Source:

```text
2）打开Anaconda3的命令行，通过如下命令切换至tf2虚拟环境。
```

Translation:

```text
2) Open the command line of Anaconda3 and switch to the tf2 virtual environment through the following command.
```

## p-0358-e31dcb233670

- Location: `paragraph:358`

Source:

```text
3）打开cmd命令行进入工程目录，比如下图中的D:\firstPyQt文件夹，操作命令如下。
```

Translation:

```text
3) Open the cmd command line and enter the project directory, such as the D:\firstPyQt folder in the picture below. The operation command is as follows.
```

## p-0363-ea9f7b3a0dce

- Location: `paragraph:363`

Source:

```text
4）输入如下命令，通过pyinstaller工具进行打包。
```

Translation:

```text
4) Enter the following command to package through the pyinstaller tool.
```

## p-0367-8f691421deb3

- Location: `paragraph:367`

Source:

```text
5）在firstPyQt工程目录下会多出一个dist文件夹，其中main.exe为打包的可执行程序。
```

Translation:

```text
5) There will be an additional dist folder in the firstPyQt project directory, in which main.exe is the packaged executable program.
```

## p-0369-a1c511f5a233

- Location: `paragraph:369`

Source:

```text
6）直接运行main.exe即可运行程序。
```

Translation:

```text
6) Run main.exe directly to run the program.
```

## p-0372-a617a4a572c2

- Location: `paragraph:372`

Source:

```text
2.2.7 网关运行PyQt应用
```

Translation:

```text
2.2.7 Gateway runs PyQt application
```

## p-0373-fc35464703aa

- Location: `paragraph:373`

Source:

```text
1）启动实验平台，参照附录1.2连接好网络并查看IP地址，然后通过SSH工具远程登陆到OrinNano边缘计算网关Ubuntu系统的控制台终端。
```

Translation:

```text
1) Start the experimental platform, connect to the network and check the IP address by referring to Appendix 1.2, and then remotely log in to the console terminal of the OrinNano edge computing gateway Ubuntu system through the SSH tool.
```

## p-0375-bbb593a2970d

- Location: `paragraph:375`

Source:

```text
2）参照附录1.3-1.4将工程文件夹firstPyQt拷贝至网关系统内某个目录下。
```

Translation:

```text
2) Refer to Appendix 1.3-1.4 to copy the project folder firstPyQt to a directory in the gateway system.
```

## p-0378-fa922db3a3a1

- Location: `paragraph:378`

Source:

```text
3）通过鼠标打开网关中的终端软件，如果左侧列表中没有该图标，点击左上角所有程序，然后搜索Terminal，将图标拖动至左侧栏即可。
```

Translation:

```text
3) Open the terminal software in the gateway with the mouse. If the icon is not in the list on the left, click on all programs in the upper left corner, then search for Terminal, and drag the icon to the left column.
```

## p-0381-80204d7caafa

- Location: `paragraph:381`

Source:

```text
4）通过键盘输入如下命令进入对应的拷贝的工程路径下，然后执行main.py程序。
```

Translation:

```text
4) Enter the following command through the keyboard to enter the corresponding copied project path, and then execute the main.py program.
```

## p-0382-1b26a0344944

- Location: `paragraph:382`

Source:

```text
cd test/firstPyQt                                                                 #请以实际的拷贝路径为准
```

Translation:

```text
cd test/firstPyQt #Please refer to the actual copy path.
```

## p-0386-6a4885944cda

- Location: `paragraph:386`

Source:

```text
5）程序运行后，在网关屏幕上也会弹出一个Qt的界面，UI与之前设计的一致。
```

Translation:

```text
5) After the program is run, a Qt interface will pop up on the gateway screen, and the UI is consistent with the previous design.
```

## p-0388-d542257ddaea

- Location: `paragraph:388`

Source:

```text
6）注意：如果直接在SSH工具中运行此程序，那么程序会在PC端显示。
```

Translation:

```text
6) Note: If you run this program directly in the SSH tool, the program will be displayed on the PC side.
```

## p-0391-9fbc2c9e012b

- Location: `paragraph:391`

Source:

```text
2.3 导入PyQt5实验实例工程
```

Translation:

```text
2.3 Import PyQt5 experimental example project
```

## p-0392-37f8acbfc964

- Location: `paragraph:392`

Source:

```text
2.3.1 运行实验实例工程
```

Translation:

```text
2.3.1 Run the experimental example project
```

## p-0393-de136848dd48

- Location: `paragraph:393`

Source:

```text
1）将光盘中提供的实验例程代码解压至默认的工程保存路径下（一般默认为C:\Users\用户名\PycharmProjects）。
```

Translation:

```text
1) Unzip the experimental routine code provided in the CD to the default project saving path (generally the default is C:\Users\username\PycharmProjects).
```

## p-0396-e0bcd8c7166f

- Location: `paragraph:396`

Source:

```text
2）在Pycharm软件的欢迎界面中点击Open。
```

Translation:

```text
2) Click Open in the welcome interface of Pycharm software.
```

## p-0399-fbb8ff596dae

- Location: `paragraph:399`

Source:

```text
3）浏览并选择工程的路径，然后点击OK。
```

Translation:

```text
3) Browse and select the path of the project, then click OK.
```

## p-0402-eb8ceca8ec7d

- Location: `paragraph:402`

Source:

```text
4）工程打开后，Pycharm软件中会弹出提示“No Python interpreter configured for the project”，点击其后面的Configure Python interpreter选项。
```

Translation:

```text
4) After the project is opened, the prompt "No Python interpreter configured for the project" will pop up in the Pycharm software. Click the Configure Python interpreter option behind it.
```

## p-0405-1f218b698cc7

- Location: `paragraph:405`

Source:

```text
5）在弹出的选项中选择Python 3.7 (tf2)选项。
```

Translation:

```text
5) Select the Python 3.7 (tf2) option in the pop-up options.
```

## p-0408-4f30f3afcf24

- Location: `paragraph:408`

Source:

```text
6）错误消除后直接运行程序，可以看到导入的实例代码能够正常运行。
```

Translation:

```text
6) After the error is eliminated, run the program directly and you can see that the imported example code can run normally.
```

## p-0412-9cb0f2f4fa0e

- Location: `paragraph:412`

Source:

```text
2.3.2 修改实验实例UI
```

Translation:

```text
2.3.2 Modify the experimental instance UI
```

## p-0413-7f2f43923814

- Location: `paragraph:413`

Source:

```text
在实例代码工程UI界面基础上进行修改，可以大大减少开发时间，具体操作如下：
```

Translation:

```text
Modifying the UI interface of the example code project can greatly reduce development time. The specific operations are as follows:
```

## p-0414-e3247460d51b

- Location: `paragraph:414`

Source:

```text
1）在Pycharm软件的Tools->External Tools菜单中选择之前创建的Qt Designer工具，打开Qt Designer设计器。
```

Translation:

```text
1) Select the previously created Qt Designer tool in the Tools->External Tools menu of the Pycharm software and open the Qt Designer.
```

## p-0416-7a7aa1ff05f2

- Location: `paragraph:416`

Source:

```text
2）Qt Designer设计器默认会弹出新建窗体的提示框。
```

Translation:

```text
2) Qt Designer will pop up a prompt box for creating a new form by default.
```

## p-0419-64a0581b0bc6

- Location: `paragraph:419`

Source:

```text
3）因为实例代码工程的UI已经设计好了，所以这里就不再新建，而是选择打开。
```

Translation:

```text
3) Because the UI of the example code project has been designed, we will not create a new one here, but choose to open it.
```

## p-0422-bbe3c89ee2b8

- Location: `paragraph:422`

Source:

```text
4）浏览并选择工程中的 .ui文件，然后打开。
```

Translation:

```text
4) Browse and select the .ui file in the project, and then open it.
```

## p-0425-63ca7803166e

- Location: `paragraph:425`

Source:

```text
5）打开后我们就可以查看实验实例代码工程的UI界面效果，以及窗口和各种组件的属性、动作、槽等一些设置，可以调整参数或者在界面上增删组件。
```

Translation:

```text
5) After opening, we can view the UI interface effect of the experimental example code project, as well as some settings such as the properties, actions, and slots of the window and various components. We can adjust parameters or add or delete components on the interface.
```

## p-0428-696a39bfde01

- Location: `paragraph:428`

Source:

```text
6）修改UI之后，直接选择菜单栏文件->保存选项，即可覆盖原.ui文件。
```

Translation:

```text
6) After modifying the UI, directly select File->Save option in the menu bar to overwrite the original .ui file.
```

## p-0431-8f42389d069f

- Location: `paragraph:431`

Source:

```text
7）最后在Pycharm软件中将.ui换成.py即可让修改的界面生效。
```

Translation:

```text
7) Finally, replace .ui with .py in Pycharm software to make the modified interface take effect.
```

## p-0434-ee7cf7911b72

- Location: `paragraph:434`

Source:

```text
第三章 PyQt5基础应用开发
```

Translation:

```text
Chapter 3 PyQt5 basic application development
```

## p-0435-6e3ebcd89b24

- Location: `paragraph:435`

Source:

```text
3.1 窗口属性设置
```

Translation:

```text
3.1 Window property settings
```

## p-0436-1eb7ce15551f

- Location: `paragraph:436`

Source:

```text
3.1.1 实验目的
```

Translation:

```text
3.1.1 Experimental purpose
```

## p-0437-bbb5820aefab

- Location: `paragraph:437`

Source:

```text
1）学习PyQt5窗口属性的设置方法。
```

Translation:

```text
1) Learn how to set the PyQt5 window properties.
```

## p-0438-d1fe1e583169

- Location: `paragraph:438`

Source:

```text
2）通过PyQt5属性设置重新设计UI。
```

Translation:

```text
2) Redesign the UI through PyQt5 property settings.
```

## p-0439-aad02652f3c5

- Location: `paragraph:439`

Source:

```text
3.1.2 基础知识
```

Translation:

```text
3.1.2 Basic knowledge
```

## p-0440-f649a2fa09a1

- Location: `paragraph:440`

Source:

```text
窗口的对象名称，相当于窗口的标识，是唯一的，在编写代码时，对窗口的任何设置和使用都是通过该名称进行操作的。窗口可以通过Qt Designer界面中的属性编辑器进行调整，比如可以设置窗口的对象名称、标题、大小、图标等。
```

Translation:

```text
The object name of the window, which is equivalent to the identifier of the window, is unique. When writing code, any setting and use of the window are operated through this name. The window can be adjusted through the property editor in the Qt Designer interface. For example, the object name, title, size, icon, etc. of the window can be set.
```

## p-0441-86fb926d624e

- Location: `paragraph:441`

Source:

```text
实验步骤
```

Translation:

```text
Procedure
```

## p-0442-af423b94b3a2

- Location: `paragraph:442`

Source:

```text
注意：本实验是在第二章新建的firstPyQt工程基础上进行操作的。
```

Translation:

```text
Note: This experiment is based on the firstPyQt project newly created in Chapter 2.
```

## p-0443-e9cd46ac08c6

- Location: `paragraph:443`

Source:

```text
3.1.3.1 设置窗口名称
```

Translation:

```text
3.1.3.1 Set window name
```

## p-0444-db706cbf5c12

- Location: `paragraph:444`

Source:

```text
选择窗口对象
```

Translation:

```text
Select window object
```

## p-0445-6bb9cf9bf85d

- Location: `paragraph:445`

Source:

```text
在Qt Designer设计器的对象查看器中选择主窗体MainWindow。
```

Translation:

```text
Select the main form MainWindow in the Object Viewer of Qt Designer.
```

## p-0448-da6310d3456c

- Location: `paragraph:448`

Source:

```text
修改窗口名称
```

Translation:

```text
Modify window name
```

## p-0449-02b45ece3c4c

- Location: `paragraph:449`

Source:

```text
在属性编辑器中将QObject->objectName属性修改为firstMain，并用组合快捷键Ctrl + S保存修改。
```

Translation:

```text
Modify the QObject->objectName property to firstMain in the property editor, and use the shortcut key combination Ctrl + S to save the changes.
```

## p-0452-3da1d136b78c

- Location: `paragraph:452`

Source:

```text
将.ui转换为.py
```

Translation:

```text
Convert .ui to .py
```

## p-0453-c46c28a328d1

- Location: `paragraph:453`

Source:

```text
因为UI修改过，回到Pycharm软件使用pyUIC工具将新的firstPyQt.ui转换成firstPyQt.py，打开firstPyQt.py文件可以看到窗口名称已自动修改。
```

Translation:

```text
Because the UI has been modified, go back to the Pycharm software and use the pyUIC tool to convert the new firstPyQt.ui into firstPyQt.py. When you open the firstPyQt.py file, you can see that the window name has been automatically modified.
```

## p-0456-705b46080bd7

- Location: `paragraph:456`

Source:

```text
修改main.py
```

Translation:

```text
Modify main.py
```

## p-0457-a73fa6022259

- Location: `paragraph:457`

Source:

```text
打开main.py文件，将下图修改对应行的代码。
```

Translation:

```text
Open the main.py file and modify the corresponding lines of code in the figure below.
```

## p-0461-5f2b0ca3b625

- Location: `paragraph:461`

Source:

```text
运行工程
```

Translation:

```text
Run project
```

## p-0462-1b4ce03e516a

- Location: `paragraph:462`

Source:

```text
直接运行代码如下图所示，因为只是修改窗口对象名称，所以在界面上没有变化，此时窗口大小为800 x 600像素。
```

Translation:

```text
Run the code directly as shown in the figure below. Because you only modify the window object name, there is no change in the interface. At this time, the window size is 800 x 600 pixels.
```

## p-0465-4b1a2cb1665b

- Location: `paragraph:465`

Source:

```text
3.1.3.2 设置窗口标题栏
```

Translation:

```text
3.1.3.2 Set window title bar
```

## p-0466-02d8d7493143

- Location: `paragraph:466`

Source:

```text
在属性编辑器中找到windowTitle选项，将其修改为myFirstPyQtAPP，然后通过组合快捷键Ctrl + S保存修改。
```

Translation:

```text
Find the windowTitle option in the property editor, modify it to myFirstPyQtAPP, and then save the modification by combining the shortcut key Ctrl + S.
```

## p-0469-c2564811d513

- Location: `paragraph:469`

Source:

```text
回到Pycharm软件使用pyUIC工具重新生成.py文件。
```

Translation:

```text
Return to the Pycharm software and use the pyUIC tool to regenerate the .py file.
```

## p-0470-5a2426747933

- Location: `paragraph:470`

Source:

```text
重新运行程序后界面如下，可以看到窗口标题已显示为myFirstPyQtAPP。
```

Translation:

```text
After re-running the program, the interface is as follows. You can see that the window title is displayed as myFirstPyQtAPP.
```

## p-0473-3e3109c2bc24

- Location: `paragraph:473`

Source:

```text
3.1.3.3 设置窗口大小
```

Translation:

```text
3.1.3.3 Set window size
```

## p-0474-0fc45df31258

- Location: `paragraph:474`

Source:

```text
在属性编辑器中找到geometry选项，将宽度和高度分别修改为400*300，然后通过组合快捷键Ctrl + S保存修改。
```

Translation:

```text
Find the geometry option in the attribute editor, modify the width and height to 400*300 respectively, and then save the modifications by combining the shortcut keys Ctrl + S.
```

## p-0477-c2564811d513

- Location: `paragraph:477`

Source:

```text
回到Pycharm软件使用pyUIC工具重新生成.py文件。
```

Translation:

```text
Return to the Pycharm software and use the pyUIC tool to regenerate the .py file.
```

## p-0478-6aaf1548919f

- Location: `paragraph:478`

Source:

```text
重新运行程序后界面如下，可以看到窗口明显缩小，大小为400 x 300像素。
```

Translation:

```text
After re-running the program, the interface is as follows. You can see that the window is significantly reduced, with a size of 400 x 300 pixels.
```

## p-0481-d3168e23cad4

- Location: `paragraph:481`

Source:

```text
3.1.3.4 设置窗口图标
```

Translation:

```text
3.1.3.4 Set window icon
```

## p-0482-ff65a2e49b23

- Location: `paragraph:482`

Source:

```text
准备一张图标图片，将其放到工程路径下。
```

Translation:

```text
Prepare an icon picture and place it in the project path.
```

## p-0484-c7c2c9ad6f89

- Location: `paragraph:484`

Source:

```text
在属性编辑器中找到windowIcon选项，并点击右侧。
```

Translation:

```text
Find the windowIcon option in the property editor and click on the right side.
```

## p-0486-8e4151da40f4

- Location: `paragraph:486`

Source:

```text
点击windowIcon选项右侧的按钮，在弹出的列表中选择“选择文件…”选项。
```

Translation:

```text
Click the button to the right of the windowIcon option and select the "Select File..." option in the pop-up list.
```

## p-0488-cea01ffc501d

- Location: `paragraph:488`

Source:

```text
在弹出的选择一个像素映射的对话框中，浏览选择之前拷贝的qt.png文件，然后打开。
```

Translation:

```text
In the dialog box that pops up to select a pixel map, browse to select the qt.png file you copied before, and then open it.
```

## p-0490-555a1f8862cf

- Location: `paragraph:490`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存修改。
```

Translation:

```text
Save changes in Qt Designer using the shortcut key combination Ctrl + S.
```

## p-0491-c2564811d513

- Location: `paragraph:491`

Source:

```text
回到Pycharm软件使用pyUIC工具重新生成.py文件。
```

Translation:

```text
Return to the Pycharm software and use the pyUIC tool to regenerate the .py file.
```

## p-0493-c514bfce14de

- Location: `paragraph:493`

Source:

```text
实验结果
```

Translation:

```text
Experimental results
```

## p-0494-a08d10d0541a

- Location: `paragraph:494`

Source:

```text
重新运行程序后界面如下，可以看到窗口左上角的图标已经发生了变化。
```

Translation:

```text
After re-running the program, the interface is as follows. You can see that the icon in the upper left corner of the window has changed.
```

## p-0496-90b5931d32ea

- Location: `paragraph:496`

Source:

```text
通过窗口属性设置的方法。修改了窗口名称、标题栏、窗口大小以及窗口的图标。
```

Translation:

```text
Method set through window properties. Modified the window name, title bar, window size and window icon.
```

## p-0500-152e474fc9c4

- Location: `paragraph:500`

Source:

```text
3.2 信号与槽
```

Translation:

```text
3.2 Signals and slots
```

## p-0501-0680158c42c6

- Location: `paragraph:501`

Source:

```text
3.2.1 实验目的
```

Translation:

```text
3.2.1 Experimental purpose
```

## p-0502-4331bdac6505

- Location: `paragraph:502`

Source:

```text
1）学习PyQt5信号与槽窗的使用方法。
```

Translation:

```text
1) Learn how to use PyQt5 signals and slot windows.
```

## p-0503-a9e36a53af81

- Location: `paragraph:503`

Source:

```text
2）通过PyQt5信号与槽窗实现UI界面上的按钮触发动作。
```

Translation:

```text
2) Implement button triggering actions on the UI interface through PyQt5 signals and slot windows.
```

## p-0504-dbe1fe830355

- Location: `paragraph:504`

Source:

```text
3）掌握PyQt5中自定义槽函数的方法。
```

Translation:

```text
3) Master the method of customizing slot functions in PyQt5.
```

## p-0505-3e8467f23d8e

- Location: `paragraph:505`

Source:

```text
3.2.2 基础知识
```

Translation:

```text
3.2.2 Basic knowledge
```

## p-0506-5866a5464537

- Location: `paragraph:506`

Source:

```text
信号（signal）与槽（slot）是Qt的核心机制，也是进行PyQt5编程时对象之间通信的基础。在PyQt5中，每一个QObject对象（包括各种窗口和控件）都支持信号与槽机制，通过信号与槽的关联，就可以实现对象之间的通信。当信号发射时，连接的槽函数（方法）将会自动执行。在PyQt5中，信号与槽是通过对象的signal.connect()方法进行连接的。
```

Translation:

```text
Signals and slots are the core mechanisms of Qt and the basis for communication between objects when programming PyQt5. In PyQt5, every QObject object (including various windows and controls) supports the signal and slot mechanism. Through the association of signals and slots, communication between objects can be achieved. When the signal is emitted, the connected slot function (method) will be automatically executed. In PyQt5, signals and slots are connected through the object's signal.connect() method.
```

## p-0508-29eb1b8a79d0

- Location: `paragraph:508`

Source:

```text
3.2.3 实验步骤
```

Translation:

```text
3.2.3 Experimental steps
```

## p-0509-af423b94b3a2

- Location: `paragraph:509`

Source:

```text
注意：本实验是在第二章新建的firstPyQt工程基础上进行操作的。
```

Translation:

```text
Note: This experiment is based on the firstPyQt project newly created in Chapter 2.
```

## p-0510-eb6e19302c8e

- Location: `paragraph:510`

Source:

```text
3.2.3.1 编辑信号与槽
```

Translation:

```text
3.2.3.1 Edit signals and slots
```

## p-0511-bbf33a5bd8a6

- Location: `paragraph:511`

Source:

```text
在Qt Designer设计器中查看器中选择pushButton。
```

Translation:

```text
Select the pushButton in the Qt Designer viewer.
```

## p-0513-a7e186f092e4

- Location: `paragraph:513`

Source:

```text
在属性编辑器中找到text选项，将其内容改为“关闭”。
```

Translation:

```text
Find the text option in the property editor and change its content to "off".
```

## p-0516-b151b041bba5

- Location: `paragraph:516`

Source:

```text
在菜单栏中选择“Edit -> 编辑信号/槽”选项。
```

Translation:

```text
Select the "Edit -> Edit Signal/Slot" option in the menu bar.
```

## p-0519-e6da8c88dc76

- Location: `paragraph:519`

Source:

```text
此时工具箱等位置会变成灰色，用鼠标选中UI窗口中的按钮，然后长按鼠标左键拖动到空白位置。
```

Translation:

```text
At this time, the toolbox and other locations will turn gray. Use the mouse to select the button in the UI window, then press and hold the left mouse button and drag it to a blank location.
```

## p-0522-dcd7d90c94ed

- Location: `paragraph:522`

Source:

```text
在弹出的配置连接对话框中，勾选左下角的单选框，在左侧列表中选择clicked()，在右侧列表中选择close()，然后点击OK。
```

Translation:

```text
In the pop-up configuration connection dialog box, check the radio button in the lower left corner, select clicked() in the left list, select close() in the right list, and then click OK.
```

## p-0525-18d25aa7de25

- Location: `paragraph:525`

Source:

```text
完成信号与槽的关联之后，预览窗口如下图所示。此时可以通过组合快捷键Ctrl + S保存修改。
```

Translation:

```text
After completing the association of signals and slots, the preview window is as shown below. At this point, you can save the modifications by pressing the shortcut key combination Ctrl + S.
```

## p-0527-5a461fd23ae0

- Location: `paragraph:527`

Source:

```text
回到Pycharm使用pyUIC工具重新生成.py文件，且该文件中自动添加了相关代码。
```

Translation:

```text
Go back to Pycharm and use the pyUIC tool to regenerate the .py file, and the relevant code is automatically added to the file.
```

## p-0529-e61fe69498c9

- Location: `paragraph:529`

Source:

```text
重新运行程序，点击关闭按钮后，程序界面窗口会自动关闭。
```

Translation:

```text
Rerun the program and click the close button, and the program interface window will automatically close.
```

## p-0532-db5bb3c1e7ee

- Location: `paragraph:532`

Source:

```text
3.2.3.2 自定义槽
```

Translation:

```text
3.2.3.2 Custom slots
```

## p-0533-62b9a4af042f

- Location: `paragraph:533`

Source:

```text
自定义槽本质上就是自定义一个函数，函数来实现相应的功能，其操作方法如下：
```

Translation:

```text
A custom slot is essentially a custom function that implements the corresponding function. The operation method is as follows:
```

## p-0534-7e843ce515be

- Location: `paragraph:534`

Source:

```text
取消原来信号与槽的关联
```

Translation:

```text
Cancel the association between the original signal and slot
```

## p-0535-3f357034895e

- Location: `paragraph:535`

Source:

```text
在Qt Designer中选择信号/槽编辑器，选择之前关联项，然后点击上面的“-”按钮进行删除。通过组合快捷键Ctrl + S保存修改，回到Pycharm使用pyUIC工具重新生成.py文件。
```

Translation:

```text
Select the signal/slot editor in Qt Designer, select the previous associated item, and then click the "-" button above to delete it. Save the changes by combining the shortcut keys Ctrl + S, return to Pycharm and use the pyUIC tool to regenerate the .py file.
```

## p-0538-f5134d338dd0

- Location: `paragraph:538`

Source:

```text
重写UI类
```

Translation:

```text
Rewrite UI class
```

## p-0539-3f4736da3fb2

- Location: `paragraph:539`

Source:

```text
在main.py中重写继承关于ui的类，然后在函数入口通过重写的mainWindow()启动界面窗口，代码如下：
```

Translation:

```text
Rewrite the inherited ui class in main.py, and then start the interface window through the rewritten mainWindow() at the function entry. The code is as follows:
```

## p-0550-a7e1a3d349c9

- Location: `paragraph:550`

Source:

```text
自定义槽函数
```

Translation:

```text
Custom slot function
```

## p-0551-b63af884da95

- Location: `paragraph:551`

Source:

```text
在类mainWindow中添加槽函数ClickEvent()，修改后代码如下：
```

Translation:

```text
Add the slot function ClickEvent() in class mainWindow. The modified code is as follows:
```

## p-0555-9d38b7ecdd4f

- Location: `paragraph:555`

Source:

```text
关联信号与槽
```

Translation:

```text
Associated signals and slots
```

## p-0556-7f55c7088836

- Location: `paragraph:556`

Source:

```text
在mainWindow类中完成clicked信号与槽函数ClickEvent()的关联，修改后代码如下：
```

Translation:

```text
Complete the association between the clicked signal and the slot function ClickEvent() in the mainWindow class. The modified code is as follows:
```

## p-0561-82b7de943d24

- Location: `paragraph:561`

Source:

```text
#pushButton的clicked信号与槽ClickEvent关联
```

Translation:

```text
#pushButton's clicked signal is associated with the slot ClickEvent
```

## p-0566-a3255c7f6212

- Location: `paragraph:566`

Source:

```text
以上几步代码编辑完成后如下图所示：
```

Translation:

```text
After the above steps of code editing are completed, it will look like the following picture:
```

## p-0570-56587f647cb6

- Location: `paragraph:570`

Source:

```text
3.2.4 实验结果
```

Translation:

```text
3.2.4 Experimental results
```

## p-0571-34cd6fef1bf3

- Location: `paragraph:571`

Source:

```text
直接运行代码，在弹出的窗口中点击关闭按钮后，窗口左下角会显示出“Hello World！”的字符串。
```

Translation:

```text
Run the code directly and click the close button in the pop-up window. The string "Hello World!" will be displayed in the lower left corner of the window.
```

## p-0576-4a1a0c8f51e4

- Location: `paragraph:576`

Source:

```text
3.3 多窗口设计
```

Translation:

```text
3.3 Multi-window design
```

## p-0577-37ba79609965

- Location: `paragraph:577`

Source:

```text
3.3.1 实验目的
```

Translation:

```text
3.3.1 Experimental purpose
```

## p-0578-50f578cb346a

- Location: `paragraph:578`

Source:

```text
学习PyQt5多窗口的使用方法。
```

Translation:

```text
Learn how to use PyQt5 multi-window.
```

## p-0579-f8322cb742e5

- Location: `paragraph:579`

Source:

```text
3.3.2 基础知识
```

Translation:

```text
3.3.2 Basic knowledge
```

## p-0580-7c73fa4071ef

- Location: `paragraph:580`

Source:

```text
一个完整的综合应用一般都是由多个窗口组成的，通过弹窗、或者窗口之间跳转等方式实现交互功能。
```

Translation:

```text
A complete comprehensive application is generally composed of multiple windows, and interactive functions are implemented through pop-up windows or jumping between windows.
```

## p-0581-55c4fd94a0d3

- Location: `paragraph:581`

Source:

```text
3.3.3 实验步骤
```

Translation:

```text
3.3.3 Experimental steps
```

## p-0582-351c19262c80

- Location: `paragraph:582`

Source:

```text
通过Pycharm软件新建一个名为multiWindow的工程。
```

Translation:

```text
Create a new project named multiWindow through Pycharm software.
```

## p-0585-32c858d94cb8

- Location: `paragraph:585`

Source:

```text
通过External Tools打开Qt Designer设计器。
```

Translation:

```text
Open Qt Designer via External Tools.
```

## p-0587-44ad23497809

- Location: `paragraph:587`

Source:

```text
在默认弹窗中选择创建一个MainWindow，然后在菜单栏选择文件->新建(F)…选项，在弹窗中选择创建第二个MainWindow。
```

Translation:

```text
Select to create a MainWindow in the default pop-up window, then select the File->New (F)... option in the menu bar, and select to create a second MainWindow in the pop-up window.
```

## p-0590-a432e3aa3610

- Location: `paragraph:590`

Source:

```text
修改窗口属性
```

Translation:

```text
Modify window properties
```

## p-0591-a78a55bd5d8f

- Location: `paragraph:591`

Source:

```text
分别修改两个窗口的“objectName”为：mutliWindow1和mutliWindow2
```

Translation:

```text
Modify the "objectName" of the two windows respectively to: mutliWindow1 and mutliWindow2
```

## p-0592-9b4727388366

- Location: `paragraph:592`

Source:

```text
分别修改两个窗口的“windowTitle”为：window1和window2
```

Translation:

```text
Modify the "windowTitle" of the two windows respectively to: window1 and window2
```

## p-0593-58317d8192ee

- Location: `paragraph:593`

Source:

```text
分别修改两个窗口的宽度和高度为：300 x 200
```

Translation:

```text
Modify the width and height of the two windows respectively: 300 x 200
```

## p-0594-82afd18639a0

- Location: `paragraph:594`

Source:

```text
在mutliWindow1窗口中，添加一个pushButton按钮
```

Translation:

```text
In the mutliWindow1 window, add a pushButton button
```

## p-0596-0b53a4e27fd0

- Location: `paragraph:596`

Source:

```text
两个窗口属性设置完成后界面如下，然后依次选中两个窗口通过Ctrl + S进行保存，分别命名为mutliwindow1.ui和mutliwindow2.ui。
```

Translation:

```text
After the properties of the two windows are set, the interface is as follows. Then select the two windows in order and save them through Ctrl + S, naming them mutliwindow1.ui and mutliwindow2.ui respectively.
```

## p-0598-6105d2504403

- Location: `paragraph:598`

Source:

```text
回到Pycharm软件使用pyUIC工具依次将两个.ui文件转换成.py文件。
```

Translation:

```text
Go back to the Pycharm software and use the pyUIC tool to convert the two .ui files into .py files in sequence.
```

## p-0600-5d0e68e5bf1b

- Location: `paragraph:600`

Source:

```text
重写mainWindows
```

Translation:

```text
Rewrite mainWindows
```

## p-0601-d096cc629476

- Location: `paragraph:601`

Source:

```text
修改main.py代码，重写mainWindow类，通过它打开multiWindow1窗口，并且主函数启动程序时会调用它。
```

Translation:

```text
Modify the main.py code, rewrite the mainWindow class, open the multiWindow1 window through it, and the main function will call it when starting the program.
```

## p-0605-2dc70b7c0f72

- Location: `paragraph:605`

Source:

```text
重写window2
```

Translation:

```text
Rewrite window2
```

## p-0606-7cb20eea2139

- Location: `paragraph:606`

Source:

```text
重写Window2类，通过它打开multiWindow2窗口。
```

Translation:

```text
Rewrite the Window2 class and open the multiWindow2 window through it.
```

## p-0611-a87df8097801

- Location: `paragraph:611`

Source:

```text
关联槽函数
```

Translation:

```text
Associated slot function
```

## p-0612-7caeed72b0fc

- Location: `paragraph:612`

Source:

```text
在mainWindow类中添加一个showWindow2的槽函数，并且将pushButton的clicked信号与之关联。
```

Translation:

```text
Add a slot function of showWindow2 in the mainWindow class and associate the clicked signal of pushButton with it.
```

## p-0614-030675c54e5b

- Location: `paragraph:614`

Source:

```text
def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        #pushButton的clicked信号与槽showWindow2关联
        self.pushButton.clicked.connect(self.showWindow2)
    def showWindow2(self):
        self.window2 = window2()
        self.window2.show()
```

Translation:

```text
def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        #pushButton's clicked signal is associated with slot showWindow2
        self.pushButton.clicked.connect(self.showWindow2)
    def showWindow2(self):
        self.window2 = window2()
        self.window2.show()
```

## p-0615-f911e6cdbc4b

- Location: `paragraph:615`

Source:

```text
10）代码编写完成后界面如下：
```

Translation:

```text
10) After the code is written, the interface is as follows:
```

## p-0618-b1660997998c

- Location: `paragraph:618`

Source:

```text
3.3.4 实验结果
```

Translation:

```text
3.3.4 Experimental results
```

## p-0619-b08c38c654d2

- Location: `paragraph:619`

Source:

```text
运行程序后会打开窗口1，然后点击PushButton按钮会弹出窗口2。
```

Translation:

```text
After running the program, window 1 will open, and then clicking the PushButton button will pop up window 2.
```

## p-0622-0f9fda2f5f10

- Location: `paragraph:622`

Source:

```text
3.4 控件设计-文本类
```

Translation:

```text
3.4 Control design-text class
```

## p-0623-3d0016e48db8

- Location: `paragraph:623`

Source:

```text
3.4.1 实验目的
```

Translation:

```text
3.4.1 Experimental purpose
```

## p-0624-85a38b9dce31

- Location: `paragraph:624`

Source:

```text
1）了解PyQt文本类控件Label、TextEdit、SpinBox的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt text controls Label, TextEdit, and SpinBox.
```

## p-0625-467709c880b5

- Location: `paragraph:625`

Source:

```text
2）编写代码，掌握Label、TextEdit、SpinBox的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting methods of important properties of Label, TextEdit and SpinBox.
```

## p-0626-69c8e31f83cb

- Location: `paragraph:626`

Source:

```text
3.4.2 基础知识
```

Translation:

```text
3.4.2 Basic knowledge
```

## p-0627-569b34c92261

- Location: `paragraph:627`

Source:

```text
Label控件，又称为标签控件，主要用于显示用户不能编辑的文本，标识窗体上的对象，它对应PyQt5中的QLabel类。
```

Translation:

```text
Label control, also known as label control, is mainly used to display text that cannot be edited by users and identify objects on the form. It corresponds to the QLabel class in PyQt5.
```

## p-0628-406969dda04b

- Location: `paragraph:628`

Source:

```text
TextEdit控件是多行文本框控件，主要用来显示多行的文本内容，当文本的内容超出控件的显示范围时，该控件将显示垂直滚动条。
```

Translation:

```text
The TextEdit control is a multi-line text box control, mainly used to display multi-line text content. When the text content exceeds the display range of the control, the control will display a vertical scroll bar.
```

## p-0629-44869209ea22

- Location: `paragraph:629`

Source:

```text
SpinBox控件是一个整数数字控件，允许用户选择一个整数值，通过单击向上/向下按钮或按键盘上的上/下箭头来增加/减少当前显示的值，当然用户也可以输入值。
```

Translation:

```text
The SpinBox control is an integer number control that allows the user to select an integer value and increase/decrease the currently displayed value by clicking the up/down buttons or pressing the up/down arrows on the keyboard. Of course, the user can also enter a value.
```

## p-0631-6c3898b4f868

- Location: `paragraph:631`

Source:

```text
3.4.3 实验步骤
```

Translation:

```text
3.4.3 Experimental steps
```

## p-0633-5c976f82778b

- Location: `paragraph:633`

Source:

```text
参照第2章在Pycharm软件中新建一个textPyQt工程，并将qt.png图片文件拷贝到工程的根目录（图片可在本章节的实验源代码中找到）。
```

Translation:

```text
Refer to Chapter 2 to create a new textPyQt project in Pycharm software, and copy the qt.png image file to the root directory of the project (the image can be found in the experimental source code of this chapter).
```

## p-0634-c095a94dc1d3

- Location: `paragraph:634`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加两个Label组件，两个Label的objectName分别为label1和label2。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, and add two Label components through the toolbox. The objectNames of the two Labels are label1 and label2 respectively.
```

## p-0636-17c804f37b4d

- Location: `paragraph:636`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存为textPyQt.ui，然后回到Pycharm软件中使用pyUIC工具生成textPyQt.py文件。
```

Translation:

```text
Save it as textPyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the textPyQt.py file.
```

## p-0637-d6807933cabc

- Location: `paragraph:637`

Source:

```text
修改main.py代码，直接通过setText方法让label1显示文本，让label2显示图片并指定坐标和大小，代码如下：
```

Translation:

```text
Modify the main.py code and let label1 display text directly through the setText method, and let label2 display images and specify coordinates and sizes. The code is as follows:
```

## p-0647-de0145ad91c7

- Location: `paragraph:647`

Source:

```text
self.label1.setText("你好！！！")
```

Translation:

```text
self.label1.setText("Hello!!!")
```

## p-0655-215de6036d4f

- Location: `paragraph:655`

Source:

```text
代码修改后如下图所示：
```

Translation:

```text
The code is modified as shown below:
```

## p-0657-7bc8e992b5c2

- Location: `paragraph:657`

Source:

```text
直接运行程序，弹出窗口界面如下：
```

Translation:

```text
Run the program directly, and the pop-up window interface is as follows:
```

## p-0661-95f5128267ec

- Location: `paragraph:661`

Source:

```text
说明：本小节直接在上一小节的实验代码的基础上进行操作。
```

Translation:

```text
Note: This section operates directly on the basis of the experimental code in the previous section.
```

## p-0662-341436133fc1

- Location: `paragraph:662`

Source:

```text
在Qt Designer中为窗口添加一个TextEdit控件。
```

Translation:

```text
Add a TextEdit control to the window in Qt Designer.
```

## p-0664-55d78efec578

- Location: `paragraph:664`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存textPyQt.ui，然后回到Pycharm软件中使用pyUIC工具重新生成textPyQt.py文件。
```

Translation:

```text
Save textPyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then go back to Pycharm software and use the pyUIC tool to regenerate the textPyQt.py file.
```

## p-0665-8a36c5f747c7

- Location: `paragraph:665`

Source:

```text
修改main.py代码，通过setPlainText方法为textEdit添加长字符串，修改后代码如下：
```

Translation:

```text
Modify the main.py code and add a long string to textEdit through the setPlainText method. The modified code is as follows:
```

## p-0667-1bc894b8a7a6

- Location: `paragraph:667`

Source:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
from PyQt5.QtGui import QPixmap
from PyQt5 import QtCore
import textPyQt as textUi
class mainWindow(QMainWindow, textUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.label1.setText("你好！！！")
        self.label2.setGeometry(QtCore.QRect(160, 100, 60, 60))
        self.label2.setPixmap(QPixmap('qt.png'))
        self.textEdit.setPlainText("此去西洋，深知中国自强之计，舍此无所他求；背负国家之未来，取尽洋人之科学，赴七万里长途，别祖国父母之邦，奋然无悔。 ")
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

Translation:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
from PyQt5.QtGui import QPixmap
from PyQt5 import QtCore
import textPyQt as textUi
class mainWindow(QMainWindow, textUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.label1.setText("Hello!!!")
        self.label2.setGeometry(QtCore.QRect(160, 100, 60, 60))
        self.label2.setPixmap(QPixmap('qt.png'))
        self.textEdit.setPlainText("Going to the West, I fully understand China's plan for self-improvement, and I want nothing else. I carry the future of the country on my back, absorb all the science of foreigners, and travel 70,000 miles away from my motherland and my parents. I have no regrets. ")
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

## p-0668-03f0a654bba6

- Location: `paragraph:668`

Source:

```text
运行程序窗口界面如下，拖动滑动条可以浏览全部文字。
```

Translation:

```text
The running program window interface is as follows. Drag the slider bar to browse all text.
```

## p-0672-95f5128267ec

- Location: `paragraph:672`

Source:

```text
说明：本小节直接在上一小节的实验代码的基础上进行操作。
```

Translation:

```text
Note: This section operates directly on the basis of the experimental code in the previous section.
```

## p-0673-83351ad104da

- Location: `paragraph:673`

Source:

```text
在Qt Designer中为窗口添加一个SpinBox控件。
```

Translation:

```text
Add a SpinBox control to the window in Qt Designer.
```

## p-0675-55d78efec578

- Location: `paragraph:675`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存textPyQt.ui，然后回到Pycharm软件中使用pyUIC工具重新生成textPyQt.py文件。
```

Translation:

```text
Save textPyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then go back to Pycharm software and use the pyUIC tool to regenerate the textPyQt.py file.
```

## p-0676-3f0a429fbaa0

- Location: `paragraph:676`

Source:

```text
修改main.py代码，直接通过setRange和setSingleStep方法为SpinBox控件设置范围和步长，修改后代码如下：
```

Translation:

```text
Modify the main.py code and directly set the range and step size for the SpinBox control through the setRange and setSingleStep methods. The modified code is as follows:
```

## p-0678-270dbfc5454d

- Location: `paragraph:678`

Source:

```text
def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.label1.setText("你好！！！")
        self.label2.setGeometry(QtCore.QRect(160, 100, 60, 60))
        self.label2.setPixmap(QPixmap('qt.png'))
        self.textEdit.setPlainText("此去西洋，深知中国自强之计，舍此无所他求；背负国家之未来，取尽洋人之科学，赴七万里长途，别祖国父母之邦，奋然无悔。 ")
        self.spinBox.setRange(0, 66)  # 设置范围
        self.spinBox.setSingleStep(3)  # 设置步长值
```

Translation:

```text
def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.label1.setText("Hello!!!")
        self.label2.setGeometry(QtCore.QRect(160, 100, 60, 60))
        self.label2.setPixmap(QPixmap('qt.png'))
        self.textEdit.setPlainText("Going to the West, I fully understand China's plan for self-improvement, and I want nothing else. I carry the future of the country on my back, absorb all the science of foreigners, and travel 70,000 miles away from my motherland and my parents. I have no regrets. ")
        self.spinBox.setRange(0, 66) # Set range
        self.spinBox.setSingleStep(3) # Set the step value
```

## p-0680-5a57b250e524

- Location: `paragraph:680`

Source:

```text
在mainWindow类中定义一个getvalue的槽函数
```

Translation:

```text
Define a getvalue slot function in the mainWindow class
```

## p-0684-2b1408cab147

- Location: `paragraph:684`

Source:

```text
将spinBox控件的valueChanged信号与自定义的getvalue()槽函数关联。
```

Translation:

```text
Associate the valueChanged signal of the spinBox control with the custom getvalue() slot function.
```

## p-0689-de0145ad91c7

- Location: `paragraph:689`

Source:

```text
self.label1.setText("你好！！！")
```

Translation:

```text
self.label1.setText("Hello!!!")
```

## p-0692-7ee89b6a85b9

- Location: `paragraph:692`

Source:

```text
self.textEdit.setPlainText("此去西洋，深知中国自强之计，舍此无所他求；背负国家之未来，取尽洋人之科学，赴七万里长途，别祖国父母之邦，奋然无悔。 ")
```

Translation:

```text
self.textEdit.setPlainText("Going to the West, I fully understand China's plan for self-improvement, and I want nothing else. I carry the future of the country on my back, absorb all the science of foreigners, and travel 70,000 miles away from my motherland and my parents. I have no regrets. ")
```

## p-0693-c12e28ba8a23

- Location: `paragraph:693`

Source:

```text
self.spinBox.setRange(0,66)      #设置范围
```

Translation:

```text
self.spinBox.setRange(0,66) #Set the range
```

## p-0694-4b95cbecf17a

- Location: `paragraph:694`

Source:

```text
self.spinBox.setSingleStep(3)   #设置步长值
        self.spinBox.valueChanged.connect(self.getvalue)
```

Translation:

```text
self.spinBox.setSingleStep(3) #Set the step value
        self.spinBox.valueChanged.connect(self.getvalue)
```

## p-0695-ed74f324c3a4

- Location: `paragraph:695`

Source:

```text
代码修改完成后如下图所示：
```

Translation:

```text
After the code modification is completed, it is as shown below:
```

## p-0700-8cf8cfb49f0b

- Location: `paragraph:700`

Source:

```text
3.4.4 实验结果
```

Translation:

```text
3.4.4 Experimental results
```

## p-0701-057671f5ae71

- Location: `paragraph:701`

Source:

```text
程序窗口界面如下，点击SpinBox控件右侧的箭头，可以控制空间内的数据变化，从0开始，每次自增3，并且Lable1控件会通过信号与槽同步显示数字变化。
```

Translation:

```text
The program window interface is as follows. Click the arrow on the right side of the SpinBox control to control the data changes in the space. It starts from 0 and increments by 3 each time. The Label1 control will display digital changes synchronously through signals and slots.
```

## p-0704-d97c88eb2712

- Location: `paragraph:704`

Source:

```text
在窗体中创建了三种不同的文本类控件，并分别学习了这三种控件的使用方法。
```

Translation:

```text
Created three different text controls in the form, and learned how to use these three controls respectively.
```

## p-0709-6a631405197a

- Location: `paragraph:709`

Source:

```text
3.5 控件设计-按钮类
```

Translation:

```text
3.5 Control Design-Button Class
```

## p-0710-7ffedd4da232

- Location: `paragraph:710`

Source:

```text
3.5.1 实验目的
```

Translation:

```text
3.5.1 Experimental purpose
```

## p-0711-ce58f7d94803

- Location: `paragraph:711`

Source:

```text
1）了解PyQt按钮类控件PushButton、CheckBox的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt button controls PushButton and CheckBox.
```

## p-0712-fe6198f07937

- Location: `paragraph:712`

Source:

```text
2）编写代码，掌握按钮类控件PushButton、CheckBox的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting methods of important properties of button controls PushButton and CheckBox.
```

## p-0713-52e68d32c868

- Location: `paragraph:713`

Source:

```text
3.5.2 基础知识
```

Translation:

```text
3.5.2 Basic knowledge
```

## p-0714-c705e16fa8af

- Location: `paragraph:714`

Source:

```text
PushButton控件是按钮控件，允许用户通过单击来执行操作。即可以显示文本内容，也可以显示图像。当控件被单击时，它的状态是按下然后被释放。
```

Translation:

```text
PushButton controls are button controls that allow users to perform actions by clicking on them. That is, text content can be displayed, and images can also be displayed. When a control is clicked, its state is pressed and then released.
```

## p-0715-cb1c48fea41a

- Location: `paragraph:715`

Source:

```text
CheckBox控件是复选框控件，常用于为用户提供具有是/否或真/假值的选项，对应CheckBox类。
```

Translation:

```text
The CheckBox control is a checkbox control, often used to provide users with options with yes/no or true/false values, corresponding to the CheckBox class.
```

## p-0717-a023e2256e58

- Location: `paragraph:717`

Source:

```text
3.5.3 实验步骤
```

Translation:

```text
3.5.3 Experimental steps
```

## p-0719-b184bf51b74e

- Location: `paragraph:719`

Source:

```text
参照第2章在Pycharm软件中新建一个buttonPyQt工程，并将login.ico的图片拷贝到工程根目录下（图片可在本章节的实验源代码中找到）。
```

Translation:

```text
Refer to Chapter 2 to create a new buttonPyQt project in the Pycharm software, and copy the image of login.ico to the project root directory (the image can be found in the experimental source code of this chapter).
```

## p-0720-8e8693b65758

- Location: `paragraph:720`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个PushButton控件。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300 and add a PushButton control through the toolbox.
```

## p-0722-6851f2c22777

- Location: `paragraph:722`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存为buttonPyQt.ui，然后回到Pycharm软件中使用pyUIC工具生成buttonPyQt.py文件。
```

Translation:

```text
Save it as buttonPyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the buttonPyQt.py file.
```

## p-0723-c9347efe9dd9

- Location: `paragraph:723`

Source:

```text
修改main.py代码，直接通过setText方法修改按钮的文本显示，通过setIcon方法指定按钮的显示图片，修改后代码如下：
```

Translation:

```text
Modify the main.py code, directly modify the text display of the button through the setText method, and specify the display image of the button through the setIcon method. The modified code is as follows:
```

## p-0725-e146c56aff86

- Location: `paragraph:725`

Source:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
import buttonPyQt as butUi
from PyQt5.QtGui import QIcon, QPixmap
class mainWindow(QMainWindow, butUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.setText("按钮")
        self.pushButton.setIcon(QIcon(QPixmap("login.ico")))
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

Translation:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
import buttonPyQt as butUi
from PyQt5.QtGui import QIcon, QPixmap
class mainWindow(QMainWindow, butUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.setText("Button")
        self.pushButton.setIcon(QIcon(QPixmap("login.ico")))
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

## p-0727-62ff7a38f44b

- Location: `paragraph:727`

Source:

```text
运行程序窗口界面如下，PushButton控件上会显示特定的图片+文字。
```

Translation:

```text
The running program window interface is as follows. A specific picture + text will be displayed on the PushButton control.
```

## p-0731-c05ce7aa0b16

- Location: `paragraph:731`

Source:

```text
继续在buttonPyQt工程Qt Designer设计器中，添加一个CheckBox控件。
```

Translation:

```text
Continue in the buttonPyQt project Qt Designer and add a CheckBox control.
```

## p-0733-c5c895542915

- Location: `paragraph:733`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存buttonPyQt.ui，然后回到Pycharm软件中使用pyUIC工具重新生成buttonPyQt.py文件
```

Translation:

```text
Save buttonPyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then go back to Pycharm software and use the pyUIC tool to regenerate the buttonPyQt.py file.
```

## p-0734-4066230207fd

- Location: `paragraph:734`

Source:

```text
修改main.py代码，添加如下代码为CheckBox控件控件设置文本和图片。
```

Translation:

```text
Modify the main.py code and add the following code to set text and pictures for the CheckBox control.
```

## p-0735-898b92d5186e

- Location: `paragraph:735`

Source:

```text
self.checkBox.setText("学习此技能")
```

Translation:

```text
self.checkBox.setText("Learn this skill")
```

## p-0737-65324f995b24

- Location: `paragraph:737`

Source:

```text
在mainWindow类中定义一个槽函数setBtnText，代码如下：
```

Translation:

```text
Define a slot function setBtnText in the mainWindow class, the code is as follows:
```

## p-0739-50812d638795

- Location: `paragraph:739`

Source:

```text
if self.checkBox.isChecked():
        self.pushButton.setText("已选中")
    else:
        self.pushButton.setText("未选中")
```

Translation:

```text
if self.checkBox.isChecked():
        self.pushButton.setText("selected")
    else:
        self.pushButton.setText("unchecked")
```

## p-0740-ec9b927f1c2e

- Location: `paragraph:740`

Source:

```text
checkBox控件最常用的信号是stateChanged，用来在复选框的状态发生改变时发射。添加checkbox的stateChanged信号与槽函数setBtnText关联代码。
```

Translation:

```text
The most commonly used signal for the checkBox control is stateChanged, which is emitted when the state of the check box changes. Add the code associated with the checkbox's stateChanged signal and slot function setBtnText.
```

## p-0741-898b92d5186e

- Location: `paragraph:741`

Source:

```text
self.checkBox.setText("学习此技能")
```

Translation:

```text
self.checkBox.setText("Learn this skill")
```

## p-0744-ccc7bfc3d185

- Location: `paragraph:744`

Source:

```text
main.py完整代码如下。
```

Translation:

```text
The complete code of main.py is as follows.
```

## p-0746-34205932065f

- Location: `paragraph:746`

Source:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
import buttonPyQt as butUi
from PyQt5.QtGui import QIcon, QPixmap
class mainWindow(QMainWindow, butUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.setText("按钮")
        self.pushButton.setIcon(QIcon(QPixmap("login.ico")))
        self.checkBox.setText("学习此技能")
        self.checkBox.setIcon(QIcon(QPixmap("login.ico")))
        self.checkBox.stateChanged.connect(self.setBtnText)
    def setBtnText(self):
        if self.checkBox.isChecked():
            self.pushButton.setText("已选中")
        else:
            self.pushButton.setText("未选中")
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

Translation:

```text
from PyQt5.QtWidgets import QApplication, QMainWindow
import buttonPyQt as butUi
from PyQt5.QtGui import QIcon, QPixmap
class mainWindow(QMainWindow, butUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.setText("Button")
        self.pushButton.setIcon(QIcon(QPixmap("login.ico")))
        self.checkBox.setText("Learn this skill")
        self.checkBox.setIcon(QIcon(QPixmap("login.ico")))
        self.checkBox.stateChanged.connect(self.setBtnText)
    def setBtnText(self):
        if self.checkBox.isChecked():
            self.pushButton.setText("selected")
        else:
            self.pushButton.setText("unchecked")
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

## p-0748-0dbe4efd9a0e

- Location: `paragraph:748`

Source:

```text
3.5.4 实验结果
```

Translation:

```text
3.5.4 Experimental results
```

## p-0749-2ce869453092

- Location: `paragraph:749`

Source:

```text
运行程序窗口界面如下。
```

Translation:

```text
The running program window interface is as follows.
```

## p-0751-1529d2521193

- Location: `paragraph:751`

Source:

```text
当勾选checkBox控件时，PushButton按钮控件文本会变为“已选中”。
```

Translation:

```text
When the checkBox control is checked, the PushButton button control text will change to "checked".
```

## p-0753-4ceca85e02c6

- Location: `paragraph:753`

Source:

```text
当取消勾选checkBox控件时，PushButton按钮控件文本会变为“未选中”。
```

Translation:

```text
When the checkBox control is unchecked, the PushButton button control text will change to "unchecked".
```

## p-0758-aef5835b7ea6

- Location: `paragraph:758`

Source:

```text
3.6 控件设计-时间日期类
```

Translation:

```text
3.6 Control design-time and date class
```

## p-0759-fea6f6726292

- Location: `paragraph:759`

Source:

```text
3.6.1 实验目的
```

Translation:

```text
3.6.1 Experimental purpose
```

## p-0760-d0b774ee87ea

- Location: `paragraph:760`

Source:

```text
1）了解PyQt日期时间类控件Data/TimeEdit的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of the PyQt date and time control Data/TimeEdit.
```

## p-0761-78a53dc75a11

- Location: `paragraph:761`

Source:

```text
2）编写代码，掌握日期时间类控件Data/TimeEdit的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting methods of important properties of the date and time control Data/TimeEdit.
```

## p-0762-070c46a70909

- Location: `paragraph:762`

Source:

```text
3.6.2 基础知识
```

Translation:

```text
3.6.2 Basic knowledge
```

## p-0763-afb9475e9c55

- Location: `paragraph:763`

Source:

```text
Data/TimeEdit控件对应的类是QDataTimeEdit，该控件可以同时显示和编辑日期时间，它最常用的信号是dataTimeChanged，用来在日期或时间发生改变时发射。
```

Translation:

```text
The class corresponding to the Data/TimeEdit control is QDataTimeEdit. This control can display and edit date and time at the same time. Its most commonly used signal is dataTimeChanged, which is used to emit when the date or time changes.
```

## p-0764-ba3c8eda9397

- Location: `paragraph:764`

Source:

```text
3.6.3 实验步骤
```

Translation:

```text
3.6.3 Experimental steps
```

## p-0765-99ccf515919a

- Location: `paragraph:765`

Source:

```text
参照第2章在Pycharm软件中新建一个timePyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new timePyQt project in Pycharm software.
```

## p-0766-5a43d0513b4d

- Location: `paragraph:766`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个Data/TimeEdit控件和一个Label控件。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, and add a Data/TimeEdit control and a Label control through the toolbox.
```

## p-0768-75c4737ff86f

- Location: `paragraph:768`

Source:

```text
在Qt Designer中通过组合快捷键Ctrl + S保存timePyQt.ui，然后回到Pycharm软件中使用pyUIC工具生成timePyQt.py文件。
```

Translation:

```text
Save timePyQt.ui in Qt Designer by combining the shortcut keys Ctrl + S, and then return to Pycharm software and use the pyUIC tool to generate the timePyQt.py file.
```

## p-0769-02683522c8ac

- Location: `paragraph:769`

Source:

```text
修改main.py，在mainWindow类的__init__(self)方法中定义showChangeTime()槽函数，代码如下：
```

Translation:

```text
Modify main.py and define the showChangeTime() slot function in the __init__(self) method of the mainWindow class. The code is as follows:
```

## p-0772-279c54dde05e

- Location: `paragraph:772`

Source:

```text
添加Data/TimeEdit的dataTimeChanged信号与槽函数showChangeTime的关联，另外同步系统时间并设置时间日期的格式，代码如下：
```

Translation:

```text
Add the association between the dataTimeChanged signal of Data/TimeEdit and the slot function showChangeTime, synchronize the system time and set the time and date format. The code is as follows:
```

## p-0776-0de289f033ef

- Location: `paragraph:776`

Source:

```text
代码修改完成如下图所示：
```

Translation:

```text
The code modification is completed as shown below:
```

## p-0778-55d1fb616f3c

- Location: `paragraph:778`

Source:

```text
3.6.4 实验结果
```

Translation:

```text
3.6.4 Experimental results
```

## p-0779-cff286d45e69

- Location: `paragraph:779`

Source:

```text
运行程序窗口界面如下，Data/TimeEdit控件会显示当前日期和时间。
```

Translation:

```text
The running program window interface is as follows. The Data/TimeEdit control will display the current date and time.
```

## p-0781-0d05ff178370

- Location: `paragraph:781`

Source:

```text
选择Data/TimeEdit控件内的日期或时间后，点击右侧的上下箭头，可以调整日期和时间，并且修改后的日期和时间会通过信号与槽同步显示到Label控件上。
```

Translation:

```text
After selecting the date or time in the Data/TimeEdit control, click the up and down arrows on the right to adjust the date and time, and the modified date and time will be displayed on the Label control synchronously through signals and slots.
```

## p-0787-59a926e40a2e

- Location: `paragraph:787`

Source:

```text
3.7 控件设计-进度条类
```

Translation:

```text
3.7 Control design-progress bar class
```

## p-0788-fba00f9f0f38

- Location: `paragraph:788`

Source:

```text
3.7.1 实验目的
```

Translation:

```text
3.7.1 Experimental purpose
```

## p-0789-66cd930b4054

- Location: `paragraph:789`

Source:

```text
1）了解PyQt进度条类控件ProgressBar的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of the PyQt progress bar control ProgressBar.
```

## p-0790-4e2192e635b8

- Location: `paragraph:790`

Source:

```text
2）编写代码，掌握进度条类控件ProgressBar的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting methods of important properties of the progress bar control ProgressBar.
```

## p-0791-59267d5b9285

- Location: `paragraph:791`

Source:

```text
3.7.2 基础知识
```

Translation:

```text
3.7.2 Basic knowledge
```

## p-0792-0609f0247071

- Location: `paragraph:792`

Source:

```text
ProgressBar控件对应的类是QProgressBar，该控件通常在执行长时间任务时，用进度条告诉用户当前的进展情况。它最常用的信号是valueChanged，在进度条的值发生改变时发射。
```

Translation:

```text
The corresponding class of the ProgressBar control is QProgressBar. This control usually uses a progress bar to tell the user the current progress when executing a long-term task. Its most commonly used signal is valueChanged, which is emitted when the value of the progress bar changes.
```

## p-0793-3a876713d887

- Location: `paragraph:793`

Source:

```text
3.7.3 实验步骤
```

Translation:

```text
3.7.3 Experimental steps
```

## p-0794-35c7e83c2dcf

- Location: `paragraph:794`

Source:

```text
参照第2章在Pycharm软件中新建一个progressbarPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new progressbarPyQt project in Pycharm software.
```

## p-0795-0df396a11f9a

- Location: `paragraph:795`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个PushButton控件和一个ProgressBar控件。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, and add a PushButton control and a ProgressBar control through the toolbox.
```

## p-0797-040b766c0416

- Location: `paragraph:797`

Source:

```text
通过组合快捷键Ctrl + S保存为progressbarPyQt.ui，然后回到Pycharm软件使用pyUIC工具生成progressbarPyQt.py文件。
```

Translation:

```text
Save it as progressbarPyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the progressbarPyQt.py file.
```

## p-0798-2d9ed2c6bbec

- Location: `paragraph:798`

Source:

```text
在main.py代码类mainWindow中的__init__(self)方法中为进度条设置默认值为0%，另外更改PushButton控件的文本显示。
```

Translation:

```text
Set the default value for the progress bar to 0% in the __init__(self) method in the main.py code class mainWindow, and also change the text display of the PushButton control.
```

## p-0799-c97fb3a46276

- Location: `paragraph:799`

Source:

```text
self.progressBar.setValue(0)  # 设置进度条的值
```

Translation:

```text
self.progressBar.setValue(0) #Set the value of the progress bar
```

## p-0800-17d7f337031f

- Location: `paragraph:800`

Source:

```text
self.pushButton.setText("开始")
```

Translation:

```text
self.pushButton.setText("Start")
```

## p-0801-08a4fa913660

- Location: `paragraph:801`

Source:

```text
在main.py代码类mainWindow中的__init__(self)方法中编写槽函数running()，代码如下。
```

Translation:

```text
Write the slot function running() in the __init__(self) method in the main.py code class mainWindow. The code is as follows.
```

## p-0805-2993b36afd47

- Location: `paragraph:805`

Source:

```text
添加PushButton的clicked信号与槽函数running关联，代码如下：
```

Translation:

```text
Add the PushButton's clicked signal and associate it with the slot function running. The code is as follows:
```

## p-0806-a181b226e81d

- Location: `paragraph:806`

Source:

```text
self.progressBar.setValue(0)  # 设置进度条的最小值
```

Translation:

```text
self.progressBar.setValue(0) # Set the minimum value of the progress bar
```

## p-0807-17d7f337031f

- Location: `paragraph:807`

Source:

```text
self.pushButton.setText("开始")
```

Translation:

```text
self.pushButton.setText("Start")
```

## p-0810-0577eff17a36

- Location: `paragraph:810`

Source:

```text
编辑完成后代码如下：
```

Translation:

```text
After editing, the code is as follows:
```

## p-0814-3d39b82cb120

- Location: `paragraph:814`

Source:

```text
3.7.4 实验结果
```

Translation:

```text
3.7.4 Experimental results
```

## p-0815-8c4b01ff4ef5

- Location: `paragraph:815`

Source:

```text
运行程序窗口界面如下：
```

Translation:

```text
The running program window interface is as follows:
```

## p-0817-2ac9dbe3dbfd

- Location: `paragraph:817`

Source:

```text
点击界面上的开始按钮可以控制进度条变化，每点击一次进度+10%，当达到100%时再次点击按钮，进度条会归零即为0%。
```

Translation:

```text
Click the start button on the interface to control the change of the progress bar. Each click will increase the progress by 10%. When it reaches 100%, click the button again and the progress bar will return to 0%.
```

## p-0821-d8b517f7b7ed

- Location: `paragraph:821`

Source:

```text
3.8 控件设计-对话框类
```

Translation:

```text
3.8 Control Design-Dialog Class
```

## p-0822-e3a21e33e205

- Location: `paragraph:822`

Source:

```text
3.8.1 实验目的
```

Translation:

```text
3.8.1 Experimental purpose
```

## p-0823-87f083c74e7d

- Location: `paragraph:823`

Source:

```text
1）了解PyQt对话框类控件QMessageBox的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of the PyQt dialog box control QMessageBox.
```

## p-0824-e4657600c1d5

- Location: `paragraph:824`

Source:

```text
2）编写代码，掌握对话框类控件QMessageBox的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting method of important properties of the dialog box control QMessageBox.
```

## p-0825-1957e702881c

- Location: `paragraph:825`

Source:

```text
3.8.2 基础知识
```

Translation:

```text
3.8.2 Basic knowledge
```

## p-0826-86c4d4265ced

- Location: `paragraph:826`

Source:

```text
QMessageBox控件表示对话框，内置了5种不同类型的对话框，分别是消息对话框、问答对话框、警告对话框、错误对话框和关于对话框。语法格式如下：
```

Translation:

```text
The QMessageBox control represents a dialog box and has 5 different types of built-in dialog boxes, namely message dialog box, question and answer dialog box, warning dialog box, error dialog box and about dialog box. The syntax format is as follows:
```

## p-0828-6f2d0cbeb9c2

- Location: `paragraph:828`

Source:

```text
QWidget：self或者窗口对象，表示该对话框所属的窗口
```

Translation:

```text
QWidget: self or window object, indicating the window to which the dialog box belongs
```

## p-0829-9a6e9c8dd89b

- Location: `paragraph:829`

Source:

```text
Title：字符串，表示对话框的标题
```

Translation:

```text
Title: String, representing the title of the dialog box
```

## p-0830-18f7bc99b74e

- Location: `paragraph:830`

Source:

```text
Content：字符串，表示对话框的提示内容
```

Translation:

```text
Content: string, representing the prompt content of the dialog box
```

## p-0831-c9655739a43d

- Location: `paragraph:831`

Source:

```text
Buttons：对话框中要添加的按钮
```

Translation:

```text
Buttons: buttons to be added in the dialog box
```

## p-0832-1b9fd059906a

- Location: `paragraph:832`

Source:

```text
defaultButton：默认选中的按钮
```

Translation:

```text
defaultButton: the button selected by default
```

## p-0833-4e1625f35c8a

- Location: `paragraph:833`

Source:

```text
3.8.3 实验步骤
```

Translation:

```text
3.8.3 Experimental steps
```

## p-0834-44b4b389b27c

- Location: `paragraph:834`

Source:

```text
参照第2章在Pycharm软件中新建一个messageboxPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new messageboxPyQt project in Pycharm software.
```

## p-0835-4b3dbec424ad

- Location: `paragraph:835`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加5个PushButton控件。然后依次选中5个按钮，通过属性编辑器分别修改它们的属性：Qobject->objectName依次为：pushButton1、pushButton2、pushButton3、pushButton4、pushButton5；QAbstractButton->text依次为：消息框、警告框、问答框、错误框、关于框。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, and add 5 PushButton controls through the toolbox. Then select 5 buttons in sequence and modify their properties through the property editor: Qobject->objectName is: pushButton1, pushButton2, pushButton3, pushButton4, pushButton5; QAbstractButton->text is: message box, warning box, question and answer box, error box, and about box.
```

## p-0837-09ce7e4ae4ba

- Location: `paragraph:837`

Source:

```text
通过组合快捷键Ctrl + S保存为messageboxPyQt.ui，然后回到Pycharm软件使用pyUIC工具生成messageboxPyQt.py文件。
```

Translation:

```text
Save it as messageboxPyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the messageboxPyQt.py file.
```

## p-0838-bb2ccc2fba54

- Location: `paragraph:838`

Source:

```text
在main.py代码类mainWindow中的__init__(self)方法中编写五个对话框的槽函数，代码如下：
```

Translation:

```text
Write the slot functions of five dialog boxes in the __init__(self) method in the main.py code class mainWindow. The code is as follows:
```

## p-0840-00d2d2cb92b0

- Location: `paragraph:840`

Source:

```text
QMessageBox.information(None, '消息', '消息对话框！！！', QMessageBox.Ok)
def waning(self):
    QMessageBox.warning(None, '警告', '警告对话框！！！', QMessageBox.Ok)
def question(self):
    QMessageBox.question(None, '问答', '问答对话框！！！', QMessageBox.Ok)
def wrong(self):
    QMessageBox.critical(None, '错误', '错误对话框！！！', QMessageBox.Ok)
def about(self):
    QMessageBox.about(None, '关于', '关于对话框！！！')
```

Translation:

```text
QMessageBox.information(None, 'Message', 'Message dialog!!!', QMessageBox.Ok)
def waning(self):
    QMessageBox.warning(None, 'Warning', 'Warning dialog!!!', QMessageBox.Ok)
def question(self):
    QMessageBox.question(None, 'Q&A', 'Q&A dialog!!!', QMessageBox.Ok)
def wrong(self):
    QMessageBox.critical(None, 'Error', 'Error dialog!!!', QMessageBox.Ok)
def about(self):
    QMessageBox.about(None, 'About', 'About dialog!!!')
```

## p-0841-95a6236ac3d3

- Location: `paragraph:841`

Source:

```text
添加五个PushButton的clicked信号与槽函数关联，代码如下：
```

Translation:

```text
Add five PushButton clicked signals associated with the slot function. The code is as follows:
```

## p-0844-1b29572341f3

- Location: `paragraph:844`

Source:

```text
代码完成后如下图所示：
```

Translation:

```text
After the code is completed, it will look like the following picture:
```

## p-0847-2ce869453092

- Location: `paragraph:847`

Source:

```text
运行程序窗口界面如下。
```

Translation:

```text
The running program window interface is as follows.
```

## p-0852-04425fae1db0

- Location: `paragraph:852`

Source:

```text
3.8.4 实验结果
```

Translation:

```text
3.8.4 Experimental results
```

## p-0853-7d8fdf4e5605

- Location: `paragraph:853`

Source:

```text
依次点击界面上的5各按钮，弹出对话框效果如下：
```

Translation:

```text
Click each of the 5 buttons on the interface in sequence, and the dialog box will pop up as follows:
```

## p-0857-3e46da70c6e0

- Location: `paragraph:857`

Source:

```text
3.9 布局管理-线性
```

Translation:

```text
3.9 Layout Management-Linear
```

## p-0858-d2fd8028b24d

- Location: `paragraph:858`

Source:

```text
3.9.1 实验目的
```

Translation:

```text
3.9.1 Experimental purpose
```

## p-0859-709b3c77a85f

- Location: `paragraph:859`

Source:

```text
1）了解PyQt线性布局的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt linear layout.
```

## p-0860-7941047873e4

- Location: `paragraph:860`

Source:

```text
2）编写代码，实现线性布局的设置方法。
```

Translation:

```text
2) Write code to implement the linear layout setting method.
```

## p-0861-ad5a9787a7c5

- Location: `paragraph:861`

Source:

```text
3.9.2 基础知识
```

Translation:

```text
3.9.2 Basic knowledge
```

## p-0862-52aa052fe67b

- Location: `paragraph:862`

Source:

```text
线性布局是将放入其中的组件按照垂直或水平方向来布局。VerticalLayout的基类为：QVBoxLayout。而HorizontalLayout的基类为：QHBoxLayout。
```

Translation:

```text
Linear layout arranges the components placed in it vertically or horizontally. The base class of VerticalLayout is: QVBoxLayout. The base class of HorizontalLayout is: QHBoxLayout.
```

## p-0864-d087d824f667

- Location: `paragraph:864`

Source:

```text
3.9.3 实验步骤
```

Translation:

```text
3.9.3 Experimental steps
```

## p-0865-69d14882991b

- Location: `paragraph:865`

Source:

```text
注意：本节实验和之前操作不一样，不使用Qt Designer设计器来设计UI，而是直接通过代码的方式来进行UI设计。
```

Translation:

```text
Note: The experiment in this section is different from the previous operation. It does not use Qt Designer to design the UI, but directly designs the UI through code.
```

## p-0866-85b53526e21c

- Location: `paragraph:866`

Source:

```text
3.9.3.1 VerticalLayout垂直布局
```

Translation:

```text
3.9.3.1 VerticalLayout vertical layout
```

## p-0867-71d88c2dda2e

- Location: `paragraph:867`

Source:

```text
参照第2章在Pycharm软件中新建一个vlayoutPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new vlayoutPyQt project in Pycharm software.
```

## p-0868-54bfc7f54250

- Location: `paragraph:868`

Source:

```text
重写main.py代码，初始化时会调用自定义的initUI方法设置为垂直布局，代码如下：
```

Translation:

```text
Rewrite the main.py code. During initialization, the custom initUI method will be called to set the vertical layout. The code is as follows:
```

## p-0870-7ad8f2b14f8c

- Location: `paragraph:870`

Source:

```text
from PyQt5.QtWidgets import *
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() # 初始化窗口
    def initUI(self):
        vlayout=QVBoxLayout()
        self.setLayout(vlayout)
if __name__=='__main__':
    import sys
    app=QApplication(sys.argv) # 创建窗口程序
    demo=Demo() # 创建窗口类对象
    demo.show() # 显示窗口
    sys.exit(app.exec_())
```

Translation:

```text
from PyQt5.QtWidgets import *
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() #Initialize window
    def initUI(self):
        vlayout=QVBoxLayout()
        self.setLayout(vlayout)
if __name__=='__main__':
    importsys
    app=QApplication(sys.argv) #Create a window program
    demo=Demo() #Create window class object
    demo.show() # Display window
    sys.exit(app.exec_())
```

## p-0871-132fc8b754ed

- Location: `paragraph:871`

Source:

```text
此时如果直接运行代码的话，效果如下：
```

Translation:

```text
If you run the code directly at this time, the effect will be as follows:
```

## p-0873-b9d86194d6b0

- Location: `paragraph:873`

Source:

```text
接下来在initUI的vlayout垂直布局中添加三个按钮。这里需要使用addWidget()方法添加。同时，同时使用addSpacing()方法来设置控件之间的上下间距，代码如下：
```

Translation:

```text
Next, add three buttons to the vlayout vertical layout of initUI. You need to use the addWidget() method to add it here. At the same time, use the addSpacing() method to set the upper and lower spacing between controls. The code is as follows:
```

## p-0875-a3027e9f0e8e

- Location: `paragraph:875`

Source:

```text
vlayout=QVBoxLayout()
    btn1 = QPushButton()
    btn1.setText('按钮1')
    btn2 = QPushButton()
    btn2.setText('按钮2')
    btn3 = QPushButton()
    btn3.setText('按钮3')
    vlayout.addWidget(btn1)
    vlayout.addSpacing(10)
    vlayout.addWidget(btn2)
    vlayout.addSpacing(30)
    vlayout.addWidget(btn3)
    self.setLayout(vlayout)
```

Translation:

```text
vlayout=QVBoxLayout()
    btn1 = QPushButton()
    btn1.setText('Button 1')
    btn2 = QPushButton()
    btn2.setText('Button 2')
    btn3 = QPushButton()
    btn3.setText('Button 3')
    vlayout.addWidget(btn1)
    vlayout.addSpacing(10)
    vlayout.addWidget(btn2)
    vlayout.addSpacing(30)
    vlayout.addWidget(btn3)
    self.setLayout(vlayout)
```

## p-0876-09bd39f4f049

- Location: `paragraph:876`

Source:

```text
4）运行代码效果如下，3个按钮按照垂直方向进行依次排列。
```

Translation:

```text
4) The effect of running the code is as follows. The three buttons are arranged vertically.
```

## p-0879-40ac42da8928

- Location: `paragraph:879`

Source:

```text
3.9.3.2 HorizontalLayout水平布局
```

Translation:

```text
3.9.3.2 HorizontalLayout horizontal layout
```

## p-0880-b668f5a292d5

- Location: `paragraph:880`

Source:

```text
参照第2章在Pycharm软件中新建一个hlayoutPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new hlayoutPyQt project in Pycharm software.
```

## p-0881-13ce4b13e8d4

- Location: `paragraph:881`

Source:

```text
重写main.py代码，初始化时会调用自定义的initUI方法设置为水平布局，代码如下：
```

Translation:

```text
Rewrite the main.py code. During initialization, the custom initUI method will be called to set the horizontal layout. The code is as follows:
```

## p-0883-34e38cc70fc8

- Location: `paragraph:883`

Source:

```text
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() # 初始化窗口
    def initUI(self):
```

Translation:

```text
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() #Initialize window
    def initUI(self):
```

## p-0884-98bdf2e9c992

- Location: `paragraph:884`

Source:

```text
hlayout=QHBoxLayout()
        self.setLayout(hlayout)
if __name__=='__main__':
    import sys
    app=QApplication(sys.argv) # 创建窗口程序
    demo=Demo() # 创建窗口类对象
    demo.show() # 显示窗口
    sys.exit(app.exec_())
```

Translation:

```text
hlayout=QHBoxLayout()
        self.setLayout(hlayout)
if __name__=='__main__':
    importsys
    app=QApplication(sys.argv) #Create a window program
    demo=Demo() #Create window class object
    demo.show() # Display window
    sys.exit(app.exec_())
```

## p-0885-132fc8b754ed

- Location: `paragraph:885`

Source:

```text
此时如果直接运行代码的话，效果如下：
```

Translation:

```text
If you run the code directly at this time, the effect will be as follows:
```

## p-0887-7aa26c5b8dad

- Location: `paragraph:887`

Source:

```text
接下来在initUI的hlayout垂直布局中添加三个按钮。这里需要使用addWidget()方法添加。同时，同时使用addSpacing()方法来设置控件之间的上下间距，代码如下：
```

Translation:

```text
Next, add three buttons to the hlayout vertical layout of initUI. You need to use the addWidget() method to add it here. At the same time, use the addSpacing() method to set the upper and lower spacing between controls. The code is as follows:
```

## p-0889-8d79697cf273

- Location: `paragraph:889`

Source:

```text
hlayout=QHBoxLayout()
    btn1 = QPushButton()
    btn1.setText('按钮1')
    btn2 = QPushButton()
    btn2.setText('按钮2')
    btn3 = QPushButton()
    btn3.setText('按钮3')
    hlayout.addWidget(btn1)
    hlayout.addSpacing(10)
    hlayout.addWidget(btn2)
    hlayout.addSpacing(30)
    hlayout.addWidget(btn3)
    self.setLayout(hlayout)
```

Translation:

```text
hlayout=QHBoxLayout()
    btn1 = QPushButton()
    btn1.setText('Button 1')
    btn2 = QPushButton()
    btn2.setText('Button 2')
    btn3 = QPushButton()
    btn3.setText('Button 3')
    hlayout.addWidget(btn1)
    hlayout.addSpacing(10)
    hlayout.addWidget(btn2)
    hlayout.addSpacing(30)
    hlayout.addWidget(btn3)
    self.setLayout(hlayout)
```

## p-0891-3aa144861446

- Location: `paragraph:891`

Source:

```text
运行程序效果如下，3个按钮按照水平方向进行依次排列。
```

Translation:

```text
The effect of running the program is as follows. The three buttons are arranged horizontally.
```

## p-0896-b0ef5c7c6e72

- Location: `paragraph:896`

Source:

```text
3.9.4 实验结果
```

Translation:

```text
3.9.4 Experimental results
```

## p-0897-0bdca7c6c1a7

- Location: `paragraph:897`

Source:

```text
运行第一个程序，效果如下，3个按钮按照垂直方向进行依次排列。
```

Translation:

```text
Run the first program and the effect is as follows. The three buttons are arranged vertically.
```

## p-0900-c33c95e6188a

- Location: `paragraph:900`

Source:

```text
运行第二个程序，效果如下，3个按钮按照水平方向进行依次排列。
```

Translation:

```text
Run the second program, the effect is as follows, the three buttons are arranged horizontally.
```

## p-0904-67ca321e2481

- Location: `paragraph:904`

Source:

```text
3.10 布局管理-网格
```

Translation:

```text
3.10 Layout Management-Grid
```

## p-0905-2e42d892d374

- Location: `paragraph:905`

Source:

```text
3.10.1 实验目的
```

Translation:

```text
3.10.1 Experimental purpose
```

## p-0906-818887a18462

- Location: `paragraph:906`

Source:

```text
1）了解PyQt网格布局的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt grid layout.
```

## p-0907-847c155800d9

- Location: `paragraph:907`

Source:

```text
2）编写代码，实现网格布局的设置方法。
```

Translation:

```text
2) Write code to implement the grid layout setting method.
```

## p-0908-23228da57b87

- Location: `paragraph:908`

Source:

```text
3.10.2 基础知识
```

Translation:

```text
3.10.2 Basic knowledge
```

## p-0909-1e6689baebe1

- Location: `paragraph:909`

Source:

```text
GridLayout被称为网格布局，它将位于其中的控件放入一个多行多列的网格中。GridLayout控件需要将提供给它的空间划分成行和列，并把每个控件插入到正确的单元格中，示意图如下：
```

Translation:

```text
GridLayout is known as grid layout, which places the controls located within it into a grid with multiple rows and columns. The GridLayout control needs to divide the space provided to it into rows and columns, and insert each control into the correct cell. The schematic diagram is as follows:
```

## p-0911-9ca796a4183d

- Location: `paragraph:911`

Source:

```text
GridLayout基类是QGridLayout，其常用的方法说明如下：
```

Translation:

```text
The base class of GridLayout is QGridLayout, and its commonly used methods are described as follows:
```

## p-0912-cd48beb36d29

- Location: `paragraph:912`

Source:

```text
3.10.3 实验步骤
```

Translation:

```text
3.10.3 Experimental steps
```

## p-0913-69d14882991b

- Location: `paragraph:913`

Source:

```text
注意：本节实验和之前操作不一样，不使用Qt Designer设计器来设计UI，而是直接通过代码的方式来进行UI设计。
```

Translation:

```text
Note: The experiment in this section is different from the previous operation. It does not use Qt Designer to design the UI, but directly designs the UI through code.
```

## p-0914-49c38774d0d4

- Location: `paragraph:914`

Source:

```text
参照第2章在Pycharm软件中新建一个gridPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new gridPyQt project in Pycharm software.
```

## p-0915-e5d3094ac95a

- Location: `paragraph:915`

Source:

```text
重写main.py代码，初始化时会调用自定义的initUI方法设置为网格布局，代码如下：
```

Translation:

```text
Rewrite the main.py code. During initialization, the custom initUI method will be called to set the grid layout. The code is as follows:
```

## p-0917-c6f6b6fb8d96

- Location: `paragraph:917`

Source:

```text
from PyQt5.QtWidgets import *
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() # 初始化窗口
    def initUI(self):
        grid=QGridLayout() # 创建网格布局
        self.setLayout(grid) # 设置网格布局
if __name__=='__main__':
    import sys
    app=QApplication(sys.argv) # 创建窗口程序
    demo=Demo() # 创建窗口类对象
    demo.show() # 显示窗口
    sys.exit(app.exec_())
```

Translation:

```text
from PyQt5.QtWidgets import *
class Demo(QWidget):
    def __init__(self,parent=None):
        super(Demo,self).__init__(parent)
        self.initUI() #Initialize window
    def initUI(self):
        grid=QGridLayout() # Create grid layout
        self.setLayout(grid) # Set grid layout
if __name__=='__main__':
    importsys
    app=QApplication(sys.argv) #Create a window program
    demo=Demo() #Create window class object
    demo.show() # Display window
    sys.exit(app.exec_())
```

## p-0919-7e0a60b738fe

- Location: `paragraph:919`

Source:

```text
接下来在initUI的gird网格布局中，实现一个登陆窗口。代码如下：
```

Translation:

```text
Next, implement a login window in the grid layout of initUI. The code is as follows:
```

## p-0921-6b3ca7d2ab42

- Location: `paragraph:921`

Source:

```text
grid=QGridLayout() # 创建网格布局
    # 创建并设置标签文本
    label1=QLabel()
    label1.setText("用户名:")
    # 创建输入文本框
    text1=QLineEdit()
    # 创建并设置标签文本
    label2 = QLabel()
    label2.setText("密码：")
    # 创建输入文本框
    text2 = QLineEdit()
    # 创建“登录”和“取消”按钮
    btn1=QPushButton()
    btn1.setText("登录")
    btn2 = QPushButton()
    btn2.setText("取消")
    # 第一行第一列添加标签控件，并设置左对齐
    grid.addWidget(label1,0,0,QtCore.Qt.AlignLeft)
    # 第一行第二列添加输入文本框控件，并设置左对齐
    grid.addWidget(text1, 0, 1, QtCore.Qt.AlignLeft)
    # 第二行第一列添加标签控件，并设置左对齐
    grid.addWidget(label2, 1, 0, QtCore.Qt.AlignLeft)
    # 第二行第二列添加输入文本框控件，并设置左对齐
    grid.addWidget(text2, 1, 1, QtCore.Qt.AlignLeft)
    # 第三行第一列添加按钮控件，并设置居中对齐
    grid.addWidget(btn1, 2, 0, QtCore.Qt.AlignCenter)
    # 第三行第二列添加按钮控件，并设置居中对齐
    grid.addWidget(btn2, 2, 1, QtCore.Qt.AlignCenter)
    self.setLayout(grid) # 设置网格布局
```

Translation:

```text
grid=QGridLayout() # Create grid layout
    # Create and set label text
    label1=QLabel()
    label1.setText("Username:")
    #Create input text box
    text1=QLineEdit()
    # Create and set label text
    label2 = QLabel()
    label2.setText("Password:")
    #Create input text box
    text2 = QLineEdit()
    # Create "Login" and "Cancel" buttons
    btn1=QPushButton()
    btn1.setText("Login")
    btn2 = QPushButton()
    btn2.setText("Cancel")
    #Add a label control in the first row and column and set left alignment
    grid.addWidget(label1,0,0,QtCore.Qt.AlignLeft)
    #Add an input text box control in the first row and second column, and set left alignment
    grid.addWidget(text1, 0, 1, QtCore.Qt.AlignLeft)
    #Add a label control in the second row and first column and set left alignment
    grid.addWidget(label2, 1, 0, QtCore.Qt.AlignLeft)
    #Add an input text box control in the second row and column, and set left alignment
    grid.addWidget(text2, 1, 1, QtCore.Qt.AlignLeft)
    #Add a button control in the third row and first column and set center alignment
    grid.addWidget(btn1, 2, 0, QtCore.Qt.AlignCenter)
    #Add button controls in the third row and second column and set center alignment
    grid.addWidget(btn2, 2, 1, QtCore.Qt.AlignCenter)
    self.setLayout(grid) # Set grid layout
```

## p-0924-f544aec04f83

- Location: `paragraph:924`

Source:

```text
3.10.4 实验结果
```

Translation:

```text
3.10.4 Experimental results
```

## p-0925-b93d7681bb61

- Location: `paragraph:925`

Source:

```text
运行程序效果如下，界面中各种控件通过网格布局进行定位和对齐。
```

Translation:

```text
The effect of running the program is as follows. Various controls in the interface are positioned and aligned through grid layout.
```

## p-0932-259353f18da9

- Location: `paragraph:932`

Source:

```text
第四章 PyQt5进阶应用开发
```

Translation:

```text
Chapter 4 PyQt5 Advanced Application Development
```

## p-0933-305c799635f4

- Location: `paragraph:933`

Source:

```text
4.1 数据库操作
```

Translation:

```text
4.1 Database operations
```

## p-0934-a155870d1678

- Location: `paragraph:934`

Source:

```text
4.1.1 实验目的
```

Translation:

```text
4.1.1 Experimental purpose
```

## p-0935-cd2e9101dd4d

- Location: `paragraph:935`

Source:

```text
1）了解PyQt数据库的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of the PyQt database.
```

## p-0936-36af8fb402f2

- Location: `paragraph:936`

Source:

```text
2）编写代码，完成数据库的新建、操作（新增、删除、修改、查询）等方法。
```

Translation:

```text
2) Write code to complete database creation and operations (add, delete, modify, query) and other methods.
```

## p-0937-9854b48c9c96

- Location: `paragraph:937`

Source:

```text
4.1.2 基础知识
```

Translation:

```text
4.1.2 Basic knowledge
```

## p-0938-ea5f630353eb

- Location: `paragraph:938`

Source:

```text
Python操作数据库的流程如下：
```

Translation:

```text
The process of Python operating database is as follows:
```

## p-0940-babedb8509c3

- Location: `paragraph:940`

Source:

```text
SQLite不是一个客户端/服务器结构的数据库引擎，而是一种嵌入式数据库，该数据库本身就是一个文件。SQLite将整个数据库（包括定义、表、索引以及数据本身）作为一个单独的、可跨平台使用的文件存储在主机中。
```

Translation:

```text
SQLite is not a database engine with a client/server structure, but an embedded database that is itself a file. SQLite stores the entire database (including definitions, tables, indexes, and the data itself) as a single, cross-platform file on the host.
```

## p-0941-8dab9b57f8dc

- Location: `paragraph:941`

Source:

```text
Python中内置了SQLite3，所以在Python中使用SQLite数据库，不需要安装任何模块，可以直接使用。
```

Translation:

```text
SQLite3 is built into Python, so when using SQLite database in Python, you don’t need to install any modules and can be used directly.
```

## p-0943-0baedc554e10

- Location: `paragraph:943`

Source:

```text
创建数据库文件的流程如下图所示：
```

Translation:

```text
The process of creating a database file is shown in the figure below:
```

## p-0945-2c34c5f7edbb

- Location: `paragraph:945`

Source:

```text
数据库创建后就可以对数据进行增删改查等操作了。
```

Translation:

```text
After the database is created, operations such as adding, deleting, modifying, and querying data can be performed.
```

## p-0946-6d8e4224d4ee

- Location: `paragraph:946`

Source:

```text
操作数据库文件的流程如下图所示：
```

Translation:

```text
The process of operating database files is shown in the figure below:
```

## p-0948-9cf623156889

- Location: `paragraph:948`

Source:

```text
1）向数据表中新增数据可以使用SQL中的insert语句，语法如下：
```

Translation:

```text
1) To add data to the data table, you can use the insert statement in SQL. The syntax is as follows:
```

## p-0949-d96eb58c186c

- Location: `paragraph:949`

Source:

```text
2）查询数据表中数据可以使用SQL中的select语句，语法如下：
```

Translation:

```text
2) To query data in the data table, you can use the select statement in SQL. The syntax is as follows:
```

## p-0950-d8f4e8d4e90b

- Location: `paragraph:950`

Source:

```text
查询数据库时通常使用如下3种方式：
```

Translation:

```text
The following three methods are usually used when querying the database:
```

## p-0951-d058e63ce02b

- Location: `paragraph:951`

Source:

```text
fetchone()：获取查询结果集中的下一条记录
```

Translation:

```text
fetchone(): Get the next record in the query result set
```

## p-0952-b86ee1daf9d7

- Location: `paragraph:952`

Source:

```text
fetchmany()：获取指定数量的记录
```

Translation:

```text
fetchmany(): Get a specified number of records
```

## p-0953-7e927d0ac088

- Location: `paragraph:953`

Source:

```text
fetchall()：获取结构集的所有记录
```

Translation:

```text
fetchall(): Get all records of the structure set
```

## p-0954-474ee6f8d650

- Location: `paragraph:954`

Source:

```text
3）修改数据表中数据可以使用SQL中的update语句，语法如下：
```

Translation:

```text
3) To modify the data in the data table, you can use the update statement in SQL. The syntax is as follows:
```

## p-0955-128b45ad5197

- Location: `paragraph:955`

Source:

```text
4）修改数据表中数据可以使用SQL中的delete语句，语法如下：
```

Translation:

```text
4) To modify the data in the data table, you can use the delete statement in SQL. The syntax is as follows:
```

## p-0957-c01ce5556dce

- Location: `paragraph:957`

Source:

```text
4.1.3 实验步骤
```

Translation:

```text
4.1.3 Experimental steps
```

## p-0958-42e4ee46d5c7

- Location: `paragraph:958`

Source:

```text
新建数据库
```

Translation:

```text
Create a new database
```

## p-0959-e435fc3faa79

- Location: `paragraph:959`

Source:

```text
参照第2章在Pycharm软件中新建一个sqlitePyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new sqlitePyQt project in Pycharm software.
```

## p-0960-5f4f95c5755a

- Location: `paragraph:960`

Source:

```text
修改main.py的内容，首先创建一个mydb.db的数据库文件，然后执行SQL语句创建一个user（用户表），user表包含了id和name两个字段，代码如下：
```

Translation:

```text
Modify the content of main.py, first create a database file mydb.db, and then execute SQL statements to create a user (user table). The user table contains two fields, id and name. The code is as follows:
```

## p-0962-415841ad3d89

- Location: `paragraph:962`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mrsoft.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 执行一条SQL语句，创建user表
cursor.execute('create  table  user (id int(10)  primary key, name varchar(20))')
# 关闭游标
cursor.close()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mrsoft.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Execute a SQL statement and create the user table
cursor.execute('create table user (id int(10) primary key, name varchar(20))')
# Close cursor
cursor.close()
# Close Connection
conn.close()
```

## p-0963-6fb8af2cbc1b

- Location: `paragraph:963`

Source:

```text
此时运行程序，工程内会新增一个mydb.db数据库文件。
```

Translation:

```text
When you run the program at this time, a mydb.db database file will be added to the project.
```

## p-0966-2057b118b74f

- Location: `paragraph:966`

Source:

```text
添加数据
```

Translation:

```text
Add data
```

## p-0967-b5da4e82e3fb

- Location: `paragraph:967`

Source:

```text
接上一小节sqlitePyQt工程继续操作。因为已经创建了user表，所以可直接操作user表，向user表中插入3条用户信息，需要使用commit()方法提交事务。代码如下：
```

Translation:

```text
Continue the operation of the sqlitePyQt project from the previous section. Because the user table has been created, you can directly operate the user table and insert 3 pieces of user information into the user table. You need to use the commit() method to submit the transaction. The code is as follows:
```

## p-0969-0bb90411dbde

- Location: `paragraph:969`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 执行一条SQL语句，创建user表
#cursor.execute('create  table  user (id int(10)  primary key, name varchar(20))')
# 执行一条SQL语句，插入一条记录
cursor.execute('insert into user (id, name) values ("1", "hello")')
cursor.execute('insert into user (id, name) values ("2", "python")')
cursor.execute('insert into user (id, name) values ("3", "qt")')
# 关闭游标
cursor.close()
# 提交事务
conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mydb.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Execute a SQL statement and create the user table
#cursor.execute('create table user (id int(10) primary key, name varchar(20))')
#Execute a SQL statement and insert a record
cursor.execute('insert into user (id, name) values ("1", "hello")')
cursor.execute('insert into user (id, name) values ("2", "python")')
cursor.execute('insert into user (id, name) values ("3", "qt")')
# Close cursor
cursor.close()
# Submit transaction
conn.commit()
# Close Connection
conn.close()
```

## p-0970-534cd57e818f

- Location: `paragraph:970`

Source:

```text
2）上面代码中创建user表的语句（标记绿色的代码）被注释掉了，因为创建数据库和数据表的操作上一步已完成，如果不注释则会提示user数据表已存在。
```

Translation:

```text
2) The statement that creates the user table in the above code (code marked green) is commented out because the operation of creating the database and data table has been completed in the previous step. If it is not commented out, it will prompt that the user data table already exists.
```

## p-0973-b335a61696c8

- Location: `paragraph:973`

Source:

```text
查询数据
```

Translation:

```text
Query data
```

## p-0974-db8e482319a4

- Location: `paragraph:974`

Source:

```text
接上一小节sqlitePyQt工程继续操作，修改main.py注释掉之前的创建数据表、添加数据等代码，然后通过fetchone()方法查询数据并print打印出来。
```

Translation:

```text
Continue the operation of the sqlitePyQt project from the previous section, modify main.py to comment out the previous code for creating data tables, adding data, etc., and then query the data through the fetchone() method and print it out.
```

## p-0976-6c7d2ff25edb

- Location: `paragraph:976`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 执行一条SQL语句，创建user表
# cursor.execute('create  table  user (id int(10)  primary key, name varchar(20))')
# 执行一条SQL语句，插入一条记录
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
# 执行查询语句
cursor.execute('select * from user')
# 获取查询结果
result1 = cursor.fetchone()
print(result1)
# 关闭游标
cursor.close()
# 提交事务
#conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mydb.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Execute a SQL statement and create the user table
# cursor.execute('create table user (id int(10) primary key, name varchar(20))')
#Execute a SQL statement and insert a record
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
#Execute query statement
cursor.execute('select * from user')
# Get query results
result1 = cursor.fetchone()
print(result1)
# Close cursor
cursor.close()
# Submit transaction
#conn.commit()
# Close Connection
conn.close()
```

## p-0977-071f7830728b

- Location: `paragraph:977`

Source:

```text
此时运行代码，在Pycharm软件底部的Run栏中会打印出使用fetchone()方法返回的一个元组。
```

Translation:

```text
When you run the code at this time, a tuple returned by the fetchone() method will be printed in the Run column at the bottom of the Pycharm software.
```

## p-0979-c45291d4ae6a

- Location: `paragraph:979`

Source:

```text
继续修改main.py，注释掉之前的数据库操作代码，然后通过fetchmany()方法查询数据并print打印出来，打印如下：
```

Translation:

```text
Continue to modify main.py, comment out the previous database operation code, then query the data through the fetchmany() method and print it out, the print is as follows:
```

## p-0981-662e439d7c6c

- Location: `paragraph:981`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 执行一条SQL语句，创建user表
# cursor.execute('create  table  user (id int(10)  primary key, name varchar(20))')
# 执行一条SQL语句，插入一条记录
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
# 执行查询语句
cursor.execute('select * from user')
# 获取查询结果
#result1 = cursor.fetchone()
#print(result1)
result2 = cursor.fetchmany(2) # 使用fetchmany方法查询多条数据
print(result2)
# 关闭游标
cursor.close()
# 提交事务
#conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mydb.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Execute a SQL statement and create the user table
# cursor.execute('create table user (id int(10) primary key, name varchar(20))')
#Execute a SQL statement and insert a record
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
#Execute query statement
cursor.execute('select * from user')
# Get query results
#result1 = cursor.fetchone()
#print(result1)
result2 = cursor.fetchmany(2) # Use the fetchmany method to query multiple pieces of data
print(result2)
# Close cursor
cursor.close()
# Submit transaction
#conn.commit()
# Close Connection
conn.close()
```

## p-0982-7d38b27c8642

- Location: `paragraph:982`

Source:

```text
此时运行代码，在Pycharm软件底部的Run栏中会打印出fetchmany(2)返回列表中包含的2个元组。
```

Translation:

```text
Run the code at this time, and the 2 tuples included in the list returned by fetchmany(2) will be printed in the Run column at the bottom of the Pycharm software.
```

## p-0984-c8f72147fec5

- Location: `paragraph:984`

Source:

```text
继续修改main.py，注释掉之前的创建数据表、添加数据等代码，然后通过fetchall()方法查询数据并print打印出来。代码如下：
```

Translation:

```text
Continue to modify main.py, comment out the previous code for creating data tables, adding data, etc., then query the data through the fetchall() method and print it out. The code is as follows:
```

## p-0986-e633f5ea8bcd

- Location: `paragraph:986`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 执行一条SQL语句，创建user表
# cursor.execute('create  table  user (id int(10)  primary key, name varchar(20))')
# 执行一条SQL语句，插入一条记录
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
# 执行查询语句
cursor.execute('select * from user')
# 获取查询结果
#result1 = cursor.fetchone()
#print(result1)
#result2 = cursor.fetchmany(2) # 使用fetchmany方法查询多条数据
#print(result2)
result3 = cursor.fetchall() # 使用fetchall方法查询多条数据
print(result3)
# 关闭游标
cursor.close()
# 提交事务
#conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mydb.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Execute a SQL statement and create the user table
# cursor.execute('create table user (id int(10) primary key, name varchar(20))')
#Execute a SQL statement and insert a record
# cursor.execute('insert into user (id, name) values ("1", "hello")')
# cursor.execute('insert into user (id, name) values ("2", "python")')
# cursor.execute('insert into user (id, name) values ("3", "qt")')
#Execute query statement
cursor.execute('select * from user')
# Get query results
#result1 = cursor.fetchone()
#print(result1)
#result2 = cursor.fetchmany(2) # Use the fetchmany method to query multiple pieces of data
#print(result2)
result3 = cursor.fetchall() # Use the fetchall method to query multiple pieces of data
print(result3)
# Close cursor
cursor.close()
# Submit transaction
#conn.commit()
# Close Connection
conn.close()
```

## p-0987-330e6cfb711f

- Location: `paragraph:987`

Source:

```text
此时运行代码，在Pycharm软件底部的Run栏中会打印出fetchall()方法返回列表中包含的所有user表中数据组成的元组。
```

Translation:

```text
Run the code at this time, and a tuple consisting of all user table data contained in the list returned by the fetchall() method will be printed in the Run column at the bottom of the Pycharm software.
```

## p-0990-dd26bc4ab61e

- Location: `paragraph:990`

Source:

```text
修改数据
```

Translation:

```text
Modify data
```

## p-0991-27c86d90e3f3

- Location: `paragraph:991`

Source:

```text
接上一小节继续操作，修改main.py通过updata命令更新user数据表中的第2条数据内容，然后重新查询并打印数据表，代码如下：
```

Translation:

```text
Continue the operation from the previous section, modify main.py to update the second data content in the user data table through the updata command, and then requery and print the data table. The code is as follows:
```

## p-0993-311fbaa083b9

- Location: `paragraph:993`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
cursor.execute('update user set name = ? where id = ?',('world',2))
cursor.execute('select * from user')
result = cursor.fetchall()
print(result)
# 关闭游标
cursor.close()
# 提交事务
conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to SQLite database
# The database file is mydb.db. If the file does not exist, it will be automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
cursor.execute('update user set name = ? where id = ?',('world',2))
cursor.execute('select * from user')
result = cursor.fetchall()
print(result)
# Close cursor
cursor.close()
# Submit transaction
conn.commit()
# Close Connection
conn.close()
```

## p-0994-0060c9f9e5e7

- Location: `paragraph:994`

Source:

```text
此时运行代码，在Pycharm软件底部的Run栏中会打印user表中的全部数据，可以看到第二条已被修改为“world”。
```

Translation:

```text
When you run the code at this time, all the data in the user table will be printed in the Run column at the bottom of the Pycharm software. You can see that the second entry has been modified to "world".
```

## p-0997-89a259713bae

- Location: `paragraph:997`

Source:

```text
删除数据
```

Translation:

```text
Delete data
```

## p-0998-2e47fbff725c

- Location: `paragraph:998`

Source:

```text
接上一小节继续操作，修改main.py通过delete命令删除user数据表中的第1条数据，然后重新查询并打印数据表，代码如下：
```

Translation:

```text
Continue the operation from the previous section, modify main.py to delete the first data in the user data table through the delete command, and then requery and print the data table. The code is as follows:
```

## p-1000-90d5ff6d35e6

- Location: `paragraph:1000`

Source:

```text
# 连接到SQLite数据库
# 数据库文件是mydb.db，如果文件不存在，会自动在当前目录创建
conn = sqlite3.connect('mydb.db')
# 创建一个Cursor
cursor = conn.cursor()
# 删除ID为1的用户
cursor.execute('delete from user where id = ?',(1,))
# 获取所有用户信息
cursor.execute('select * from user')
# 记录查询结果
result = cursor.fetchall()
print(result)
# 关闭游标
cursor.close()
# 提交事务
conn.commit()
# 关闭Connection
conn.close()
```

Translation:

```text
# Connect to the SQLite database
# The database file is mydb.db. If it does not exist, it is automatically created in the current directory.
conn = sqlite3.connect('mydb.db')
# Create a Cursor
cursor = conn.cursor()
# Delete the user with ID 1
cursor.execute('delete from user where id = ?',(1,))
# Retrieve all user information
cursor.execute('select * from user')
# Record the query result
result = cursor.fetchall()
print(result)
# Close the cursor
cursor.close()
# Commit the transaction
conn.commit()
# Close the Connection
conn.close()
```

## p-1003-c6e147c50b8e

- Location: `paragraph:1003`

Source:

```text
4.1.4 实验结果
```

Translation:

```text
4.1.4 Experimental results
```

## p-1004-0c017210cdd5

- Location: `paragraph:1004`

Source:

```text
可以通过运行不同程序代码，在PyQt中创建+SQL数据库，并完成了新增、删除、修改、查询等多种数据库操作方法，在Pycharm软件底部的Run栏中会打印出user表中的全部数据。
```

Translation:

```text
You can create a +SQL database in PyQt by running different program codes, and complete various database operations such as adding, deleting, modifying, and querying. All data in the user table will be printed out in the Run column at the bottom of the Pycharm software.
```

## p-1008-6c673e433ad0

- Location: `paragraph:1008`

Source:

```text
4.2 文件操作
```

Translation:

```text
4.2 File operations
```

## p-1009-f3e893c759f5

- Location: `paragraph:1009`

Source:

```text
4.2.1 实验目的
```

Translation:

```text
4.2.1 Experimental purpose
```

## p-1010-efef4a021789

- Location: `paragraph:1010`

Source:

```text
1）了解PyQt内置文件操作的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt's built-in file operations.
```

## p-1011-c57a83f47766

- Location: `paragraph:1011`

Source:

```text
2）编写代码，掌握内置文件操作的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting methods of important attributes for built-in file operations.
```

## p-1012-614cfbc44ef1

- Location: `paragraph:1012`

Source:

```text
4.2.2 基础知识
```

Translation:

```text
4.2.2 Basic knowledge
```

## p-1013-11906467f1fe

- Location: `paragraph:1013`

Source:

```text
1）创建和打开文件
```

Translation:

```text
1) Create and open files
```

## p-1014-f6bcf885350e

- Location: `paragraph:1014`

Source:

```text
open()方法实现创建或打开文件对象，基本语法格式如下：
```

Translation:

```text
The open() method creates or opens a file object. The basic syntax format is as follows:
```

## p-1015-22b962bd368f

- Location: `paragraph:1015`

Source:

```text
filename：要创建或打开的文件名称，需要使用单引号或双引号括起来。
```

Translation:

```text
filename: The name of the file to be created or opened. It needs to be enclosed in single or double quotes.
```

## p-1016-a1f4416162c3

- Location: `paragraph:1016`

Source:

```text
mode：可选参数，用于指定文件的打开模式
```

Translation:

```text
mode: optional parameter, used to specify the file opening mode
```

## p-1017-1fef3d61722d

- Location: `paragraph:1017`

Source:

```text
2）写入文件
```

Translation:

```text
2) Write to file
```

## p-1018-60769b6ddb87

- Location: `paragraph:1018`

Source:

```text
write()方法实现向文件中写入内容，基本语法格式如下：
```

Translation:

```text
The write() method implements writing content to a file. The basic syntax format is as follows:
```

## p-1019-6195c3764a52

- Location: `paragraph:1019`

Source:

```text
3）读取文件
```

Translation:

```text
3) Read the file
```

## p-1020-b083bbc758a2

- Location: `paragraph:1020`

Source:

```text
基本语法格式如下：
```

Translation:

```text
The basic syntax format is as follows:
```

## p-1021-929a867d2f31

- Location: `paragraph:1021`

Source:

```text
4）关闭文件
```

Translation:

```text
4) Close the file
```

## p-1022-855db4bcbda4

- Location: `paragraph:1022`

Source:

```text
打开文件后，需要及时关闭，以免对文件造成不必要的破坏。close()语法的格式如下：
```

Translation:

```text
After opening a file, you need to close it promptly to avoid unnecessary damage to the file. The format of close() syntax is as follows:
```

## p-1024-c078dbfe32c5

- Location: `paragraph:1024`

Source:

```text
4.2.3 实验步骤
```

Translation:

```text
4.2.3 Experimental steps
```

## p-1025-d80801ff27ab

- Location: `paragraph:1025`

Source:

```text
参照第2章在Pycharm软件中新建一个filePyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new filePyQt project in Pycharm software.
```

## p-1026-12a6eb69a898

- Location: `paragraph:1026`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个PushButton控件和一个LineEdit控件。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, and add a PushButton control and a LineEdit control through the toolbox.
```

## p-1028-2503cd4011d9

- Location: `paragraph:1028`

Source:

```text
通过组合快捷键Ctrl + S保存为filePyQt.ui，然后回到Pycharm软件使用pyUIC工具生成filePyQt.py文件。
```

Translation:

```text
Save it as filePyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the filePyQt.py file.
```

## p-1029-6456a488b84b

- Location: `paragraph:1029`

Source:

```text
修改main.py显示UI主窗口界面，并设定两个控件的初始文本状态，修改代码如下。
```

Translation:

```text
Modify main.py to display the UI main window interface and set the initial text state of the two controls. Modify the code as follows.
```

## p-1032-8ce5781ea9f8

- Location: `paragraph:1032`

Source:

```text
self.statusbar.showMessage("输入框为空：按钮为读取；输入框有字符串：按钮为写入")
```

Translation:

```text
self.statusbar.showMessage("The input box is empty: the button is for reading; the input box has a string: the button is for writing")
```

## p-1033-a026559cb48f

- Location: `paragraph:1033`

Source:

```text
self.pushButton.setText('读取\写入')
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

Translation:

```text
self.pushButton.setText('read\write')
if __name__ == '__main__':
    app = QApplication(sys.argv)
    ui = mainWindow()
    ui.show()
    sys.exit(app.exec_())
```

## p-1034-0763b6fcacbe

- Location: `paragraph:1034`

Source:

```text
在mainWindow类的__init__(self)方法中自动创建info.txt文件，并写入内容。代码如下：
```

Translation:

```text
The info.txt file is automatically created in the __init__(self) method of the mainWindow class and the content is written. The code is as follows:
```

## p-1037-df268c975ba8

- Location: `paragraph:1037`

Source:

```text
file.write("第一条数据。")
        file.close()
```

Translation:

```text
file.write("The first piece of data.")
        file.close()
```

## p-1038-c139d41c1ce3

- Location: `paragraph:1038`

Source:

```text
在mainWindow类中定义一个getText槽函数，将按钮的clicked信号与槽函数关联。代码如下：
```

Translation:

```text
Define a getText slot function in the mainWindow class and associate the button's clicked signal with the slot function. The code is as follows:
```

## p-1041-b98a8117949e

- Location: `paragraph:1041`

Source:

```text
file.write("第一条数据。")
```

Translation:

```text
file.write("The first piece of data.")
```

## p-1046-b2bfa0b5a2b3

- Location: `paragraph:1046`

Source:

```text
继续修改getText函数，对输入数据有效性进行判断。判断str为空时，则读取‘info.txt’文件内容，并通过状态栏statusbar显示出来；str不为空，则写入str到‘info.txt’文件，并通过状态栏statusbar显示‘写入成功’，代码如下：
```

Translation:

```text
Continue to modify the getText function to judge the validity of the input data. When str is judged to be empty, read the contents of the 'info.txt' file and display it through the status bar status bar; if str is not empty, write str to the 'info.txt' file and display 'write successfully' through the status bar status bar. The code is as follows:
```

## p-1048-4d8abc81b401

- Location: `paragraph:1048`

Source:

```text
str = self.lineEdit.text()
    if str == "":
        file = open('info.txt', 'r')
        info = file.read()
        file.close()
        self.statusbar.showMessage("读取成功："+info)
    else:
        file = open('info.txt', 'w')
        file.write(str)
        file.close()
        self.statusbar.showMessage("写入成功")
```

Translation:

```text
str = self.lineEdit.text()
    if str == "":
        file = open('info.txt', 'r')
        info = file.read()
        file.close()
        self.statusbar.showMessage("Read successfully: "+info)
    else:
        file = open('info.txt', 'w')
        file.write(str)
        file.close()
        self.statusbar.showMessage("Written successfully")
```

## p-1051-632e2815be40

- Location: `paragraph:1051`

Source:

```text
4.2.4 实验结果
```

Translation:

```text
4.2.4 Experimental results
```

## p-1052-11360baff7ce

- Location: `paragraph:1052`

Source:

```text
运行程序窗口界面如下，不输入内容直接点击按钮，会读到“第一条数据”。
```

Translation:

```text
The running program window interface is as follows. Click the button without inputting content, and the "first data" will be read.
```

## p-1054-7764e418ace1

- Location: `paragraph:1054`

Source:

```text
输入文本内容后点击按钮，会将输入内容写入到文件。再次不输入内容直接点击按钮，会读到新的文件内容，与写入时输入的内容一致。
```

Translation:

```text
After entering the text content and clicking the button, the input content will be written to the file. Click the button again without entering content, and the new file content will be read, which is consistent with the content entered when writing.
```

## p-1058-95bf234b9d1f

- Location: `paragraph:1058`

Source:

```text
4.3 多线程-Qtimer
```

Translation:

```text
4.3 Multi-threading-Qtimer
```

## p-1059-103f18d8e656

- Location: `paragraph:1059`

Source:

```text
4.3.1 实验目的
```

Translation:

```text
4.3.1 Experimental purpose
```

## p-1060-9203b5942b44

- Location: `paragraph:1060`

Source:

```text
1）了解PyQt定时器QTimer的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt timer QTimer.
```

## p-1061-a37e68867af2

- Location: `paragraph:1061`

Source:

```text
2）编写代码，掌握定时器QTimer操作的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting method of important attributes of the timer QTimer operation.
```

## p-1062-94fa0dadc1dc

- Location: `paragraph:1062`

Source:

```text
4.3.2 基础知识
```

Translation:

```text
4.3.2 Basic knowledge
```

## p-1063-f543f04ec3c2

- Location: `paragraph:1063`

Source:

```text
在PyQt5中，如果需要周期性地执行某项操作，就可以使用QTimer类实现，QTimer类表示计时器，它可以定期发射timeout信号，时间间隔的长度在start()方法中指定，已毫秒为单位，如果要停止计时器，则需要使用方法stop()方法。
```

Translation:

```text
In PyQt5, if you need to perform an operation periodically, you can use the QTimer class. The QTimer class represents a timer. It can emit timeout signals regularly. The length of the time interval is specified in the start() method, in milliseconds. If you want to stop the timer, you need to use the stop() method.
```

## p-1064-db790ff5153a

- Location: `paragraph:1064`

Source:

```text
在使用QTimer类时，首先需要进行导入，代码如下：
```

Translation:

```text
When using the QTimer class, you first need to import it. The code is as follows:
```

## p-1065-0fc3d08780e5

- Location: `paragraph:1065`

Source:

```text
4.3.3 实验步骤
```

Translation:

```text
4.3.3 Experimental steps
```

## p-1066-bd98d9a2f5a8

- Location: `paragraph:1066`

Source:

```text
参照第2章在Pycharm软件中新建一个qtimePyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new qtimePyQt project in Pycharm software.
```

## p-1067-86760d1e882a

- Location: `paragraph:1067`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个PushButton控件和一个ProgressBar控件，通过属性编辑器设置进度条的初始值和按钮的显示文本。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, add a PushButton control and a ProgressBar control through the toolbox, and set the initial value of the progress bar and the display text of the button through the property editor.
```

## p-1069-875f1497d151

- Location: `paragraph:1069`

Source:

```text
通过组合快捷键Ctrl + S保存为qtimePyQt.ui，然后回到Pycharm软件使用pyUIC工具生成qtimePyQt.py文件。
```

Translation:

```text
Save it as qtimePyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the qtimePyQt.py file.
```

## p-1070-0d4f6837eaf7

- Location: `paragraph:1070`

Source:

```text
修改main.py显示UI主窗口界面，代码如下：
```

Translation:

```text
Modify main.py to display the UI main window interface, the code is as follows:
```

## p-1073-1b387c5d1360

- Location: `paragraph:1073`

Source:

```text
在mainWindow类中定义一个running槽函数，然后将按钮的clicked信号与之关联。代码如下：
```

Translation:

```text
Define a running slot function in the mainWindow class, and then associate the button's clicked signal with it. The code is as follows:
```

## p-1075-ab7148aeaa2a

- Location: `paragraph:1075`

Source:

```text
class mainWindow(QMainWindow, qtimeUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.clicked.connect(self.running)
    def running(self):
        global btnStatus
        if btnStatus:
            self.pushButton.setText("开始")
            btnStatus = False
        else:
            self.pushButton.setText("停止")
            btnStatus = True
```

Translation:

```text
class mainWindow(QMainWindow, qtimeUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.clicked.connect(self.running)
    def running(self):
        global btnStatus
        if btnStatus:
            self.pushButton.setText("Start")
            btnStatus = False
        else:
            self.pushButton.setText("Stop")
            btnStatus = True
```

## p-1076-bf42c568a768

- Location: `paragraph:1076`

Source:

```text
编写QTimer相关代码，实现如下功能：
```

Translation:

```text
Write QTimer related code to achieve the following functions:
```

## p-1077-5f3b4c6d8b21

- Location: `paragraph:1077`

Source:

```text
点击开始按钮启动定时器，通过信号timeout与槽函数timeOutFun()关联，定时器每间隔200ms触发一次定时器槽函数，则progressbar进度加1，当到进度达到100%时，则归0。
```

Translation:

```text
Click the start button to start the timer, which is associated with the slot function timeOutFun() through the signal timeout. The timer triggers the timer slot function every 200ms, and the progress bar progress is increased by 1. When the progress reaches 100%, it returns to 0.
```

## p-1078-9b98286e5645

- Location: `paragraph:1078`

Source:

```text
点击停止按钮后停止定时器。
```

Translation:

```text
Stop the timer after clicking the stop button.
```

## p-1080-1764ea626770

- Location: `paragraph:1080`

Source:

```text
btnStatus = False
i = 0
class mainWindow(QMainWindow, qtimeUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.clicked.connect(self.running)
    def running(self):
        global btnStatus
        if btnStatus:
            self.pushButton.setText("开始")
            btnStatus = False
            self.timer.stop()
        else:
            self.pushButton.setText("停止")
            btnStatus = True
            self.timer = QTimer(self)
            self.timer.start(200)
           self.timer.timeout.connect(self.timOutFun)
    def timOutFun(self):
        global i
        self.progressBar.setProperty("value", i)
        i+=1
        if i > 100:
            i = 0
```

Translation:

```text
btnStatus = False
i = 0
class mainWindow(QMainWindow, qtimeUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)
        self.pushButton.clicked.connect(self.running)
    def running(self):
        global btnStatus
        if btnStatus:
            self.pushButton.setText("Start")
            btnStatus = False
            self.timer.stop()
        else:
            self.pushButton.setText("Stop")
            btnStatus = True
            self.timer = QTimer(self)
            self.timer.start(200)
           self.timer.timeout.connect(self.timOutFun)
    def timOutFun(self):
        global i
        self.progressBar.setProperty("value", i)
        i+=1
        if i > 100:
            i = 0
```

## p-1082-b97191ade173

- Location: `paragraph:1082`

Source:

```text
4.3.4 实验结果
```

Translation:

```text
4.3.4 Experimental results
```

## p-1083-d4beb83c7444

- Location: `paragraph:1083`

Source:

```text
运行程序后，点击界面上的开始按钮，进度条控件会自动增加，可以点击停止按钮让进度条暂停，当进度条达到100%后会自动归零。
```

Translation:

```text
After running the program, click the start button on the interface, and the progress bar control will automatically increase. You can click the stop button to pause the progress bar. When the progress bar reaches 100%, it will automatically return to zero.
```

## p-1091-f6bd822f8963

- Location: `paragraph:1091`

Source:

```text
4.4 多线程-QThread
```

Translation:

```text
4.4 Multithreading-QThread
```

## p-1092-d3ed0ec4d854

- Location: `paragraph:1092`

Source:

```text
4.4.1 实验目的
```

Translation:

```text
4.4.1 Experimental purpose
```

## p-1093-23e2dbb2cd5b

- Location: `paragraph:1093`

Source:

```text
1）了解PyQt线程类QThread的重要属性和方法。
```

Translation:

```text
1) Understand the important properties and methods of PyQt thread class QThread.
```

## p-1094-cdaca41c8d77

- Location: `paragraph:1094`

Source:

```text
2）编写代码，掌握线程类QThread操作的重要属性的设置方法。
```

Translation:

```text
2) Write code and master the setting method of important attributes of thread class QThread operation.
```

## p-1095-d7354ddc91f5

- Location: `paragraph:1095`

Source:

```text
4.4.2 基础知识
```

Translation:

```text
4.4.2 Basic knowledge
```

## p-1096-ef815904b627

- Location: `paragraph:1096`

Source:

```text
在PyQt5中，要实现一个线程，需要创建QThread类的一个子类，并且实现其run()方法。在使用QThread类时，首先需要进行导入，代码如下：
```

Translation:

```text
In PyQt5, to implement a thread, you need to create a subclass of the QThread class and implement its run() method. When using the QThread class, you first need to import it. The code is as follows:
```

## p-1097-945d0f1240c2

- Location: `paragraph:1097`

Source:

```text
QThread类常用方法说明：
```

Translation:

```text
Description of common methods of QThread class:
```

## p-1098-2f9c9eb04dd2

- Location: `paragraph:1098`

Source:

```text
QThread类的常用信号及说明：
```

Translation:

```text
Common signals and descriptions of the QThread class:
```

## p-1099-37737cd99a68

- Location: `paragraph:1099`

Source:

```text
线程的生命周期：
```

Translation:

```text
Thread life cycle:
```

## p-1101-d1a16144401e

- Location: `paragraph:1101`

Source:

```text
4.4.3 实验步骤
```

Translation:

```text
4.4.3 Experimental steps
```

## p-1102-b817a9092b12

- Location: `paragraph:1102`

Source:

```text
参照第2章在Pycharm软件中新建一个qthreadPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new qthreadPyQt project in Pycharm software.
```

## p-1103-aab2700c25f3

- Location: `paragraph:1103`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为400 x 300，并通过工具箱添加一个PushButton控件，设置进度条的初始值。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 400 x 300, add a PushButton control through the toolbox, and set the initial value of the progress bar.
```

## p-1105-a256ccf8b7e4

- Location: `paragraph:1105`

Source:

```text
通过组合快捷键Ctrl + S保存为qthreadPyQt.ui，然后回到Pycharm软件使用pyUIC工具生成qthreadPyQt.py文件。
```

Translation:

```text
Save it as qthreadPyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the qthreadPyQt.py file.
```

## p-1106-928a7cbd83e9

- Location: `paragraph:1106`

Source:

```text
修改main.py显示UI主窗口界面，修改代码如下：
```

Translation:

```text
Modify main.py to display the UI main window interface. Modify the code as follows:
```

## p-1109-7565f44eaa76

- Location: `paragraph:1109`

Source:

```text
修改main.py添加一个PBarThread线程，在该线程中进行计数，并且在窗口启动时调用该线程。
```

Translation:

```text
Modify main.py to add a PBarThread thread, count in this thread, and call this thread when the window starts.
```

## p-1112-2c2d84c0204b

- Location: `paragraph:1112`

Source:

```text
修改main.py，在PBarThread线程中定时发送sigOut信号。然后定义一个showPBbar槽函数，用于接收PBarThread中emit传送过来的num，并根据num动态显示进度条。并且在__init__(self)方法中将信号与槽函数进行关联。
```

Translation:

```text
Modify main.py to send the sigOut signal regularly in the PBarThread thread. Then define a showPBbar slot function to receive the num sent by emit in PBarThread and dynamically display the progress bar based on num. And associate the signal with the slot function in the __init__(self) method.
```

## p-1117-c2e0189459a3

- Location: `paragraph:1117`

Source:

```text
4.4.4 实验结果
```

Translation:

```text
4.4.4 Experimental results
```

## p-1118-b948e74a76af

- Location: `paragraph:1118`

Source:

```text
运行程序后进度条会自动从0%到100%循环自增变化。
```

Translation:

```text
After running the program, the progress bar will automatically cycle from 0% to 100%.
```

## p-1124-1609e4adbb4e

- Location: `paragraph:1124`

Source:

```text
4.5 网络编程-TCP
```

Translation:

```text
4.5 Network Programming-TCP
```

## p-1125-cf1c02995707

- Location: `paragraph:1125`

Source:

```text
4.5.1 实验目的
```

Translation:

```text
4.5.1 Experimental purpose
```

## p-1126-681007d8121a

- Location: `paragraph:1126`

Source:

```text
1）掌握PyQt网络Socket编程方法。
```

Translation:

```text
1) Master the PyQt network Socket programming method.
```

## p-1127-73dae4ac91bd

- Location: `paragraph:1127`

Source:

```text
2）编写TCP客户端进行网络通信。
```

Translation:

```text
2) Write a TCP client for network communication.
```

## p-1128-96b9e5920cb9

- Location: `paragraph:1128`

Source:

```text
4.5.2 基础知识
```

Translation:

```text
4.5.2 Basic knowledge
```

## p-1129-8735d8c8ce9b

- Location: `paragraph:1129`

Source:

```text
Socket模块
```

Translation:

```text
Socket module
```

## p-1130-37d00f42377c

- Location: `paragraph:1130`

Source:

```text
Python提供了两个基本的socket模块：
```

Translation:

```text
Python provides two basic socket modules:
```

## p-1131-f2a9538fb91a

- Location: `paragraph:1131`

Source:

```text
1）socket模块，提供了标准的BSD Sockets API
```

Translation:

```text
1) The socket module provides the standard BSD Sockets API
```

## p-1132-817c6cbba034

- Location: `paragraph:1132`

Source:

```text
2）SocketServer模块，提供了服务器中心类，可以简化网络服务器的开发
```

Translation:

```text
2) The SocketServer module provides server center classes, which can simplify the development of network servers.
```

## p-1133-4eda8e6e4f66

- Location: `paragraph:1133`

Source:

```text
本节实验，主要是完成socket模块的TCP客户端编程。
```

Translation:

```text
The experiment in this section is mainly to complete the TCP client programming of the socket module.
```

## p-1134-c393d838a095

- Location: `paragraph:1134`

Source:

```text
socket()函数
```

Translation:

```text
socket() function
```

## p-1135-9d49440985d7

- Location: `paragraph:1135`

Source:

```text
Python 中，我们用 socket()函数来创建套接字，语法格式如下：
```

Translation:

```text
In Python, we use the socket() function to create a socket. The syntax is as follows:
```

## p-1136-4c367b5405a4

- Location: `paragraph:1136`

Source:

```text
参数说明：
```

Translation:

```text
Parameter description:
```

## p-1137-964d80d45a7c

- Location: `paragraph:1137`

Source:

```text
family: 套接字家族可以使用AF_UNIX或者AF_INET。
```

Translation:

```text
family: Socket family can use AF_UNIX or AF_INET.
```

## p-1138-4abcba373eac

- Location: `paragraph:1138`

Source:

```text
type: 套接字类型可以根据是面向连接的还是非连接分为SOCK_STREAM或SOCK_DGRAM。
```

Translation:

```text
type: The socket type can be classified as SOCK_STREAM or SOCK_DGRAM depending on whether it is connection-oriented or non-connection.
```

## p-1139-4985422259f0

- Location: `paragraph:1139`

Source:

```text
protocol: 一般不填默认为 0。
```

Translation:

```text
protocol: Generally left unfilled, the default value is 0.
```

## p-1140-a7199a9f485e

- Location: `paragraph:1140`

Source:

```text
Socket 对象(内建)方法
```

Translation:

```text
Socket object (built-in) methods
```

## p-1141-2485663072a0

- Location: `paragraph:1141`

Source:

```text
1）服务器端套接字
```

Translation:

```text
1) Server side socket
```

## p-1142-42a82fa2c97c

- Location: `paragraph:1142`

Source:

```text
2）客户端套接字
```

Translation:

```text
2) Client socket
```

## p-1143-166d05b50999

- Location: `paragraph:1143`

Source:

```text
3）公共用途的套接字函数
```

Translation:

```text
3) Public-purpose socket functions
```

## p-1145-9185f64e8b6f

- Location: `paragraph:1145`

Source:

```text
4.5.3 实验步骤
```

Translation:

```text
4.5.3 Experimental steps
```

## p-1146-9384ddb7260c

- Location: `paragraph:1146`

Source:

```text
参照第2章在Pycharm软件中新建一个socketClientPyQt工程。
```

Translation:

```text
Refer to Chapter 2 to create a new socketClientPyQt project in Pycharm software.
```

## p-1147-1adce7edf404

- Location: `paragraph:1147`

Source:

```text
在Qt Designer中创建一个MainWindow窗口，窗口尺寸为500 x 350，并通过工具箱添加三个Label控件、三个PushButton控件、一个TextBrower控件、一个TextEdit控件，摆到下图中对应的位置，并且设置文本显示初始值。
```

Translation:

```text
Create a MainWindow window in Qt Designer with a window size of 500 x 350, and add three Label controls, three PushButton controls, a TextBrower control, and a TextEdit control through the toolbox, place them in the corresponding positions as shown in the figure below, and set the initial value of the text display.
```

## p-1149-379477f6cc84

- Location: `paragraph:1149`

Source:

```text
上图界面中除了接收区和发送区两个Label控件以外，其他的控件在代码中都需要使用，所以要为这些控制设置objectName便于后面代码的编写，依次按照下表中填写objectName，另外控件尺寸也可以参考下表：
```

Translation:

```text
In the interface above, except for the two Label controls in the receiving area and the sending area, other controls need to be used in the code, so the objectName must be set for these controls to facilitate the writing of subsequent codes. Fill in the objectName in accordance with the following table. In addition, the control size can also refer to the following table:
```

## p-1150-728d0378accf

- Location: `paragraph:1150`

Source:

```text
通过组合快捷键Ctrl + S保存为socketClientPyQt.ui，然后回到Pycharm软件使用pyUIC工具生成socketClientPyQt.py文件。
```

Translation:

```text
Save it as socketClientPyQt.ui by combining the shortcut key Ctrl + S, and then return to the Pycharm software and use the pyUIC tool to generate the socketClientPyQt.py file.
```

## p-1151-0d4f6837eaf7

- Location: `paragraph:1151`

Source:

```text
修改main.py显示UI主窗口界面，代码如下：
```

Translation:

```text
Modify main.py to display the UI main window interface, the code is as follows:
```

## p-1156-ef6e45a050b2

- Location: `paragraph:1156`

Source:

```text
定义个initView函数，初始化连接服务器的Ip地址和端口的显示。
```

Translation:

```text
Define an initView function to initialize the display of the IP address and port connected to the server.
```

## p-1158-28e40cf0c3b7

- Location: `paragraph:1158`

Source:

```text
Port = 8899

class mainWindow(QMainWindow, scUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)

        self.initView()

    def initView(self):
        self.lab_serverinfo.setText("  IP地址：  " + ServerIP + "   端口：" + str(Port))
```

Translation:

```text
Port=8899

class mainWindow(QMainWindow, scUi.Ui_MainWindow):
    def __init__(self):
        super(mainWindow, self).__init__()
        self.setupUi(self)

        self.initView()

    def initView(self):
        self.lab_serverinfo.setText(" IP address: " + ServerIP + " Port: " + str(Port))
```

## p-1159-a89e608d8f86

- Location: `paragraph:1159`

Source:

```text
此时运行代码效果如下：
```

Translation:

```text
The effect of running the code at this time is as follows:
```

## p-1161-59bafa0a7598

- Location: `paragraph:1161`

Source:

```text
定义btnConnect、btnDisConnect、btnSend槽函数，然后分别通过“连接服务器”、“断开服务器”和“发送数据”按钮的clicked信号与它们关联。代码如下：
```

Translation:

```text
Define btnConnect, btnDisConnect, and btnSend slot functions, and then associate them with the clicked signals of the "Connect Server", "Disconnect Server" and "Send Data" buttons respectively. The code is as follows:
```

## p-1163-e6820cf2a4e2

- Location: `paragraph:1163`

Source:

```text
self.lab_serverinfo.setText("  IP地址：  " + ServerIP + "   端口：" + str(Port))
    self.pbtn_connect.clicked.connect(self.btnConnect)
    self.pbtn_disconnect.clicked.connect(self.btnDisConnect)
```

Translation:

```text
self.lab_serverinfo.setText(" IP address: " + ServerIP + " Port: " + str(Port))
    self.pbtn_connect.clicked.connect(self.btnConnect)
    self.pbtn_disconnect.clicked.connect(self.btnDisConnect)
```

## p-1167-b6edc1224db1

- Location: `paragraph:1167`

Source:

```text
先定义连接的socket以及状态标志位变量，然后分别实现btnConnect、btnDisConnect、btnSend这三个槽函数的功能：
```

Translation:

```text
First define the connected socket and status flag variables, and then implement the functions of the three slot functions btnConnect, btnDisConnect, and btnSend respectively:
```

## p-1168-8dacd4fb8f2b

- Location: `paragraph:1168`

Source:

```text
定义变量
```

Translation:

```text
Define variables
```

## p-1171-1ddbc9416736

- Location: `paragraph:1171`

Source:

```text
实现连接服务器按钮功能
```

Translation:

```text
Implement the connect server button function
```

## p-1172-37f45c648997

- Location: `paragraph:1172`

Source:

```text
通过socket连接对应IP和端口的服务器，连接成功后提示信息，创建子线程用于接收服务器数据，并将连接状态标志位设为真。
```

Translation:

```text
Connect to the server corresponding to the IP and port through the socket, prompt a message after the connection is successful, create a sub-thread to receive server data, and set the connection status flag to true.
```

## p-1174-2c24a2847bd4

- Location: `paragraph:1174`

Source:

```text
self.socketClientID = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        try:
            address = (ServerIP, Port)
            self.socketClientID.connect(address)
        except Exception as ret:
            self.statusbar.showMessage('无法连接目标服务器!!!')
            self.socketStatus = False
        else:
            msg = 'TCP客户端已连接IP:%s端口:%s\n' % address
            self.statusbar.showMessage(msg)

            #用于TCP客户端创建子线程的方法，阻塞式接收数据
            self.client_th = threading.Thread(target=self.tcp_client_recv,args=(address,))
            self.client_th.start()
            self.socketStatus = True
            self.pbtn_connect.setEnabled(False)
            self.pbtn_disconnect.setEnabled(True)
```

Translation:

```text
self.socketClientID = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        try:
            address = (ServerIP, Port)
            self.socketClientID.connect(address)
        except Exception as ret:
            self.statusbar.showMessage('Unable to connect to target server!!!')
            self.socketStatus = False
        else:
            msg = 'TCP client has connected to IP:%s port:%s\n' % address
            self.statusbar.showMessage(msg)

            #Method used for TCP client to create sub-threads, blocking data reception
            self.client_th = threading.Thread(target=self.tcp_client_recv,args=(address,))
            self.client_th.start()
            self.socketStatus = True
            self.pbtn_connect.setEnabled(False)
            self.pbtn_disconnect.setEnabled(True)
```

## p-1175-0dccf831e145

- Location: `paragraph:1175`

Source:

```text
实现断开服务器按钮功能
```

Translation:

```text
Implement the disconnect server button function
```

## p-1176-c2107db2145d

- Location: `paragraph:1176`

Source:

```text
修改连接状态标志位，设置连接和断开按钮的状态，关闭socket。
```

Translation:

```text
Modify the connection status flag, set the status of the connect and disconnect buttons, and close the socket.
```

## p-1178-801ae0af9ff1

- Location: `paragraph:1178`

Source:

```text
try:
        if self.socketStatus:
            self.socketStatus = False
            self.pbtn_connect.setEnabled(True)
            self.pbtn_disconnect.setEnabled(False)
            self.socketClientID.close()
            msg = '已断开网络'
            self.statusbar.showMessage(msg)
    except Exception as ret:
        pass
```

Translation:

```text
try:
        if self.socketStatus:
            self.socketStatus = False
            self.pbtn_connect.setEnabled(True)
            self.pbtn_disconnect.setEnabled(False)
            self.socketClientID.close()
            msg = 'Disconnected from the network'
            self.statusbar.showMessage(msg)
    except Exception as ret:
        pass
```

## p-1179-63215efca422

- Location: `paragraph:1179`

Source:

```text
实现发送数据按钮功能
```

Translation:

```text
Implement the send data button function
```

## p-1180-44bf56065622

- Location: `paragraph:1180`

Source:

```text
如果服务器已连接，则获取文本输入框的内容，通过send方法发送给服务器。
```

Translation:

```text
If the server is connected, obtain the content of the text input box and send it to the server through the send method.
```

## p-1182-3c1a44963d95

- Location: `paragraph:1182`

Source:

```text
if self.socketStatus is False:
        msg = '请先连接网络服务器'
    else:
        try:
            send_msg = (str(self.textEdit.toPlainText())).encode('utf-8')
            self.socketClientID.send(send_msg)
            msg = 'TCP客户端已发送'
        except Exception as ret:
            msg = '发送失败'
    self.statusbar.showMessage(msg)
```

Translation:

```text
if self.socketStatus is False:
        msg = 'Please connect to the network server first'
    else:
        try:
            send_msg = (str(self.textEdit.toPlainText())).encode('utf-8')
            self.socketClientID.send(send_msg)
            msg = 'TCP client sent'
        except Exception as ret:
            msg = 'Failed to send'
    self.statusbar.showMessage(msg)
```

## p-1183-4001a5273815

- Location: `paragraph:1183`

Source:

```text
实现tcp_client_recv线程，用于处理服务器发送过来的数据，并将接收到的数据在textBrowser中显示出来。
```

Translation:

```text
Implement the tcp_client_recv thread to process the data sent by the server and display the received data in textBrowser.
```

## p-1185-c5e9ad8b3b98

- Location: `paragraph:1185`

Source:

```text
# 用于TCP客户端创建子线程的方法，阻塞式接收
    while True:
        recv_msg = self.socketClientID.recv(1024)
        if recv_msg:
            msg = recv_msg.decode('utf-8')
            self.textBrowser.insertPlainText(msg)
        else:
            self.socketClientID.close()
            msg = '从服务器断开连接\n'
            self.statusbar.showMessage(msg)
            break
```

Translation:

```text
# Method used for TCP client to create sub-threads, blocking reception
    while True:
        recv_msg = self.socketClientID.recv(1024)
        if recv_msg:
            msg = recv_msg.decode('utf-8')
            self.textBrowser.insertPlainText(msg)
        else:
            self.socketClientID.close()
            msg = 'Disconnect from server\n'
            self.statusbar.showMessage(msg)
            break
```

## p-1188-26776dc97aea

- Location: `paragraph:1188`

Source:

```text
4.5.4 实验结果
```

Translation:

```text
4.5.4 Experimental results
```

## p-1189-4cbf3ec1877f

- Location: `paragraph:1189`

Source:

```text
运行程序界面如下：
```

Translation:

```text
The running program interface is as follows:
```

## p-1191-275cbe90fa40

- Location: `paragraph:1191`

Source:

```text
可以在光盘开发环境目录中找到SocketTool工具，打开工具选择TCP Server选项，然后点击创建按钮，输入监听端口后确认，端口号需要与代码中一致即可。这样就创建一个TCP服务端。
```

Translation:

```text
You can find the SocketTool tool in the CD development environment directory. Open the tool and select the TCP Server option, then click the Create button, enter the listening port and confirm. The port number needs to be consistent with the code. This creates a TCP server.
```

## p-1193-32a00f49a0b6

- Location: `paragraph:1193`

Source:

```text
TCP服务端创建后会自动启动TCP的监听。
```

Translation:

```text
After the TCP server is created, it will automatically start TCP monitoring.
```

## p-1195-f267c7360435

- Location: `paragraph:1195`

Source:

```text
这时点击程序的连接服务器按钮，连接后程序连接按钮会变灰，在服务端也会显示已连接。
```

Translation:

```text
At this time, click the connect server button of the program. After the connection, the program connection button will turn gray, and the server will also display connected.
```

## p-1197-ced828e399bf

- Location: `paragraph:1197`

Source:

```text
连接成功后程序和测试工具之间可以通过TCP进行数据收发。
```

Translation:

```text
After the connection is successful, data can be sent and received between the program and the test tool through TCP.
```

## p-1200-f8519014aa5e

- Location: `paragraph:1200`

Source:

```text
附录
```

Translation:

```text
appendix
```

## p-1201-f4385f9540e1

- Location: `paragraph:1201`

Source:

```text
附1.1 平台启动
```

Translation:

```text
Appendix 1.1 Platform startup
```

## p-1202-b0710cf9c8ce

- Location: `paragraph:1202`

Source:

```text
1）将标准220V电源线插入箱体背面的220V电源接口。
```

Translation:

```text
1) Insert the standard 220V power cord into the 220V power interface on the back of the box.
```

## p-1204-4f424fecce41

- Location: `paragraph:1204`

Source:

```text
2）再将实验平台箱体左侧的220V电源开关从“O”拨到“—”状态，此时箱体内的电源模块会给大底板供电。
```

Translation:

```text
2) Turn the 220V power switch on the left side of the experimental platform box from "O" to "-". At this time, the power module in the box will supply power to the large base plate.
```

## p-1206-ae26ac5ab1c1

- Location: `paragraph:1206`

Source:

```text
3）如果开启电源设备没正常启动，请检查大底板左下角的电源开关是否开启（拨到ON）。另外实验需要使用到摄像头，确保USB SELECT开关拨到右侧（ARM端）。
```

Translation:

```text
3) If the device does not start normally after turning on the power supply, please check whether the power switch in the lower left corner of the large base plate is turned on (switched to ON). In addition, the experiment requires the use of a camera. Make sure the USB SELECT switch is turned to the right (ARM side).
```

## p-1208-78c6fdef6fed

- Location: `paragraph:1208`

Source:

```text
4）实验平台开机后显示器上会有启动打印，等待系统启动，确保Orin nano边缘计算网关运行的系统是我们提供的Ubuntu 20.04.5 LTS desktop系统（网关系统内已集成各种开发环境，其中Python版本为3.8.10）。系统启动后界面如下：
```

Translation:

```text
4) After the experimental platform is turned on, there will be a startup print on the monitor. Wait for the system to start. Make sure that the system running the Orin nano edge computing gateway is the Ubuntu 20.04.5 LTS desktop system provided by us (various development environments have been integrated into the gateway system, and the Python version is 3.8.10). After the system starts, the interface is as follows:
```

## p-1210-8af69d5fd521

- Location: `paragraph:1210`

Source:

```text
5）进行系统后，检查无线鼠标及键盘是否能正常使用（蓝牙接收器已接上，鼠标和键盘开关已打开并确保有电）
```

Translation:

```text
5) After installing the system, check whether the wireless mouse and keyboard can be used normally (the Bluetooth receiver has been connected, the mouse and keyboard switches have been turned on and make sure there is power)
```

## p-1213-b070592aa5a1

- Location: `paragraph:1213`

Source:

```text
附1.2 网络连接
```

Translation:

```text
Appendix 1.2 Network connection
```

## p-1214-3a1079480f12

- Location: `paragraph:1214`

Source:

```text
PC端开发的PyQt5程序需要通过sftp等工具拷贝到边缘计算网关上运行，所以边缘计算网关与电脑的之间网络通畅是必要条件。边缘计算网关的网络连接可以采用有线或Wifi，确保边缘计算网关和PC在一个局域网内。
```

Translation:

```text
The PyQt5 program developed on the PC needs to be copied to the edge computing gateway through sftp and other tools to run, so a smooth network between the edge computing gateway and the computer is a necessary condition. The network connection of the edge computing gateway can be wired or Wifi, ensuring that the edge computing gateway and the PC are within a local area network.
```

## p-1216-3e551039a359

- Location: `paragraph:1216`

Source:

```text
以Wifi联网方式为例，具体联网方式如下：
```

Translation:

```text
Taking Wifi networking as an example, the specific networking methods are as follows:
```

## p-1217-8e3fbbe1b866

- Location: `paragraph:1217`

Source:

```text
1）点击实验平台屏幕右上角的网络图标，并在弹出网络列表中选择一个Wifi热点名称。（使用试验箱自带的鼠标操作。）
```

Translation:

```text
1) Click the network icon in the upper right corner of the experimental platform screen and select a Wifi hotspot name in the pop-up network list. (Use the mouse that comes with the test box to operate.)
```

## p-1219-1cd4b21dcac0

- Location: `paragraph:1219`

Source:

```text
2）在弹出对话框中输入对应Wifi热点的登录密码。（使用试验箱自带的键盘操作。）
```

Translation:

```text
2) Enter the login password corresponding to the Wifi hotspot in the pop-up dialog box. (Use the keyboard that comes with the test box to operate.)
```

## p-1221-77e49d81d0b2

- Location: `paragraph:1221`

Source:

```text
3）点击左侧软件列表中的Terminal程序，在弹出窗口中输入ifconfig命令查询网关的IP地址。
```

Translation:

```text
3) Click the Terminal program in the software list on the left, and enter the ifconfig command in the pop-up window to query the IP address of the gateway.
```

## p-1223-55fb7b7cfb8b

- Location: `paragraph:1223`

Source:

```text
4）输入命令后打印如下，其中名为wlan0的即为Wifi的IP地址。如果采用有线方式连接则查看eth0网卡中的IP地址，若未分配IP地址，可以使用指令手动配置IP地址。
```

Translation:

```text
4) After entering the command, print the following, where the name wlan0 is the IP address of Wifi. If you use a wired connection, check the IP address in the eth0 network card. If no IP address is assigned, you can use the command to manually configure the IP address.
```

## p-1224-ec45e9413ffd

- Location: `paragraph:1224`

Source:

```text
ifconfig eth0 192.168.31.146 netmask 255.255.255.0      //手动设置有线网络IP
```

Translation:

```text
ifconfig eth0 192.168.31.146 netmask 255.255.255.0 //Manually set the wired network IP
```

## p-1226-8b69c5ce10f0

- Location: `paragraph:1226`

Source:

```text
附1.3 SSH登录
```

Translation:

```text
Appendix 1.3 SSH login
```

## p-1227-325445273eca

- Location: `paragraph:1227`

Source:

```text
1）运行产品光盘\02-软件环境中的MobaXterm 软件，点击左上角的Session按钮。
```

Translation:

```text
1) Run the MobaXterm software in the product CD\02-software environment and click the Session button in the upper left corner.
```

## p-1230-4225dfe334dd

- Location: `paragraph:1230`

Source:

```text
2）在弹出的对话框中选择SSH协议，然后点击OK按钮。
```

Translation:

```text
2) Select the SSH protocol in the pop-up dialog box, and then click the OK button.
```

## p-1233-434251856f5b

- Location: `paragraph:1233`

Source:

```text
3）在SSH的Remote host栏填写网关的IP地址，然后点击OK按钮。
```

Translation:

```text
3) Fill in the IP address of the gateway in the Remote host field of SSH, and then click the OK button.
```

## p-1236-98346de93d37

- Location: `paragraph:1236`

Source:

```text
4）然后提示输入登录的账户和密码，用户名为：jetson，密码为：12345678。
```

Translation:

```text
4) Then you are prompted to enter the login account and password. The user name is: Jetson and the password is: 12345678.
```

## p-1239-bde231bdf3b4

- Location: `paragraph:1239`

Source:

```text
5）如果登录的账户和密码验证通过，则会进入到OrinNano网关系统的系统命令行，同时在MobaXterm 软件左侧Stfp栏中可以看到系统的目录结构，可以通过它来双向拷贝文件。
```

Translation:

```text
5) If the login account and password are verified, you will enter the system command line of the OrinNano gateway system. At the same time, you can see the system directory structure in the Stfp column on the left side of the MobaXterm software, through which you can copy files in both directions.
```

## p-1244-881054ed1c62

- Location: `paragraph:1244`

Source:

```text
附1.4 文件拷贝
```

Translation:

```text
Attachment 1.4 File copy
```

## p-1245-0a0f7f485488

- Location: `paragraph:1245`

Source:

```text
1）MobaXterm软件左侧的Stfp可以显示网关的系统的文件目录，还可以用于文件拷贝，可以提前新建一个文件夹用于存放文件，点击工具栏刷新按钮就可以在目录中找到新增的文件夹。
```

Translation:

```text
1) The Stfp on the left side of the MobaXterm software can display the file directory of the gateway system and can also be used for file copy. You can create a new folder in advance to store files. Click the refresh button on the toolbar to find the new folder in the directory.
```

## p-1249-3558611155ca

- Location: `paragraph:1249`

Source:

```text
2）然后长按鼠标左键拖动PC端文件至MobaXterm软件左侧的Stfp栏中对应文件夹路径下。
```

Translation:

```text
2) Then long press the left button of the mouse and drag the PC file to the corresponding folder path in the Stfp column on the left side of the MobaXterm software.
```

## p-1252-18edd27f080d

- Location: `paragraph:1252`

Source:

```text
3）拷贝后在Stfp栏中能看到从PC电脑复制过来的文件，在命令行中输入ls命令同样可以查看该文件。
```

Translation:

```text
3) After copying, you can see the file copied from the PC in the Stfp column. You can also view the file by entering the ls command on the command line.
```

## p-1255-3f7ff284ce6c

- Location: `paragraph:1255`

Source:

```text
4）MobaXterm软件中的Stfp工具支持单文件、多文件或文件夹拖拽，同时也支持双向拖拽，即可以将网关上的文件反向拖拽至PC电脑。
```

Translation:

```text
4) The Stfp tool in MobaXterm software supports single file, multiple files or folder drag and drop, and also supports two-way drag and drop, that is, files on the gateway can be dragged in reverse to the PC.
```

## t-003-r001-c001-66a7c7a9fb96

- Location: `table:3/row:1/cell:1`

Source:

```text
方  法
```

Translation:

```text
method
```

## t-003-r001-c002-04879b870463

- Location: `table:3/row:1/cell:2`

Source:

```text
说  明
```

Translation:

```text
illustrate
```

## t-003-r002-c002-fffb9afefce3

- Location: `table:3/row:2/cell:2`

Source:

```text
row:行数
column：列数
alignment：对齐方式
```

Translation:

```text
row: number of rows
column: number of columns
alignment: alignment
```

## t-003-r003-c002-bd0e0f2f7c7e

- Location: `table:3/row:3/cell:2`

Source:

```text
设置行比例
```

Translation:

```text
Set row proportions
```

## t-003-r004-c002-94497d1a4f2a

- Location: `table:3/row:4/cell:2`

Source:

```text
设置列比例
```

Translation:

```text
Set column proportions
```

## t-003-r005-c002-781184b35668

- Location: `table:3/row:5/cell:2`

Source:

```text
设置控件在水平和垂直方向上的间距
```

Translation:

```text
Set the horizontal and vertical spacing of controls
```

## t-004-r001-c001-194c0c264747

- Location: `table:4/row:1/cell:1`

Source:

```text
insert into 表名（字段1,字段2,.....,字段n） values（字段值1,字段值2，...,字段值n）
```

Translation:

```text
insert into table name (field 1, field 2,..., field n) values ​​(field value 1, field value 2,..., field value n)
```

## t-005-r001-c001-fb4287ec8bf6

- Location: `table:5/row:1/cell:1`

Source:

```text
select 字段名1,字段名2,..... from 表名 where 查询条件
```

Translation:

```text
select field name 1, field name 2,... from table name where query conditions
```

## t-006-r001-c001-9dabd5ed7988

- Location: `table:6/row:1/cell:1`

Source:

```text
update 表名 set 字段名 = 字段值 where 查询条件
```

Translation:

```text
update table name set field name = field value where query condition
```

## t-007-r001-c001-b84cdfec666f

- Location: `table:7/row:1/cell:1`

Source:

```text
delete from 表名 where 查询条件
```

Translation:

```text
delete from table name where query condition
```

## t-009-r001-c001-321b9558401a

- Location: `table:9/row:1/cell:1`

Source:

```text
值
```

Translation:

```text
value
```

## t-009-r001-c002-26670dda421f

- Location: `table:9/row:1/cell:2`

Source:

```text
说明
```

Translation:

```text
illustrate
```

## t-009-r001-c003-f08b55c42cfa

- Location: `table:9/row:1/cell:3`

Source:

```text
注意
```

Translation:

```text
Notice
```

## t-009-r002-c002-937ef81ff676

- Location: `table:9/row:2/cell:2`

Source:

```text
以只读模式打开文件。文件的指针将会放在文件的开头
```

Translation:

```text
Open the file in read-only mode. The file pointer will be placed at the beginning of the file
```

## t-009-r002-c003-e8c783c1a00d

- Location: `table:9/row:2/cell:3`

Source:

```text
文件必须存在
```

Translation:

```text
File must exist
```

## t-009-r003-c002-263e6b32eb2d

- Location: `table:9/row:3/cell:2`

Source:

```text
以二进制格式打开文件，并且采用只读模式。文件的指针将会放在文件的开头。一般用于非文本文件，如图片、声音等
```

Translation:

```text
Open the file in binary format and in read-only mode. The file pointer will be placed at the beginning of the file. Generally used for non-text files, such as pictures, sounds, etc.
```

## t-009-r003-c003-e8c783c1a00d

- Location: `table:9/row:3/cell:3`

Source:

```text
文件必须存在
```

Translation:

```text
File must exist
```

## t-009-r004-c002-a0cddb4ed74a

- Location: `table:9/row:4/cell:2`

Source:

```text
打开文件后，可以读取文件内容，也可以写入新的内容覆盖原有内容（从文件开头进行覆盖）
```

Translation:

```text
After opening the file, you can read the file content, or write new content to overwrite the original content (overwrite from the beginning of the file)
```

## t-009-r004-c003-e8c783c1a00d

- Location: `table:9/row:4/cell:3`

Source:

```text
文件必须存在
```

Translation:

```text
File must exist
```

## t-009-r005-c002-24bda740924a

- Location: `table:9/row:5/cell:2`

Source:

```text
以二进制格式打开文件，并且采用读写模式。文件的指针将会放在文件的开头。一般用于非文本文件，如图片、声音等
```

Translation:

```text
Open the file in binary format and in read-write mode. The file pointer will be placed at the beginning of the file. Generally used for non-text files, such as pictures, sounds, etc.
```

## t-009-r005-c003-e8c783c1a00d

- Location: `table:9/row:5/cell:3`

Source:

```text
文件必须存在
```

Translation:

```text
File must exist
```

## t-009-r006-c002-6db774a5e75d

- Location: `table:9/row:6/cell:2`

Source:

```text
以只写模式打开文件
```

Translation:

```text
Open file in write-only mode
```

## t-009-r006-c003-0887a2f6ff19

- Location: `table:9/row:6/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r007-c002-9a7686f5ef58

- Location: `table:9/row:7/cell:2`

Source:

```text
以二进制格式打开文件，并且采用只写模式。一般用于非文本文件，如图片、声音等
```

Translation:

```text
Open the file in binary format and in write-only mode. Generally used for non-text files, such as pictures, sounds, etc.
```

## t-009-r007-c003-0887a2f6ff19

- Location: `table:9/row:7/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r008-c002-4b67b5d1bcf9

- Location: `table:9/row:8/cell:2`

Source:

```text
打开文件后，先清空原有内容，使其变为一个空的文件，对这个空文件有读写权限
```

Translation:

```text
After opening the file, first clear the original content to make it an empty file, and have read and write permissions on this empty file.
```

## t-009-r008-c003-0887a2f6ff19

- Location: `table:9/row:8/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r009-c002-b571f649c3d5

- Location: `table:9/row:9/cell:2`

Source:

```text
以二进制格式打开文件，并且采用读写模式。一般用于非文本文件，如图片、声音等
```

Translation:

```text
Open the file in binary format and in read-write mode. Generally used for non-text files, such as pictures, sounds, etc.
```

## t-009-r009-c003-0887a2f6ff19

- Location: `table:9/row:9/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r010-c002-014c8c713d9e

- Location: `table:9/row:10/cell:2`

Source:

```text
以追加模式打开一个文件。如果该文件已经存在，文件指针将放在文件的末尾（即新内容会被写入到已有内容之后），否则，创建新文件用于写入
```

Translation:

```text
Open a file in append mode. If the file already exists, the file pointer will be placed at the end of the file (i.e. the new content will be written after the existing content), otherwise, a new file will be created for writing
```

## t-009-r010-c003-0887a2f6ff19

- Location: `table:9/row:10/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r011-c002-1b42a6d3da81

- Location: `table:9/row:11/cell:2`

Source:

```text
以二进制格式打开文件，并且采用追加模式。如果该文件已经存在，文件指针将放在文件的末尾（即新内容会被写入到已有内容之后），否则，创建新文件用于写入
```

Translation:

```text
Open the file in binary format and in append mode. If the file already exists, the file pointer will be placed at the end of the file (i.e. the new content will be written after the existing content), otherwise, a new file will be created for writing
```

## t-009-r011-c003-0887a2f6ff19

- Location: `table:9/row:11/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r012-c002-15c240cc9c9d

- Location: `table:9/row:12/cell:2`

Source:

```text
以读写模式打开文件。如果该文件已经存在，文件指针将放在文件的末尾（即新内容会被写入到已有内容之后），否则，创建新文件用于读写
```

Translation:

```text
Open the file in read-write mode. If the file already exists, the file pointer will be placed at the end of the file (that is, the new content will be written after the existing content), otherwise, a new file will be created for reading and writing.
```

## t-009-r012-c003-0887a2f6ff19

- Location: `table:9/row:12/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-009-r013-c002-1dd2ebf1d4ff

- Location: `table:9/row:13/cell:2`

Source:

```text
以二进制格式打开文件，并且采用追加模式。如果该文件已经存在，文件指针将放在文件的末尾（即新内容会被写入到已有内容之后），否则，创建新文件用于读写
```

Translation:

```text
Open the file in binary format and in append mode. If the file already exists, the file pointer will be placed at the end of the file (that is, the new content will be written after the existing content), otherwise, a new file will be created for reading and writing.
```

## t-009-r013-c003-0887a2f6ff19

- Location: `table:9/row:13/cell:3`

Source:

```text
文件存在，则将其覆盖，否则创建新文件
```

Translation:

```text
If the file exists, it will be overwritten, otherwise a new file will be created
```

## t-011-r001-c001-66a7c7a9fb96

- Location: `table:11/row:1/cell:1`

Source:

```text
方  法
```

Translation:

```text
method
```

## t-011-r001-c002-04879b870463

- Location: `table:11/row:1/cell:2`

Source:

```text
说  明
```

Translation:

```text
illustrate
```

## t-011-r002-c002-21d41ac5882e

- Location: `table:11/row:2/cell:2`

Source:

```text
读取指定个数字符
```

Translation:

```text
Read the specified number of characters
```

## t-011-r003-c002-f25deb806672

- Location: `table:11/row:3/cell:2`

Source:

```text
读取一行
```

Translation:

```text
read a line
```

## t-011-r004-c002-5107dac808f8

- Location: `table:11/row:4/cell:2`

Source:

```text
读取所有行
```

Translation:

```text
read all lines
```

## t-015-r001-c001-66a7c7a9fb96

- Location: `table:15/row:1/cell:1`

Source:

```text
方  法
```

Translation:

```text
method
```

## t-015-r001-c002-04879b870463

- Location: `table:15/row:1/cell:2`

Source:

```text
说  明
```

Translation:

```text
illustrate
```

## t-015-r002-c002-81ad37f29c85

- Location: `table:15/row:2/cell:2`

Source:

```text
线程的起点，在调用start()方法之后，新创建的线程讲调用该方法
```

Translation:

```text
The starting point of the thread. After calling the start() method, the newly created thread will call this method.
```

## t-015-r003-c002-f25deb806672

- Location: `table:15/row:3/cell:2`

Source:

```text
读取一行
```

Translation:

```text
read a line
```

## t-015-r004-c002-5107dac808f8

- Location: `table:15/row:4/cell:2`

Source:

```text
读取所有行
```

Translation:

```text
read all lines
```

## t-015-r005-c002-5d763922174d

- Location: `table:15/row:5/cell:2`

Source:

```text
以秒为单位休眠线程
```

Translation:

```text
Sleep thread in seconds
```

## t-015-r006-c002-30e1ae6a05b1

- Location: `table:15/row:6/cell:2`

Source:

```text
退出线程的事件循环并返回代码0（成功），相当于exit(0)
```

Translation:

```text
Exits the thread's event loop and returns code 0 (success), equivalent to exit(0)
```

## t-015-r007-c002-da9432d8950a

- Location: `table:15/row:7/cell:2`

Source:

```text
退出线程的事件循环，并返回代码。0成功，任何非0表示错误
```

Translation:

```text
Exits the thread's event loop and returns the code. 0 for success, anything other than 0 indicates an error
```

## t-015-r008-c002-5cf53776868f

- Location: `table:15/row:8/cell:2`

Source:

```text
是否完成
```

Translation:

```text
Is it done?
```

## t-015-r009-c002-2d9f07226107

- Location: `table:15/row:9/cell:2`

Source:

```text
是否正在运行
```

Translation:

```text
Is it running
```

## t-016-r001-c001-66a7c7a9fb96

- Location: `table:16/row:1/cell:1`

Source:

```text
方  法
```

Translation:

```text
method
```

## t-016-r001-c002-04879b870463

- Location: `table:16/row:1/cell:2`

Source:

```text
说  明
```

Translation:

```text
illustrate
```

## t-016-r002-c002-dea98a1b5d01

- Location: `table:16/row:2/cell:2`

Source:

```text
在调用run()方法之前，在相关线程开始执行时从该线程发射
```

Translation:

```text
Emitted from the relevant thread when it begins execution before calling the run() method.
```

## t-016-r003-c002-6907b0073a01

- Location: `table:16/row:3/cell:2`

Source:

```text
在相关线程完成执行之前从该线程发射
```

Translation:

```text
Emitted from a related thread before it completes execution
```

## t-018-r001-c001-7209958c07e5

- Location: `table:18/row:1/cell:1`

Source:

```text
函数
```

Translation:

```text
function
```

## t-018-r001-c002-412f54dc38e6

- Location: `table:18/row:1/cell:2`

Source:

```text
描述
```

Translation:

```text
describe
```

## t-018-r002-c002-ea3401d4bcb3

- Location: `table:18/row:2/cell:2`

Source:

```text
绑定地址（host,port）到套接字， 在 AF_INET下，以元组（host,port）的形式表示地址。
```

Translation:

```text
Bind the address (host, port) to the socket. Under AF_INET, the address is expressed in the form of a tuple (host, port).
```

## t-018-r003-c002-181d35a76e68

- Location: `table:18/row:3/cell:2`

Source:

```text
开始 TCP 监听。backlog 指定在拒绝连接之前，操作系统可以挂起的最大连接数量。该值至少为 1，大部分应用程序设为 5 就可以了。
```

Translation:

```text
Start TCP listening. backlog specifies the maximum number of connections that the operating system can suspend before rejecting the connection. This value should be at least 1, and 5 will be fine for most applications.
```

## t-018-r004-c002-aa5e21c1590a

- Location: `table:18/row:4/cell:2`

Source:

```text
被动接受TCP客户端连接,(阻塞式)等待连接的到来
```

Translation:

```text
Passively accept TCP client connections and (blocking) wait for the arrival of the connection
```

## t-019-r001-c001-7209958c07e5

- Location: `table:19/row:1/cell:1`

Source:

```text
函数
```

Translation:

```text
function
```

## t-019-r001-c002-412f54dc38e6

- Location: `table:19/row:1/cell:2`

Source:

```text
描述
```

Translation:

```text
describe
```

## t-019-r002-c002-ea3401d4bcb3

- Location: `table:19/row:2/cell:2`

Source:

```text
绑定地址（host,port）到套接字， 在 AF_INET下，以元组（host,port）的形式表示地址。
```

Translation:

```text
Bind the address (host, port) to the socket. Under AF_INET, the address is expressed in the form of a tuple (host, port).
```

## t-019-r003-c002-96b0b313c2c0

- Location: `table:19/row:3/cell:2`

Source:

```text
主动初始化TCP服务器连接，。一般address的格式为元组（hostname,port），如果连接出错，返回socket.error错误。
```

Translation:

```text
Actively initialize the TCP server connection. Generally, the format of address is a tuple (hostname, port). If a connection error occurs, a socket.error error is returned.
```

## t-019-r004-c002-074cc10954bb

- Location: `table:19/row:4/cell:2`

Source:

```text
connect()函数的扩展版本,出错时返回出错码,而不是抛出异常
```

Translation:

```text
An extended version of the connect() function that returns an error code instead of throwing an exception when an error occurs.
```

## t-020-r001-c001-7209958c07e5

- Location: `table:20/row:1/cell:1`

Source:

```text
函数
```

Translation:

```text
function
```

## t-020-r001-c002-412f54dc38e6

- Location: `table:20/row:1/cell:2`

Source:

```text
描述
```

Translation:

```text
describe
```

## t-020-r002-c001-d2d56f6c9e6a

- Location: `table:20/row:2/cell:1`

Source:

```text
公共用途的套接字函数
```

Translation:

```text
Public-purpose socket functions
```

## t-020-r002-c002-d2d56f6c9e6a

- Location: `table:20/row:2/cell:2`

Source:

```text
公共用途的套接字函数
```

Translation:

```text
Public-purpose socket functions
```

## t-020-r003-c002-c1cfe10fb6ad

- Location: `table:20/row:3/cell:2`

Source:

```text
接收 TCP 数据，数据以字符串形式返回，bufsize 指定要接收的最大数据量。flag 提供有关消息的其他信息，通常可以忽略。
```

Translation:

```text
Receive TCP data, the data is returned in the form of a string, bufsize specifies the maximum amount of data to be received. flag provides additional information about the message and can usually be ignored.
```

## t-020-r004-c002-33fa55efc819

- Location: `table:20/row:4/cell:2`

Source:

```text
发送 TCP 数据，将 string 中的数据发送到连接的套接字。返回值是要发送的字节数量，该数量可能小于 string 的字节大小。
```

Translation:

```text
Send TCP data, sending the data in a string to the connected socket. The return value is the number of bytes to send, which may be less than the string's byte size.
```

## t-020-r005-c002-14f69102e656

- Location: `table:20/row:5/cell:2`

Source:

```text
完整发送 TCP 数据。将 string 中的数据发送到连接的套接字，但在返回之前会尝试发送所有数据。成功返回 None，失败则抛出异常。
```

Translation:

```text
Send TCP data intact. Sends the data in string to the connected socket, but attempts to send all data before returning. Returns None on success, throws an exception on failure.
```

## t-020-r006-c002-0e696b966e9b

- Location: `table:20/row:6/cell:2`

Source:

```text
接收 UDP 数据，与 recv() 类似，但返回值是（data,address）。其中 data 是包含接收数据的字符串，address 是发送数据的套接字地址。
```

Translation:

```text
Receive UDP data, similar to recv(), but the return value is (data, address). where data is a string containing the received data and address is the socket address to which the data is sent.
```

## t-020-r007-c002-96dd13417106

- Location: `table:20/row:7/cell:2`

Source:

```text
发送 UDP 数据，将数据发送到套接字，address 是形式为（ipaddr，port）的元组，指定远程地址。返回值是发送的字节数。
```

Translation:

```text
Send UDP data to a socket, where address is a tuple of the form (ipaddr, port) specifying the remote address. The return value is the number of bytes sent.
```

## t-020-r008-c002-846ba7bbc611

- Location: `table:20/row:8/cell:2`

Source:

```text
关闭套接字
```

Translation:

```text
close socket
```

## t-020-r009-c002-1407df07990d

- Location: `table:20/row:9/cell:2`

Source:

```text
返回连接套接字的远程地址。返回值通常是元组（ipaddr,port）。
```

Translation:

```text
Returns the remote address of the connection socket. The return value is usually a tuple (ipaddr, port).
```

## t-020-r010-c002-26f0a7383e40

- Location: `table:20/row:10/cell:2`

Source:

```text
返回套接字自己的地址。通常是一个元组(ipaddr,port)
```

Translation:

```text
Returns the socket's own address. Usually a tuple (ipaddr,port)
```

## t-020-r011-c002-af2cc29f1d7d

- Location: `table:20/row:11/cell:2`

Source:

```text
设置给定套接字选项的值。
```

Translation:

```text
Sets the value of the given socket option.
```

## t-020-r012-c002-20a3d0c65d8b

- Location: `table:20/row:12/cell:2`

Source:

```text
返回套接字选项的值。
```

Translation:

```text
Returns the value of the socket option.
```

## t-020-r013-c002-ab9fc6dc6d9f

- Location: `table:20/row:13/cell:2`

Source:

```text
设置套接字操作的超时期，timeout是一个浮点数，单位是秒。值为None表示没有超时期。一般，超时期应该在刚创建套接字时设置，因为它们可能用于连接的操作（如connect()）
```

Translation:

```text
Set the timeout period for socket operations. timeout is a floating point number in seconds. A value of None means there is no timeout period. Generally, timeouts should be set when the socket is first created, as they may be used for connection operations (such as connect())
```

## t-020-r014-c002-6d762d81d03f

- Location: `table:20/row:14/cell:2`

Source:

```text
返回当前超时期的值，单位是秒，如果没有设置超时期，则返回None。
```

Translation:

```text
Returns the value of the current timeout period in seconds. If no timeout period is set, None is returned.
```

## t-020-r015-c002-694f88789dfa

- Location: `table:20/row:15/cell:2`

Source:

```text
返回套接字的文件描述符。
```

Translation:

```text
Returns the socket's file descriptor.
```

## t-020-r016-c002-ca975959d76a

- Location: `table:20/row:16/cell:2`

Source:

```text
如果flag为0，则将套接字设为非阻塞模式，否则将套接字设为阻塞模式（默认值）。非阻塞模式下，如果调用recv()没有发现任何数据，或send()调用无法立即发送数据，那么将引起socket.error异常。
```

Translation:

```text
If flag is 0, set the socket to non-blocking mode, otherwise set the socket to blocking mode (default). In non-blocking mode, if no data is found by calling recv(), or the send() call cannot send data immediately, a socket.error exception will be caused.
```

## t-020-r017-c002-3d3757daacd2

- Location: `table:20/row:17/cell:2`

Source:

```text
创建一个与该套接字相关连的文件
```

Translation:

```text
Create a file associated with the socket
```

## t-021-r001-c003-fd20702c73d1

- Location: `table:21/row:1/cell:3`

Source:

```text
大小
```

Translation:

```text
size
```

## t-021-r001-c004-c11d5120dc6a

- Location: `table:21/row:1/cell:4`

Source:

```text
作用
```

Translation:

```text
effect
```

## t-021-r002-c001-938345cf7823

- Location: `table:21/row:2/cell:1`

Source:

```text
Label控件
```

Translation:

```text
Label control
```

## t-021-r002-c004-3efd940ad987

- Location: `table:21/row:2/cell:4`

Source:

```text
显示连接信息：服务器ip与port
```

Translation:

```text
Display connection information: server ip and port
```

## t-021-r003-c001-b1d9c099bd61

- Location: `table:21/row:3/cell:1`

Source:

```text
PushButton控件
```

Translation:

```text
PushButton control
```

## t-021-r003-c004-4f6fa89d5f92

- Location: `table:21/row:3/cell:4`

Source:

```text
连接服务器按钮
```

Translation:

```text
Connect to server button
```

## t-021-r004-c001-b1d9c099bd61

- Location: `table:21/row:4/cell:1`

Source:

```text
PushButton控件
```

Translation:

```text
PushButton control
```

## t-021-r004-c004-6fa4ee7089ad

- Location: `table:21/row:4/cell:4`

Source:

```text
断开服务器按钮
```

Translation:

```text
Disconnect server button
```

## t-021-r005-c001-9a7b8a4e1767

- Location: `table:21/row:5/cell:1`

Source:

```text
TextBrowser控件
```

Translation:

```text
TextBrowser control
```

## t-021-r005-c004-c6201732b06f

- Location: `table:21/row:5/cell:4`

Source:

```text
接收服务器数据
```

Translation:

```text
Receive server data
```

## t-021-r006-c001-f0f59cb7cfbb

- Location: `table:21/row:6/cell:1`

Source:

```text
TextEdit控件
```

Translation:

```text
TextEdit control
```

## t-021-r006-c004-7adfca284677

- Location: `table:21/row:6/cell:4`

Source:

```text
输入发送数据
```

Translation:

```text
Enter sending data
```

## t-021-r007-c001-b1d9c099bd61

- Location: `table:21/row:7/cell:1`

Source:

```text
PushButton控件
```

Translation:

```text
PushButton control
```

## t-021-r007-c004-997e48b40782

- Location: `table:21/row:7/cell:4`

Source:

```text
发送数据按钮
```

Translation:

```text
Send data button
```

